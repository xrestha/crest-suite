# Cross-repo ledger — crest-suite ↔ hss-suite

**Why this file exists.** A sister app, **hss-suite** (an internal operations platform for a
Kathmandu hospitality-support company, at `C:\HSS`), was built by copying this repo's HR module,
payroll engine, settlement and `src/utils/bsCalendar.js`. Copies only stay cheap if fixes flow back.
Until 2026-09-02 nothing on this side recorded that relationship at all — this repo's `CLAUDE.md`
did not mention hss-suite once — so defects found over there had nowhere to land here.

**The rule** (also in `CLAUDE.md` on both sides): *a bug fixed there is still open here until it is
filed in `docs/CROSS-REPO.md` on this side.* The mirror of this file is `c:\HSS\docs\CROSS-REPO.md`.

**Format.** One row per item. *Found in* is where it surfaced, *Affects* is which repo has to act.
Newest at the top; never delete a closed row — a shared defect that came back is worth seeing twice.

## Open — this repo has to act

| Date | Found in | Affects | Item | Status |
| --- | --- | --- | --- | --- |

## Open — waiting on hss-suite

| Date | Found in | Affects | Item | Status |
| --- | --- | --- | --- | --- |
| 2026-09-16 | crest-suite | hss-suite | **A PWA manifest field that is inert on every device you test on, and load-bearing on the one you ship to.** Not HR and not shared code — listed because the shape is portable to any repo with a `manifest.json`, and hss-suite has at least two (its own app and its Staff app). Crest's `public/manifest.json` declared `orientation: portrait-primary` for a year. Desktop Chrome ignores manifest orientation entirely, and that is where the app is developed, demoed and run on a Windows till — so nothing ever surfaced it. Under `display: standalone` on Android, Chrome honours it: a count tablet or an Android POS terminal added to the home screen would have been locked to portrait on a bolted-down landscape screen, with nothing in the app able to override it. S764 set it to `any`. **The transferable half is why the value was there at all**: the second manifest (`staff.webmanifest`, an employee's own phone) is correctly `portrait-primary`, and the two files look like siblings that should agree. They answer to different devices. **Check every manifest in hss-suite for `orientation`, and for any field copied between manifests without asking which devices install that one.** | **OPEN (likely N/A)** |
| 2026-09-16 | crest-suite | hss-suite | **A chime helper that news up an `AudioContext` per call goes silent after about six, with no error.** Not HR and not shared code — listed because the SHAPE is portable to any browser notification either repo grows, and because of how it hid. Crest (S763) had four copies of a two-tone Web Audio beep, each doing `const ctx = new AudioContext()` per call and never closing it. Chrome caps a document at ~6 concurrent contexts, so on any screen opened once and left running — a kitchen board, a till, a host stand — the seventh event of a service made **no sound at all**, silently: no exception, no console line, the visual half still working. It surfaced only when something needed to REPEAT. The fix is one module-level context, `resume()`d on use (a context created before a user gesture starts suspended, and a tab left alone can have one suspended under it). **The transferable lesson is the comment**: the shared file's own header had enumerated three of the inline copies and declined to migrate them — *“each is on a live service screen and none of them is wrong”* — and all three were wrong, identically. A note saying duplicates are fine is a claim nobody has re-checked since it was written. **Check hss-suite for any Web Audio or `new Audio()` notification** (the Staff app's reminder surfaces are the likely home) and for any comment sanctioning duplicates. | **OPEN (likely N/A)** |
| 2026-09-14 | crest-suite | hss-suite | **Payroll Finalize/Reopen as one database transaction; a leaver's staff logins blocked at settlement; blocked Self-Service access that actually ends.** Crest (S753, migration `20260915090000`). (1) `finalize_payroll_run(run, payslip_ids, repayments)` / `reopen_payroll_run(run)`: one transaction under `hr_pay_lock`, refusing unless the stored payslips are exactly the ids the page checked, re-validating the repayment allocation; replaces five browser writes and a post-flip payslip recount. Repayments tagged `payroll_run_id`/`final_settlement_id` can only be written by those functions (`hr_advance_repayments_guard_ledger`) — a manager could delete one over REST. (2) Final Settlement Finalize bans the leaver's linked HR/IMS/POS staff logins (auth.users.banned_until + sessions deleted + `profiles.settlement_blocked_by`), never deletes them — every profiles FK from bills/KOTs/shifts is SET NULL, so a delete blanks the person's name on their history; Reopen unbans its own. Nobody below the Owner finalizes their own settlement. (3) `access_blocked` revokes Self-Service sessions by trigger, and every Staff-app RPC calls `hr_self_service_assert_active()` — the old block only refused the NEXT PIN login. (4) Payslips/settlements store the insurance premiums their tax used; the TDS certificate reads them. (5) Festival/incentive `tds_overridden`. (6) `log_audit()` names the Edge Function caller via an `x-crest-actor` header. **The shared ones to diff: hss-suite's payroll finalize/reopen path, its Final Settlement login handling, and its Self-Service RPCs.** Not ported this session by decision. | **OPEN** |
| 2026-09-14 | crest-suite | hss-suite | **HR Staff, Final Settlement, Gratuity and HR Reports: a "No Access" login that became the Owner, a settlement that paid the final month without SSF or tax, a paid settlement any tab could rewrite, and gratuity offset by SSF that was never paid.** Crest (S752, migration `20260914230000`). (1) Owner detection: a POS PIN login whose rank was cleared passed every negative Owner test — `is_client_owner()`, `isOwner`, `isCallerOwner` and `isLastOwnerLogin` now test `pos_email` too; `admin-user-ops` refuses a staff create/update with no rank, and only the Owner/admin may grant HR manager. The staff pages lost their "No Access" option and their silent load-time re-rank (a mismatch banner + confirmed Apply instead). (2) Final Settlement: the leaver's final month runs through `computePayslip` (OT, SSF, CIT, absence) with a true-up TDS `computeFinalMonthTds`, stored as `month_*` columns the SSF challan, TDS sheet and TDS certificate now read; approved TADA claims are paid by the settlement; notice pay is basic ÷ 30 per day, deducted on resignation and ADDED on termination; leave encashment is earned-to-date this BS year; festival share by completed months (`settlementCompute.js`). Finalize and Reopen are one-transaction SECURITY DEFINER functions that re-check advances, TADA, payroll and overlap under a per-client advisory lock shared with payroll Finalize; a guard trigger makes a finalized row immutable except mark-paid. (3) Gratuity: service is COMPLETED months (the day matters) and the SSF offset is the employer SSF actually stored on payslips × 3.33/20, not an inferred start date (`gratuityCompute.js`, `ssfEnrolment.js`). (4) Self-approval of leave, overtime and advances refused by trigger (Owner/admin exempt); leave stores `decided_by`. (5) `settings` staff role lists writable only by Owner or that module's manager. (6) HR-role logins may read `monthly_periods`. (7) Restore order and Danger Zone: `hr_advance_repayments` after/before `hr_payroll_runs`. **The settlement and gratuity engines are the shared ones — diff hss-suite's `FinalSettlement` and `gratuityCompute`; the missing final-month SSF/TDS and the phantom SSF offset are the money ones.** | **OPEN** |
| 2026-09-14 | crest-suite | hss-suite | **Payroll, Calculation, Festival Allowance, Incentives, Advances and TADA: a trip claim paid in two months, bonuses taxed as the year's only income, a negative take-home, and money tables a supervisor could write.** Crest (S751, migration `20260914210000`). Engine (`payrollData.js`, `payrollCompute.js`, `tds.js`, new `bonusTax.js`): (1) `fetchApprovedTadaMap` matched any claim OVERLAPPING the month, paid-by-payroll included, so a trip from 30 Bhadra to 2 Ashwin was put into both runs — now `status = 'approved' AND end_date <= month end`, so each claim is paid once, by the first payroll after approval. (2) The advance cut is capped at pay left after TDS, and a fixed deduction (CIT) is cut before take-home goes negative (`buildPayrollRows`, one builder for both payroll pages). (3) Payroll's employee list is active/probation OR `end_date` in or after the month, minus a finalized settlement covering it, and no payslip for a month not worked at all (`fetchPayrollEmployees`, `employedInPeriod`). (4) `payslipDrift` treats a TDS difference as an override only when `hr_payslips.tds_overridden` says a person typed it. (5) Festival/incentive tax: a pay month (`bs_month`) decides the fiscal year, the months left are projected at basic + allowances (was basic alone), other finalized bonuses that year raise the base, and `fetchYtdMap` adds finalized bonuses to monthly TDS's YTD. (6) Festival months of service = completed BS months to the festival date (`completedServiceMonths`). DB: manager-rank write policies on every payroll money table; finalized runs, payslips, festival and incentive rows locked (Reopen the only way back); advance repayments capped at what is owed, status synced to the balance, Settle refused while owed, a named Write-off status; TADA claim ladder enforced by trigger (no self-approval, Mark Paid needs a manager, decided claims frozen), `submit_my_tada_claim` refuses duplicates/NaN/reversed dates; `monthly_periods` delete Owner/admin only and never with finalized payroll. Same-session review added: a bonus counts only bonuses paid EARLIER in the FY (`otherBonusesForFy(..., pay)`) so tax does not depend on finalize order; monthly TDS treats bonus tax as settled at source (`ytdBonusWithheld`), or the months after a bonus withheld nothing; Finalize marks TADA Paid before the advance writes; advance cuts and TADA round to paisa; migration `20260914220000` makes the guards stamp approver/paid/write-off fields for the operator too. **Every engine file named here is shared — diff hss-suite's copies; the TADA double-pay and the advance cap are the money ones.** | **OPEN** |
| 2026-09-14 | crest-suite | hss-suite | **Roster, Attendance, Leave and Overtime: rank lived only in the pages, a paid month stayed editable, and the Staff app chose its own leave day count.** Crest (S749, migration `20260914170000`): (1) RESTRICTIVE HR-supervisor write policies on `hr_attendance`, `hr_leave_requests`, `hr_leave_types`, `hr_overtime_entries`, `hr_roster`, `hr_shift_types`, `hr_shift_swap_requests`, `hr_roster_publish_state` — an `hr_role = 'staff'` login could approve leave/OT and rewrite attendance over REST. (2) `hr_attendance_guard_finalized` / `hr_overtime_guard_finalized` refuse any write in a month whose payroll run is finalized (the existence check must sit in the SECURITY DEFINER lookup — under the caller's RLS an HR account sees no `monthly_periods` and it passed vacuously). (3) `submit_my_leave_request` stored the phone's `p_days`; `hr_leave_requests_validate` now derives `days`, refuses overlapping open requests, backwards dates and half-day ranges. (4) One overtime entry per employee per day (payroll summed duplicates). (5) Approving a swap was five browser writes with no status re-check — now `approve_shift_swap`, one transaction. (6) `Roster.jsx` deleted duplicate-named shift types on every load while `hr_roster.shift_type_id` is SET NULL, blanking roster days; shift types in use cannot be deleted. (7) `AttendanceSheet.jsx` had no request guard on period change (a save could land in another month), Clear Day was not scoped to listed staff, a status change to Absent/Leave/Off kept the day's OT (which `tallyAttendance` pays from every row), and the bulk buttons overwrote approved leave. (8) Second pass: public holidays inside a leave are no longer charged — the DB gained `bs_months` (generated from `bsCalendar.js`) and `bs_to_ad()`; `request_shift_swap` refuses past, unpublished and already-requested days (migration `20260914180000`). (9) `computePayslip` now pays a `holiday` row to daily (1 day) and hourly (8 h) staff, and `zeroHourStatus` returns Off rather than Holiday for an unrecognised zero-hour shift. **The HR module was ported — check each shape in hss-suite, starting with the Roster dedupe-delete and the self-service leave RPC.** | **OPEN** |
| 2026-09-14 | crest-suite | hss-suite | **The SSF deposit deadline changed from 15 to 25 days, and a deleted seeded holiday came back on the next Seed.** (1) The Act to Amend Some Nepal Acts, 2082 (Nepal Gazette 2082/04/14, 30 July 2025) amended s.4(4) of the Contribution-based Social Security Act: employers deposit within **25** days after the month ends, not 15. Crest's HR Dashboard SSF card, its tooltip, Help and the HR guide all said the 15th; S748 added `SSF_DEPOSIT_DAY = 25` in `payrollConstants.js` and read it everywhere. Confirmed from a law-firm summary quoting the amendment; the Law Commission's consolidated text was unreachable. (2) Holiday Calendar's Seed is name-keyed, so deleting the Terai Holi (as the seed report tells a hill outlet to do) was undone by the next Seed. S748 made Remove a `hr_holiday_calendar.removed_at` stamp (migration `20260914160000`), added a Removed list with Put back / Delete for good, moved Seed's rules into a tested `planSeed()`, and filtered `removed_at IS NULL` in Overtime.jsx and demandForecastData.js; S750's browser check found Put back left "not added back, because you removed it" on screen, so it now drops that name from the Seed report. **Check hss-suite's HR dashboard for the 15th, and whether it carries the Holiday Calendar and its seed.** | **OPEN** |
| 2026-09-14 | crest-suite | hss-suite | **Saving the Employee form wrote the whole loaded row back, and a hidden end date zeroed a permanent employee's pay.** `EmployeeForm.jsx` saved `{ ...form }` where `form` was the row as the list loaded it (up to 10 minutes old from the page cache here), so an unrelated edit reverted basic salary, bank, SSF, pay basis, `access_blocked`, and a Final Settlement's `status`/`end_date` made in the meantime. Separately, the end-date picker rendered only for Contract/Part-time while the value stayed saved, and `computePayslip`'s `daysAfterExit` pays a monthly employee nothing after `end_date` — a contract hire switched to Permanent kept an invisible date and their pay stopped at it. Crest (S748): `employeeFormData.js` `changedEmployeeFields()` sends only fields the form owns that actually changed; pay/bank/SSF are no longer form fields; End Date shows whenever set, with a warning when past on an active employee; manual Deactivate / Resigned / Terminated name the Final Settlement consequence. **Check hss-suite's ported `EmployeeForm.jsx` — both shapes are likely there.** | **OPEN** |
| 2026-09-14 | crest-suite | hss-suite | **Pay Setup's form could wipe an employee's allowances, and its previews used the SSF flag alone.** `PayForm.jsx` read `hr_salary_components` with `if (!data) return`, and Save deletes then re-inserts the set — so a failed read, or Save pressed before the read landed, deleted every allowance (dearness included) and inserted nothing. `PaySetup.jsx`/`PayForm.jsx` computed SSF on `ssf_enrolled` alone while payroll needs a number too (`isSsfContributor`), labelled a pre-tax figure "what the employee actually receives", hid probation staff on the default tab, and gave daily/hourly rows one cell too many (colSpan 2 under SSF Employer). Crest (S748): Save refused until the component read is `ok`; previews on `isSsfContributor` with an "SSF no. missing" chip; "Net before income tax"; On payroll / All / Not on payroll tabs; read errors surfaced and paged. | **OPEN** |
| 2026-09-14 | crest-suite | hss-suite | **A CIT / provident-fund deduction came off take-home pay but never off taxable income.** `tds.js` put SSF alone into the retirement-contribution bucket (lower of NPR 5,00,000 or a third of income), while Nepal's Income Tax Act treats SSF, EPF and CIT as ONE bucket and Pay Setup's chip told owners CIT reduces taxable income. Crest (S748, decided with Aashish): `hr_salary_components.retirement_fund` marks a deduction; `retirementContributionOf()` in `payrollCompute.js`; `hr_payslips.retirement_contribution` stored so YTD sums real figures; `retirementRelief()` in `tds.js` is the one cap, used by monthly TDS and Final Settlement, and since S750 by Festival Allowance and Incentive Run through one shared `projectBonusTaxableBase()` (both pages had carried their own SSF-only copy). Migration `20260914150000`. **`tds.js` and `payrollCompute.js` are shared engine code — check hss-suite's copies.** | **OPEN** |
| 2026-09-14 | crest-suite | hss-suite | **Deleting an employee destroyed their finalized pay history, and left their Self-Service PIN working.** Five pay tables (`hr_payslips`, `hr_final_settlements`, `hr_advances`, `hr_advance_repayments`, `hr_festival_allowances`) are `ON DELETE CASCADE` from `hr_employees` with no guard, and any HR login of any rank could issue the delete over REST. `profiles.hr_employee_id` is `SET NULL`, so the login outlived its employee and `hr-selfservice-login`'s `hr_employees?.access_blocked` read `undefined` — the PIN kept signing in. Crest (S748): `hr_employees_guard_delete` BEFORE DELETE trigger + `employee_pay_history()` lookup (refuses pay history or a Self-Service login; lets a whole-client deletion cascade), `hr-selfservice-login` refuses an unlinked login, one Self-Service login per employee (unique index + Edge Function check). Also Holiday Calendar writes need HR supervisor rank, a unique `(client, date, name)` index and an audit trigger. **hss-suite's schema is separate — check its FKs and its login function.** | **OPEN** |
| 2026-09-14 | crest-suite | hss-suite | **Festival Allowance and Incentive runs gave the SSF tax treatment on the enrolment flag alone.** Payroll treats an employee as an SSF contributor only when enrolled AND holding an SSF number; `FestivalAllowance.jsx` and `IncentiveRun.jsx` waived the 1% first slab and projected SSF relief on the flag alone, so an enrolled employee with no number was taxed differently on a bonus than on salary. Crest's fix (S747): one exported `isSsfContributor(employee)` in `payrollCompute.js`, used by `computePayslip`, both pages (whose employee reads now select `ssf_no`), PayrollRun and PayrollCalculation; a test reads the four files and fails if one returns to the flag. Stored draft TDS keeps the old figure until regenerated. Still inline and equivalent here: `gratuityCompute.js`, `laborForecast.js`, `HrReports.jsx`, `PayslipBody.jsx`; flag-only previews in `PaySetup.jsx`/`PayForm.jsx`. **Check hss-suite's copies of both pages — they are ported files.** | **OPEN** |
| 2026-09-14 | crest-suite | hss-suite | **A credit note's revenue reversal took back the full menu price.** Not HR, but listed in case hss-suite ever carries POS: the bill had posted revenue AFTER its discount, so a discounted bill's credit note removed more than it added, and with no open period the reversal was skipped silently. Crest (S747): `creditNotePosting.js`, `pos_credit_notes.ims_posted_at`, `sales_entries.pos_credit_note_id`. **Almost certainly not applicable** — hss-suite has no POS — recorded so a future port does not reintroduce it. | **OPEN (likely N/A)** |
| 2026-09-11 | crest-suite | hss-suite | **An approval that writes to a SECOND table has a precondition the first write does not, and the shortfall is silent in the table nobody reads for it.** Found in Crest on 2026-09-11 (S741), reported from a screenshot. Approving leave stamps the request AND writes an `hr_attendance` row per day — and the second write is the one that matters, because `payrollCompute`'s `unpaidDays` is summed only from rows that EXIST, so a day with no row is a paid day, and the attendance sheet never reads `hr_leave_requests`. Those rows hang off `monthly_periods`, and one open period per client means leave approved two or three months ahead has nowhere to write. It was approved anyway (correct) under a banner saying *"create the period(s), then re-approve"* — **neither half of which was possible**: the unique index refuses the early period, and an approved row has no Approve button. Nothing back-filled, so an approved UNPAID leave was paid in full when its month finally opened, with the Leave page and the attendance sheet each looking correct on their own. The fix runs the write at the one moment it becomes possible, i.e. when the period is minted, from BOTH creation paths. Four things generalise past leave: **the deferred write belongs at the moment its precondition is satisfied**, not behind a warning asking a human to remember; **it fills only what is absent** (a months-old approval must never overwrite a hand-marked day); **it reports rather than throws**, as its own failure stage, since the period must open either way and a best-effort second write's silence proves nothing; and **a back-fill does not see what is already in the ground**, so a separate gap finder surfaces the backlog — split into "waiting for the month" (nobody's to act on, say so) and "month exists, days missing" (actionable), with a failed check rendering as a failed check rather than as the reassuring empty answer. Also: a warning that asks for an impossible action is worse than no warning. Worth checking on the hss side wherever an approval, a posting or a settlement writes into a period-scoped table — the question is what happens when the parent period does not exist yet, and whether anything ever goes back for it. | **OPEN** |
| 2026-09-11 | crest-suite | hss-suite | **A terminal status with no action on its row is an undo that does not exist, and the mis-click that reaches it destroys the record's history.** Found in Crest on 2026-09-11 (S740). `LeaveManagement.jsx` rendered a literal `—` in the Actions cell for `rejected` and `cancelled`: no control there, and no other screen could move the row either, though the status CHECK constraint has always allowed `pending` back. Cancel sits on the same row as Approve, so one mis-click ended a request permanently — the only recovery was re-typing it, losing the dates, the employee's stated reason, `created_at` and the audit trail of who decided what. **The same module already had the answer**: Overtime's row carries an `Undo` that sets `pending`, and `FinalSettlement` has `Reopen` — Leave was the outlier, which is the shape worth checking for on the hss side (grep each approval queue for a status whose row renders no action). Three choices in the fix generalise. **Reopen to PENDING, never straight back to APPROVED**: approval is the only writer of the `hr_attendance` rows and the cancel deleted them, so a restore that jumped the queue would show an approved leave over an attendance sheet with those days blank — and payroll reads the sheet, so an unpaid leave stops deducting and a daily-wage paid leave goes unpaid. **Re-read the row's status before writing**, the mirror of the guard the decide path already had: if another session reopened *and* re-approved it, writing `pending` un-approves a leave whose days stay marked and paid — and a FAILED read must refuse too, not fall through. **Rank it above the rank that decides**: a supervisor decides a request, undoing a decision that already cleared attendance and moved a balance is manager-and-above (`hasHrAccess('manager')`, which Owner and admin both satisfy). Note for the port: hss-suite's Overtime `Undo` is at the page's own supervisor rank with no confirm and no stale re-read, so aligning the two is part of taking this one. | **OPEN** |
| 2026-09-11 | crest-suite | hss-suite | **Fixing a whole-row settings save on ONE screen leaves it on the other screen that edits the same columns — and an allow-list that omits a column is a card that writes nothing and reports success.** Found in Crest on 2026-09-11 (S739), and it is the second half of the 2026-09-10 S730 row below, which should be read first. S730 replaced the whole-row write with an allow-list of the columns the page owns. Two things went wrong that generalise to any ported settings screen. **First, the allow-list was built from the tab list and a tab was missed**: the Support tab's lower card edits three consultant columns, they were never added, so `pagePatch()` always returned `{}`, the `if (Object.keys(patch).length)` guard skipped the write, and the button still flipped to "✓ Saved" — a whole editing card that had written nothing for a week, for fields read on three other surfaces. An allow-list is still the right default (it fails by omitting rather than by leaking) but its failure is equally silent, so **assert that every tab's columns appear in the list** — a dev-only loop over a tab→fields map is enough, and it is also what keeps an unsaved edit alive across a post-save reseed. **Second, the validation was written over the UNION of the page's fields**, so a threshold check ran on every save against values in the STORED row, and refused an admin's branding save for any tenant whose row held a `0` or an inverted warn/critical pair — values saveable before S730, over fields that tab does not show. It then navigated to a tab absent from that role's tab set, leaving no tab rendered as selected. **Scope both the write and its validation to the tabs the VIEWER has**, not to the page. **Third, and the part worth grepping for directly: the second editor.** Crest's admin drawer edits the same branding/property/consultant columns plus thresholds and a QR payload, and all four of its Save buttons were still sending the row whole from a `select('*')` load — S730's exact defect, unfixed, on the screen an operator actually uses. The tell was a naming difference: the drawer called three columns "Upgrade Contact" where the settings page called them "this client's consultant". **Two names for one column set is usually two editors, and the second one rarely carries the same fixes.** Its logo handlers were missing the other half of S730 too (`await save(...)` with no catch, and the saver throws, so a refused write left the button on "Uploading…" for good and reported nothing). | **OPEN** |
| 2026-09-11 | crest-suite | hss-suite | **A value rendered at READ time is a change to every document already issued, and nothing on the edit screen says so.** Found in Crest on 2026-09-11 (S739). `pos_orders` stores only the invoice number and fiscal year; the printed document number and the bill TYPE are both assembled when a bill is PRINTED, from the settings row current at that moment — so editing the invoice prefix re-numbers every invoice the tenant has ever issued (`TI2238-CAC-82/83` → `…-CASA-…`), and switching the VAT-registered flag off reprints past Tax Invoices as plain PAN bills with the breakdown gone. Both were ordinary text/checkbox fields whose tooltip said "edit if you want something different". Neither edit is wrong to allow — a company registers for VAT, a code was typed wrong on day one — so the answer is a confirmation naming what reaches history, not a refusal. **The general test: is the stored value rendered at write time or at read time?** Anything resolved at read time is retroactive, and a tax-document number is the worst case of it. The counter-example done right in the same repo is the monthly owner report, which snapshots the flag at generation. **Relevant to any hss-suite screen that prints a numbered or dated document from live settings** — a payslip header, a settlement statement, a certificate — and the audit is mechanical: for each field on a settings screen, grep its column name in the print/HTML builders and see whether the builder reads it or the row does. | **OPEN** |
| 2026-09-11 | crest-suite | hss-suite | **A "0 is a real value and survives on purpose" contract has to hold at EVERY reader, or the field that relies on it is only safe on the ones that were checked.** Found in Crest on 2026-09-11 (S739). The plan-price table deliberately keeps a stored `0` (a tier priced at zero is a live configuration, and the resolver tests `typeof === 'number'` rather than `|| fallback` for exactly that reason, with a comment saying so). One reader downstream had `imsPrices[c.plan] || 0`, so an absent tier counted as free — which mattered the moment the form was changed to let a cleared box mean "use the shipped default" by removing the key. `??` per field, the way the resolver already falls back per field, keeps a deliberate 0 and falls back only on absent/null. **Two transferable halves.** The form half: `'' → 0` made a cleared box publish a free plan off one keystroke while the placeholder advertising the default was unreachable — if blank is meant to mean "default", blank must remove the key, and an explicit 0 is worth confirming because it is indistinguishable from a slip. The reader half: **when a module documents that a falsy value is meaningful, grep every consumer for `|| 0` and `|| default` before trusting the contract** — a single `||` in one consumer silently exempts itself, and the screens disagree rather than break. Same family as the 2026-09-10 S724 row about absence collapsed to `0`, from the other direction: there a real absence became a flattering zero, here a real zero became a default. | **OPEN** |
| 2026-09-11 | crest-suite | hss-suite | **A TEST tenant is indistinguishable from a paying customer, so the platform MRR figure counts one.** Supersedes the 2026-09-02 row that read *"Delete the dummy `BHATTI CHOILA` client row"* — **do not delete it.** Confirmed with the owner 2026-09-11: it is the practice outlet the product is being built and tested against, and it is the ONLY one (Amol's Cafe, Casa Acai Cafe and Longtail e-media are all real customers). The old row's mechanism was also a version out of date: `it-crest-sync`'s upsert is `onConflict: 'crest_client_id'`, so create-or-update is right and a delete on the HSS side is still undone by the next sync — but commit `8808ab7` added a churn pass that stamps `cancelled_at` on any row absent from the payload, so a delete in Crest now DOES propagate. That closes the loophole and makes the advice worse, not better: the prescribed action would work, and it would destroy the test tenant. **The real defect is that nothing in the pipeline marks a tenant as not-a-customer.** `billing-export` sends every client row (correctly — HSS needs to see a client go inactive rather than vanish), HSS prices it like any other, and `crestMrr()` sums it. The ledger put it at **Rs. 10,000 of Rs. 17,900**, so more than half of the headline figure on the one screen meant to be trusted at a glance is money nobody pays. **The fix belongs on the HSS side and touches Crest not at all**: an `is_internal`-style column on `it_saas_clients`, plus a fifth reason in `crestBillable()` ahead of the existing four. Two properties make that the cheap answer — the sync's upsert writes a FIXED column list, so anything outside it survives every run (which is also why the obvious manual workaround fails: `cancelled_at: null` IS in that list, so hand-cancelling the row in HSS is wiped on the next sync); and `crestBillable()` is already the one rule both the MRR tile and the Crest Clients list read, so a single condition fixes both. **Rejected alternatives**: deleting it loses the test tenant; `is_active = false` in Crest works for HSS's arithmetic but `getAccessState()` then returns `locked`, and `!isAdmin && accessState.locked` shuts every login belonging to that client out of the product — fine for admin view-as, useless for testing the real client experience. | **OPEN** |
| 2026-09-11 | crest-suite | hss-suite | **A conditional write is not a guard: zero rows matched returns `{ data: null, error: null }`, and an audit trigger's noise filter decides what is invisible forever.** Found in Crest on 2026-09-11 (S738), both on one screen. First: `.update({…}).eq('id', x).eq('status','open')` with the result destructured for `error` alone. The `.eq('status','open')` was written as a safety guard, but PostgREST matching nothing is not an error — so when the period closed under the open editor the page closed the editor, reported a save and reloaded the unchanged row. `.select('id')` plus a `!data?.length` branch is the whole fix, and unlike a dead fetch this one IS proof the write did not land, so the message may say so and name the reason instead of inviting a blind retry. **Relevant here because every finalize/reopen in a ported payroll, festival, incentive or settlement screen is exactly this shape** — `.eq('status','draft')` is the same filter doing the same silent nothing when someone else finalized first; `grep -rn "\.eq('status'" src/ \| grep -v select` finds them. Second, and the reason it went unseen for a year: `log_audit()` returned NULL for every `monthly_periods` UPDATE where `OLD.status = NEW.status`, on the reasoning that *"only a status transition is audit-worthy"*. Thirteen tables hang off that row by `period_id`, so relabelling a period's BS month moves a whole month of purchases, sales, attendance and payroll into a different reporting month in one write — the benign open/closed flip was logged and the data move was not. **An audit trigger's noise filter is an allow-list decision wearing a deny-list's clothes**: it is written to silence churn, and what it actually decides is which writes can never be reconstructed. Re-read every `RETURN NULL` in hss-suite's copy of `log_audit()` and ask what else that predicate catches — Crest's `profiles` and `pos_orders` filters are column allow-lists and are fine; the period one was a whole-shape skip. Third, smaller but portable to any month-walking code: **"the previous month" resolved as `bs_month - 1` is wrong wherever a month can be missing**, and a gap is normal (a company that paused, a month never run). Crest's repair button pointed at a month that would never exist, on precisely the clients it existed for. Resolve neighbours by sorting the rows that EXIST — the YTD tax projection that builds each month on the last is the obvious candidate here. | **OPEN** |
| 2026-09-10 | crest-suite | hss-suite | **A loader that returns `data` alone makes a failed read and a missing row the same `null`, and the screen that renders that null as defaults will save the defaults.** Found in Crest on 2026-09-10 (S736). `loadClientSettings` returned `data` from a `maybeSingle()` with `error` discarded; the admin drawer rendered the null as `SETTINGS_DEFAULTS` (blank name, blank VAT number, default thresholds, blank payment QR), marked the load as done so it never re-read, and any of four Save buttons on three tabs then wrote those blanks over the tenant's real row — the S730 whole-row-save shape with a failed read as the stale snapshot. A separate secret read on the same path was dropped the same way and re-upserted as null on every Save. **Three rules**: a loader throws or returns `{ data, error }`, never `data` alone (the tell is `const { data } = await …` inside a function whose name starts with `load`); a screen that could not read must render a failure with a Retry in place of the form, because a form is an invitation to Save; and Save refuses until a read has succeeded, checked against a flag the failure path never sets. Second from the same pass, for any screen with quick-extend buttons on a date (a contract end, a leave balance, a probation end): **"+N days" must extend from the LATER of today and the current value**, or pressing it before the date arrives shortens what was there — Crest's subscription renew buttons computed from today for months while the trial panel beside them had always done it right. **Relevant here because the settings loaders were ported with the HR module**: `grep -rn "const { data } = await" src/ \| grep -i "load\|fetch"` finds the first shape; a `setDate(getDate() + n)` on `new Date()` beside a date input finds the second. | **OPEN** |
| 2026-09-10 | crest-suite | hss-suite | **A pending-approval count of zero is the manager's good news, so a failed read hands them the most reassuring screen the product can draw.** Found in Crest on 2026-09-10 (S734). The shared hook behind both dashboards' Leave/OT/TADA/Swap tiles ran four `head: true` counts and destructured `{ count }` without `{ error }`; a refusal or a dropped connection returns `count: null`, `\|\| 0` makes it a zero, and the tiles then rendered **"0 · all clear" in green**. The general rule (a failed read is not an empty list) is old on both sides — what is new is the case where **empty IS the outcome the reader wants**, so the failure does not merely show a wrong number, it tells them not to open the queue. Such a tile needs a THIRD rendering distinct from both the good state and the skeleton, and a shared hook must RETURN the failure rather than swallow it, because each consumer says it differently. **The same screen had it again one row down**: Headcount rendered "Active Staff 0" in green over "no probation" and "Basic Payroll / Month NPR 0" whenever the employee read failed, because `empStats` was built from `(emps \|\| [])` regardless and the `?? '—'` fallback the cards already carried could never be reached. **Directly relevant — the HR module and its dashboard were ported**: check every `{ count }` destructure with no sibling `error`, and every `(rows \|\| [])` that feeds a tile whose zero reads as healthy. Two smaller ones from the same pass, both on figures rather than queues: a KPI that bands a ratio must reach for the shared band helper and render its `✓/△/▲` (a fourth local copy of the labour ladder had drifted to different thresholds, so one screen said amber where another said green), and a MASTER-DATA read feeding a KPI needs paging as much as a transaction one — it is a lookup map, so a truncation does not shorten a list, it prices rows at zero and makes the business look better. | **OPEN** |
| 2026-09-10 | crest-suite | hss-suite | **A settings screen that saves the WHOLE row writes every other screen's columns as they stood when it loaded — and nothing fails.** Found in Crest on 2026-09-10 (S730). `settings` is one row per tenant that nine pages write their own columns onto; the main Settings page seeded a form from the row at load and sent all of it on Save, so an owner who left Settings open while a manager changed a list on another screen, then saved a threshold, put the row back the way it was. Both saves reported success; the toast said saved; the manager's change was simply gone. The same page had already been fixed once for the mirror image (a platform-wide value landing on the tenant row) without anyone asking what ELSE the whole-row write carried. **Three transferable rules**: a save sends only the columns the screen edits, as a diff against the row it loaded (never `{ ...settings, one_column: x }`, which is the same defect wearing a fresher snapshot); a re-read after a save must not reseed a form wholesale, or it wipes what was typed on another tab — keep the values that differ from the *previous* seed, and replace everything only when the row belongs to a different tenant; and a threshold whose readers are `parseFloat(x) \|\| default` cannot store 0 (the box says 0, every report bands at the default), so refuse 0 at the form, store a cleared box as NULL — Postgres refuses `''` for `numeric` — and check the pair (critical > warning). **Relevant here because the HR module was ported and its settings writers came with it** (`TadaSettingsModal`, `HrStaff`'s custom roles both write one column and are fine); the candidate is any hss-suite screen that spreads a loaded settings object into its save, or any `useEffect([settings])` that does `setForm({ ...settings })`. Second, smaller, from the same pass: three bulk "renumber every row" loops did `await update(...)` with nothing read back and reported "✓ Renumbered N" whatever the database said — one of them under a per-tenant unique index that refused the second row every time. `grep -rn "await supabase" src/ \| grep -v "const {"` finds the shape. | **OPEN** |
| 2026-09-10 | crest-suite | hss-suite | **An absence collapsed to `0` becomes the most flattering figure the screen can print, and a banding function will happily band it.** Found in Crest on 2026-09-10 (S724), five times, in a subsystem that had already been fixed for exactly this twice. The shape is `const cost = someMap[id] || 0` — written to be defensive, and the `0` it substitutes is indistinguishable downstream from a measured zero. Every consumer is a ratio, and each one turned the unknown into good news: `cost / price` became **0% food cost**, which the threshold helper painted green with a ✓ and labelled *Healthy*; `(revenue − cost) / revenue` became a flat **100% margin**, which sorted the unmeasured item to the **top** of a "best performing" ranking; and a `> target` test could never fire, so the item was **silently absent** from a list of problems and the page rendered a celebratory "nothing is wrong" empty state over data nobody had entered. **The transferable rules**: carry the absence as `null` from the function that resolves it all the way to the cell, because the first `\|\| 0` in between destroys the distinction and no care downstream recovers it; and when a value is resolved by more than two call sites, make it a function — the reason all five sites disagreed is that the resolution was two lines of arithmetic retyped, and each copy got a different amount of it right. **Relevant here because payroll is full of the same shape**: a missing rate, a missing allowance, a missing attendance count, each divided into or compared against something. | **OPEN** |
| 2026-09-10 | crest-suite | hss-suite | **A dynamic import that fails after a deploy is not a crash, and on a click handler it is not even visible.** Found in Crest on 2026-09-10 (S732). Both repos are CRA apps that route-split with `React.lazy` and import `xlsx` inside export handlers, so the app fetches its own code by hashed filename from an `index.html` served before the deploy; the host replaces those files and any service worker's `activate` deletes the cached copies on the same event. **The two halves land in different places and only one is reachable from React**: a lazy ROUTE rejects during render, so an error boundary catches it and shows a generic crash card — accurate about the reload, wrong about the cause, and it sends the reader to support for something that is not broken. An `await import('xlsx')` inside an `onClick` is thrown from an event handler, which a React boundary **structurally cannot catch**, so the Export button simply does nothing and no error is reported anywhere. Crest had 43 such call sites. Fix is `src/shared/chunkReload.js` (a predicate over the webpack/Chrome/Firefox/Safari wordings, a reload at most once a minute, never while offline where the reload is answered from the cached shell and fails identically) plus an `unhandledrejection`/`error` listener registered in `index.js` for everything React cannot see. **The predicate must refuse ordinary errors** — `TypeError: Failed to fetch` and a plain null-property read are both asserted in the test — or a real crash is hidden by the mechanism meant to recover from it. Not one of the four shared modules, but it is a property of the build shape rather than of any module, so it applies to hss-suite if it is also CRA + `React.lazy` + a dynamic `xlsx` import. | **OPEN** |
| 2026-09-10 | crest-suite | hss-suite | **`sessionStorage` survives a sign-out in the same tab, so a per-tab cache keyed on the tenant serves the previous USER's data.** Found in Crest on 2026-09-10 (S731). Crest's `sessionDataCache.js` keys on `page_section_clientId` — no user id — and its own header claimed *"nothing survives past this tab/session, and stale data can never linger across a sign-out/sign-in as a different client"*. The claim is true for a different CLIENT and false for a different ACCOUNT of the same one: `sessionStorage` is scoped to the tab, not to the session, so signing out and back in served the cache straight back. **That is an isolation failure rather than staleness**, because the staff-isolation policies in both repos are RESTRICTIVE SELECT filters — the rows a staff rank must not see are exactly the ones an Owner's cache hands them, and no query runs to be refused. Fix is one line, `sessionStorage.clear()` in `signOut()`; Crest's `switchOutlet()` had been doing it for the outlet version of the same problem for months, which is the tell to look for — a clear that exists on one privileged transition and not the other. Check any hss-suite cache keyed on a tenant id rather than a user id, and check what its comment claims. | **OPEN** |
| 2026-09-10 | crest-suite | hss-suite | **A push notification `tag` groups REPEATS; a constant tag collapses everything one feature sends into one lock-screen entry.** Found in Crest on 2026-09-10 (S731). Crest's service worker defaulted to `tag: 'crest-hr'` and `hr-push` had never sent a tag of its own, so all four kinds it sends — Roster Published, Shift Swap Request, the coworker's response and the manager's decision — replaced one another: a swap request arriving on top of an unread roster publish **deleted it**, and the employee never saw the first. The shared tag was deliberate and the reasoning was sound (republishing a roster twice in a minute should not leave two identical entries); the defect is that it was applied to the feature rather than to the repeat. Fix is a tag per kind, keyed per record where several can be live at once — `crest-hr-roster` shared on purpose, `crest-hr-swap-<request id>` per request — with the worker falling back to a title-derived tag so an un-redeployed function still behaves. **Applies only if hss-suite ported the roster/self-service push**, which post-dates the original HR copy (Crest added it in S307); if it did, the worker's default and the sender are two separate places to check and the sender is the one that had never been looked at. | **OPEN** |
| 2026-09-10 | crest-suite | hss-suite | **`.update(...).eq('client_id', ...)` on a settings-style singleton row silently succeeds when the row does not exist.** Found in Crest on 2026-09-10 (S724). A per-tenant preference was saved with a bare `update` filtered on the tenant id, with the result never destructured. For any tenant whose settings row had not been created yet the update matched **zero rows and returned no error**, so the control kept the new value on screen and reverted on the next visit, with nothing said. An RLS refusal produced the identical silence. Every other settings writer in that repo already used the correct shape and this one had simply never been brought onto it. **The transferable rules**: a write to a row that may not exist needs read-then-branch (`select id` → `update` by primary key, else `insert`), the read's own error must **refuse** rather than fall through into the insert (falling through writes a *second* singleton row and silently changes what every later read returns), and the failure message must not claim the write did not land — a response lost after the server committed proves nothing, so say what to check instead. **The cheap audit**: grep for `.update(` filtered on a tenant/owner id rather than a primary key, and for any `await supabase...` whose result is not destructured. | **OPEN** |
| 2026-09-09 | crest-suite | hss-suite | **A postgrest-js builder that is never awaited sends nothing, and a whole feature can live on that for a year.** Found in Crest on 2026-09-09 (S715). `supabase.from('recipes').update({ … }).eq('id', r.id)` inside a `forEach`, with no `await` and no `.then()`. postgrest-js issues the request **inside `then()`** (`PostgrestBuilder.then` is where the `fetch` call lives), so a builder used as a bare expression statement is an object that is constructed and dropped — no request, no error, no log. The write had been dead since the day it was written; the column it targets was NULL for every client, and a paid feature in another module that reads that column had never had data to work with. Nothing surfaced it, and a later migration that backfilled a value in that same column ran against zero rows without anyone noticing. **The check is one grep and it is worth running here**: any `supabase.from(...)` chain ending in `.update(...)`, `.delete(...)`, `.insert(...)` or a filter, that is a statement rather than an `await`ed / `.then()`ed expression. It reads exactly like a deliberate fire-and-forget write, which is why review does not catch it — the shape a real fire-and-forget needs is `void p.then(…, onErr)`, not a bare builder. **Relevant here because the ported HR module has the same idiom available everywhere**, and the most likely victims are exactly the writes nobody watches: a `last_seen_at` stamp, a status flag, a denormalised cache column, an audit breadcrumb. | **OPEN** |
| 2026-09-09 | crest-suite | hss-suite | **Making a shared helper throw does not make its callers catch — and the failure mode is a hang, which no error branch will ever report.** Found in Crest on 2026-09-09 (S715). A shared cost engine was changed to `throw` on a failed read (a good change: it had been returning a map of zeros, which every caller rendered as a real figure). The commit recorded that "the callers that already catch it need no change" — and four page-level callers did not catch it at all. Each had the shape `setLoading(true) … await helper() … setLoading(false)`, so the rejection escaped **before** the flag was cleared: the page sat on its loading state indefinitely, with no error card, nothing to retry, and no way for the user to tell it from a slow network. Note the direction — these were pages that had already been swept for the "a failed read must not render as an empty period" rule and had a correct `loadError` branch; the throw simply routed around it. **The transferable rules**: when you make a shared function throw, "existing callers already handle it" is a grep for `try` around every call site, not an assumption; and any `await` between a `setLoading(true)` and its `setLoading(false)` needs either a `catch` or a `finally`, because an unhandled rejection there is indistinguishable from the network being down forever. **Relevant here because the payroll and settlement compute helpers are shared, ported, and sit behind exactly that flag pattern.** | **OPEN** |
| 2026-09-09 | crest-suite | hss-suite | **A rule written down does not reach a private copy of the thing it governs — and hss-suite is, by construction, a private copy.** Found in Crest on 2026-09-09 (S713). `recipeCostCalc.js`'s sub-recipe cost walk had a real bug fixed months ago (a cycle guard that was a *visited* set where it had to be a *path* set, so a base sub-recipe reached down two branches of one tree costed **0** the second time); the fix landed, a rules file documented it, and `MenuPricing.js` went on quietly running its own private copy of the same walk, still in the pre-fix shape, under-stating food cost against every other screen on the one page where that number sets a price. **Being private is precisely why it never received the fix** — a rule about how to write a guard only reaches the guards someone opens. The transferable half is the counting: grep for the *shape*, not the *name*, and expect the count to be higher than the shared module suggests. **Relevant here because this entire repo is that situation at a larger scale**: the payroll engine, settlement and `bsCalendar.js` were copied, so every fix Crest has made to those since the copy is a candidate for exactly this. The concrete ask is not to port this bug (recipes are not shared) but to run the census — for each shared engine, diff the two copies rather than assuming the ledger has caught everything, since the ledger only records what someone noticed. | **OPEN** |
| 2026-09-10 | crest-suite | hss-suite | **A plpgsql function body is NOT validated at `CREATE` time, so a migration can apply green and ship a function that fails on its first call — and a `prosrc LIKE '%…%'` assertion cannot catch it.** Found in Crest on 2026-09-10 (S735): `save_purchase_bill` gained `SELECT max(po_id) INTO …` over a uuid column in S709; Postgres has no `max(uuid)`, but plpgsql resolves each statement on first execution, so the migration ran clean, the source-text assertion passed, and every purchase-bill edit failed with `42883 function max(uuid) does not exist` for a day. Two things to carry over: when a migration changes a function body, the verification block should CALL it once inside `BEGIN … ROLLBACK`; and `42883` at runtime does not always mean an unapplied migration — an applied function calling something that does not exist raises the same code. For "the one non-NULL value in this set" of a uuid column use `WHERE col IS NOT NULL … LIMIT 1`, not `min`/`max`. Grep hss-suite's migrations for `min(`/`max(` over uuid columns. |
| 2026-09-09 | crest-suite | hss-suite | **A ratio computed from a zero numerator is not a ratio, and a banding function will band it as the BEST case.** Found in Crest on 2026-09-09 (S713). `fcPct = exVat > 0 ? (cost / exVat) * 100 : 0` — with no cost, that is a real `0`, `0 <= warning threshold`, and the cell printed **`0.0% ✓` in green**: the single most flattering possible rendering of "we do not know what this dish costs". It also sorted those rows to the top of an ascending worst-margin sort and exported them to Excel as `0.0%`. The page's own *+ Add Item* created rows with no cost, so it manufactured its own examples. The band function already returned `—` for `null`; the whole bug was the `: 0` upstream, and **the fix is to carry the absence as `null` from where the figure is computed all the way to the cell** — the first `? :` that defaults a missing input to `0` destroys the distinction and no care at the call site gets it back. **Relevant here because the HR module was ported and HR is full of banded ratios**: labour cost %, overtime as a share of basic, attendance %, any per-employee average with a headcount denominator. The tell is a `: 0` or `|| 0` in the same expression that computes a percentage, and a "healthy"/green band whose lower bound is open. Worth checking the SORTS too, not just the cells: Crest's New FC % sort already guarded `cost > 0` while the cells and the FC % sort did not, which is the tell that the author knew and the knowledge did not travel three lines. | **OPEN** |
| 2026-09-09 | crest-suite | hss-suite | **A "replace the whole set" save that discards both write results can destroy the set and still report success.** Found in Crest on 2026-09-09 (S713). `savePairings()` ran `delete(where parent=X)` then `insert(new rows)` with **neither** result destructured — so a refused insert after a successful delete left the item with nothing, while the modal closed on "Saved" and wrote the intended list into local state, so no screen ever said otherwise. supabase-js **resolves** with `{ data, error }` and does not throw, so a `try/catch` around it would not have helped either. Three changes, all portable: compute a **diff** so only what the user actually changed is touched (a failure can then never wipe a list they did not edit), put the **additions first** so a half-completed save leaves more than it takes, and read every error. **Relevant here because the HR module was ported**: the shape is any editor that saves a *collection* belonging to one parent — a roster week's shifts, a payslip's components, an employee's allowances or deductions, a settlement's lines. Find it with `grep -rn "\.delete()" src/` and look for an `insert` on the same table within the next few lines; the tell is a bare `await supabase...` with nothing destructured. Note this is the DROPPED-ERROR half, distinct from the ORDERING half already filed above from S711 — a save can have the right order and still lie about the outcome. | **OPEN** |
| 2026-09-09 | crest-suite | hss-suite | **A delete that clears a record's children before attempting the record itself destroys them whenever the parent delete is refused — and the refusal is the normal case, not the edge one.** Found in Crest's Recipe Costing on 2026-09-09 (S711): `deleteRecipeNow()` deleted `recipe_ingredients`, then deactivated the mirror item, then deleted the `recipes` row. `sales_entries.recipe_id` is a plain FK with no `ON DELETE`, so Postgres refuses that last step for **any dish that has ever sold** — which is most of them — leaving the recipe alive with an empty ingredient list and an error message politely telling the user to re-enter them. The two irreversible steps ran before the one that could still be refused. The fix is ordering, not a better guard: delete the parent FIRST and let `ON DELETE CASCADE` take the children in the same transaction, so a refusal leaves every row intact. Same conclusion Crest reached from the other direction in S709, where a hand-rolled two-request cascade on Purchase Orders was replaced by the FK's own. **Relevant here because the HR module was ported**: the shape to look for is any delete that clears child rows in application code before deleting the record they hang off — an employee with payslips, a settlement with its components, a roster week with its shifts. Check with `grep -rn "\.delete()" src/` and read what runs *after* each one; the tell is a second `.delete()` on the parent further down the same function, or an error message that apologises for a state the ordering created. Two further halves worth copying: enumerate the referencing tables in the app rather than trusting the database to stop you (they do not agree — Crest's `recipes` has seven referencing columns split across refuse / cascade / SET NULL / **no FK at all**, and only `confdeltype` tells you which), and offer the soft path in the refusal message, since a record with history is almost always one the user wanted to hide rather than destroy. | **OPEN** |
| 2026-09-09 | crest-suite | hss-suite | **`bsCalendar.js` is no longer byte-identical across the two repos — crest added `formatAdAsBs(iso)` (S709), an ADDITIVE export; the 88 `BS_CALENDAR` rows are untouched and still identical.** Take the function or ignore it, but know the files have diverged by one helper before the next diff is read as damage. The finding behind it is the part that transfers: **a date is picked in BS, stored as AD, and was being displayed raw as AD.** Crest's Purchase Orders printed `2025-08-31` as the Expected Delivery both in its list and on the PO document handed to a supplier — a calendar nobody in Nepal entered, on an outgoing document. This is the *display* half of the `adToBs` vs `adToBsSafe` row already open on this side: that one is about which converter, this is about whether a stored `date` column reaches the screen through a converter at all. `formatAdAsBs` is `adToBsSafe` + `BS_MONTHS` + an explicit local-midnight parse (`new Date('2025-08-31')` is UTC midnight, which at +05:45 is still the 30th), falling back to `YYYY-MM-DD (AD)` out of the verified table. **Where to look in hss-suite**: any `date` column rendered straight into JSX — HR is the ported module, so join dates, dates of birth, leave from/to, advance dates, settlement dates and TADA claim dates are the candidates; the tell is a bare `{row.some_date}` or `.slice(0, 10)` with no converter in the expression. Crest itself still has two hand-rolled copies of the same composition (`SelfServiceHome.jsx`'s `fmtBs`, `LeaveManagement.jsx`) — both ported files, so hss-suite likely has them too, and they are the right place to start. | **OPEN** |
| 2026-09-09 | crest-suite | hss-suite | **A `count: 'exact', head: true` check is NOT subject to the 1000-row cap; the row read it is validating IS — so a validation can say "all N are present" while the operation that consumes those rows sees 1000.** Found in Crest's period close on 2026-09-09 (S705). `closingCountPreflight()` counts `closing_stock` rows with `head: true` and the close dialog prints *"All 1,203 active items have a closing count"*; `carryForwardOpeningStock()` then read the rows themselves with a bare `.select()`, hit PostgREST's `db-max-rows` of 1000, and carried 1,000 of them into the new month — the other 203 opened at zero, with no error and nothing in the data to say so. Crest had deliberately aligned those two queries' *predicates* in S616 so "the sentence and the carry-forward cannot disagree", and the row cap un-aligned them anyway: **an invariant asserted between two queries is only as true as the smaller query's limit**, and a count and a read are never the same size above 1000. **Relevant here because the HR module was ported**: the shape to look for is any preflight/validation that counts rows and then hands the same predicate to something that reads them — a payroll run validated on an attendance or payslip count before summing those rows is the obvious candidate, and both are per-employee-per-day or per-employee-per-month, which crosses 1000 inside one real company-year. Crest's fix is `fetchAllRows(makeQuery)` (`src/shared/fetchAllRows.js`), which takes a **function** returning a fresh builder (a supabase-js builder is a one-shot thenable) and requires a **unique tiebreaker in the sort** or paging repeats a row on one page and skips it on the next. Two things worth copying beyond the helper: the cap returns **no error**, so every `if (error)` guard written against a failed read passes happily over a short one; and the regression test must build a **full** first page (1000 rows) or it passes against the unfixed code. Unverified from this side — check with `grep -rn "head: true" src/` and read what each count is used to justify. | **OPEN** |
| 2026-09-08 | crest-suite | hss-suite | **Every Suite client's synced `monthly_amount` changed on the 2026-09-08 sync, upward — done, figures below.** Crest's `billing-export` had priced Crest Suite Pro as a bundle REPLACING the module sum (starter 5,300 / growth 5,800 / pro 6,500) — the model Crest retired in S552 — so a Growth client on all three modules plus Suite synced as **6,500** where Crest itself said **8,700**. Fixed AND deployed on the Crest side 2026-09-08 (S703), so the next sync carries it: Suite is now additive at `plan_prices.suite`, so `monthly_amount` and `module_breakdown.suite` both move for those clients and `next_renewal_at` may come EARLIER (it used to report Suite's date alone; it is now the earliest of every active window). Nothing in hss-suite has to change to accept it — `it-crest-sync` stores the payload as sent and `crestBillable.js` reads `monthly_amount` — and **nothing has been invoiced off the old figure**, so there is no correction to make — observed on that screen 2026-09-08: all four synced clients sit in `Unlinked (4)`, and `crestBillable`/`Send to Finance` refuse without a `customer_id`, so no revenue_ledger row can exist yet. The understatement is prospective only. Seen live before the fix shipped: BHATTI CHOILA and CASA ACAI CAFE both at **Rs. 6,500.00** — the retired `pro` bundle price exactly, one flat figure on two clients with different subscriptions — against Longtail e-media at **Rs. 7,100.00**, which is `2,600 + 2,400 + 2,100` and therefore proof the function already read the admin's live `plan_prices` for the modules while Suite alone was frozen. **After the deploy and a Sync now, same screen**: CASA ACAI CAFE **7,900** (`3,500 IMS Pro + 2,400 HR + 2,000 Suite`), BHATTI CHOILA **10,000** (`3,500 + 2,400 + 2,100 POS + 2,000 Suite`), Longtail and the switched-off client unchanged, and the *Crest Suite MRR* tile 13,000 → **17,900**. Next-renewal dates did not move for either, so no client's earliest window was being hidden behind Suite's in practice — the guard is right, it just had nothing to catch here. **One real display bug follows from the same retired model**: `CrestClients.jsx:46-53`'s `planSummary()` returns EARLY on `suite_plan` — `if (row.suite_plan) return [{ label: 'Suite ' + row.suite_plan }]` — so a Suite client's row shows a single `Suite pro` chip and no IMS/HR/POS badges at all, against a `monthly_amount` that now visibly includes those modules. Its own comment states the assumption (“a full Suite plan against a la carte modules”), which is exactly the bundle model Crest retired. Suite should be an ADDITIONAL chip beside the module ones, not a replacement for them. Second, smaller: `suite_plan` is `NULL \| 'pro'` and nothing else since Crest's S548, so that label renders “Suite pro” — a tier label on an axis with one tier; “Crest Suite Pro” is the product's own name for it. Two tidies beyond that: `pricing_basis` no longer emits `'suite_bundle'` (only `'per_module'` / `'none'`), so the column comment at `supabase/migrations/20260722000003_it_department_crest_billing.sql:46` and the field list at `docs/BUILD-LOG.md:8000` are stale, and `CrestClients.jsx:305` prints the value straight through — verified display-only, no branch on it anywhere in that repo. | **OPEN** |
| 2026-09-07 | crest-suite | hss-suite | **`Tip`'s wrapper span is `display: inline` with a dashed `border-bottom`, and around a BADGE in a flex row that is worse than cosmetic.** The wrapper — not the chip — is the flex item, so it stretches to the row height while the chip inside stays at its natural height at the TOP, and the dashed rule then draws at the bottom of the stretched box, well below the chip. The chip reads as a different SIZE from its neighbours and nothing in its own styles explains it. `hss-suite/src/components/Tip.js:24` carries the identical line (`borderBottom: '1px dashed var(--theme-text3)'`), **no `<Tip>` in that repo passes a `style` override at all**, and at least five sites wrap a chip: `EmployeeList.jsx:398,402` (the ported sibling of the file fixed here, and its badge sits in a flex row beside a Tip-wrapped `<button>` that Tip correctly leaves undecorated, so one chip wears a dashed rule and its neighbour does not), plus the `⚠ OT ×2?` / `⚠ Stale` chips in `PayrollCalculation.jsx:289,294` and `PayrollRun.jsx:702` — those three already set `cursor: 'help'` on the chip itself, which suggests the author expected Tip's affordance and not its geometry. Fix is the override crest's `UsageChip.jsx` has always carried: `style={{ display: 'inline-flex', borderBottom: 'none', cursor: 'default' }}` (`style` is spread last over Tip's defaults). **Verify per site rather than sweeping**: a Tip around a real word in a sentence SHOULD keep the dashed rule, and only the flex-row cases are broken. Worth knowing how this survived here: three rounds of headless measurement all reported the chips equal, because the harness rendered a bare `<span>` while the live one sits inside a `Tip` — it agreed with itself every time and was not measuring the thing on screen. | **OPEN** |
| 2026-09-07 | crest-suite | hss-suite | **There is no `@media (pointer: coarse)` block anywhere in hss-suite, so the whole authenticated app is desktop-density on a tablet — the top bar shipped on 2026-09-07 included.** `grep -rn 'pointer: coarse' src/` returns nothing. `.app-topnav` is `display: none` below 768px, but **a coarse pointer is not a width**: a touchscreen laptop or a tablet in landscape gets the BAR, not the drawer, so `.app-topnav-pill`, `.app-jump`, the account chip and the section triggers are all mouse-density under a finger — while the drawer's own 44px bumps (`.app-sidebar .sidebar-link`, `.app-sidebar-footer-actions`) sit inside the `max-width: 768px` block, which a tablet never enters. Crest hit this twice and both were measured, not reasoned: **S603** found the 16px input floor had never left `Login.css` (below 16px iOS Safari zooms the viewport on focus and never zooms back, which on a till turns “enter the quantity” into “now pan sideways to find Save”), and **S682** found the floor reaching only the controls that had *not* opted out — eleven checkboxes set `width: 15` in an inline style object, and no selector beats an inline style, so a floor written without `!important` silently exempts exactly the controls it was written for. Crest's top-bar port added `.topbar-pill`, the context and account triggers and `.topbar-modules .module-tab` to its coarse block for this reason. Scope the tier to the input method, never to a width — a narrow desktop window should keep its own density. | **OPEN** |
| 2026-09-06 | crest-suite | hss-suite | **`'en-NP'` is not a Nepali locale — every money figure formatted with it renders Western grouping.** `new Intl.NumberFormat('en-NP').resolvedOptions().locale` is plain `en`; the region subtag carries no grouping data, so `(1248650).toLocaleString('en-NP')` is `1,248,650`, not `12,48,650`. Crest had 261 such sites and 52 local one-line formatters (`const fmt = n => Math.round(n \|\| 0).toLocaleString('en-NP')`) — **the HR payroll, advances, festival, incentive, gratuity, settlement and self-service pages all carried one**, and those files were copied into hss-suite. A further 146 sites passed no locale at all and rendered in the *viewer's browser* locale (`1.248.650` on a German-locale laptop). `en-IN` is the locale whose Latin-digit grouping matches Nepal's; `ne-NP` groups correctly but prints Devanagari digits. Crest's fix is `src/shared/nepalMoney.js` (`npr` / `nprInt` / `npr2` / `nprExact` / `nprOrDash` / `nprWords`) with each local `fmt` replaced by an alias (`const fmt = nprInt`) so call sites did not change, plus a source-reading test that fails on any money `toLocaleString` naming `en-NP`/`en-US` or any bare `toLocaleString()` off a `new Date(`. **Test the locale in one line of Node before trusting it** — this shipped for a year, and DESIGN.md had derived a layout floor from a grouped figure the product never rendered. Check with `grep -rn "'en-NP'" src/`. Note it is a visible change to every figure; Crest's owner chose Indian grouping deliberately. | **OPEN** |
| 2026-09-03 | crest-suite | hss-suite | **Component tests are impossible on react-scripts 5 + react-router 7 until two unrelated things are fixed, and neither failure names its cause.** Crest had 3 component tests against 43 suites and the reason turned out to be mechanical, not cultural. First, `react-router-dom@7.17` declares `"main": "./dist/main.js"` — **a file it does not ship** — and Jest 27 resolves by `main` because it predates the `exports` field, so any test importing a routed component dies with `Cannot find module 'react-router-dom'`. Second, once that resolves, `react-router` touches `TextEncoder` at import time and the jsdom bundled with react-scripts 5 does not define it, so the next failure is `ReferenceError: TextEncoder is not defined` pointing at the same import line. Fix is two `moduleNameMapper` entries in `package.json` (map `react-router-dom`, `react-router` and `react-router/dom` to their real `dist` files) plus a `require('util')` shim in `setupTests.js`. **Check the installed versions first** — this is a packaging bug in specific releases, not a permanent property. Not one of the four shared modules, so file it only if hss-suite is on the same CRA/router pair; if it is, the payoff is that every page in the product becomes testable, which is how Crest caught a legal-consent link regression the same day. | **OPEN** |
| 2026-09-04 | crest-suite | hss-suite | **A `.btn` with no colour variant renders as browser chrome, and the badge classes title-case prose.** Two faults in the shared class-system lineage, found in Crest's `Layout.css` on 2026-09-04. (1) `.btn` declares padding, radius, weight, cursor, focus ring and `:disabled` but **no `background` and no `color`** — a variant (`btn-primary`/`-ghost`/`-danger`) supplies those. Written bare it inherits: on a `<button>` the UA's `#f0f0f0`-on-black chrome, on an `<a>` the UA link styling, which measured **1.81:1** on a dark card against AA's 4.5:1. It is the same silent shape as `badge-green`-written-alone, on the class that never got that fix. (2) The `.badge-*` box carries `text-transform: capitalize`, right for `paid` and wrong for anything with a verb in it — a two-sentence warning rendered "Do Not Send An Agreement To A Client Until These Are Filled In". Crest added `.badge-sentence` for the second and treats a bare `className="btn"` as a bug on sight. **Relevant here because the HR module was ported from this repo**, so its screens carry Crest-shaped `btn`/`badge-*` usages; whether hss-suite's own `Layout.css` has the same gap is unverified from this side. Check with `grep -rn 'className="btn"' src/` (no variant beside it) and for `text-transform: capitalize` on the badge box. | **OPEN** |
| 2026-09-02 | crest-suite | hss-suite | **Every clock time is rendered in the viewer's timezone, not Nepal's.** Crest had 18 byte-identical inline `toLocaleTimeString('en-US', { hour: '2-digit', minute: '2-digit' })` call sites and zero occurrences of `Asia/Kathmandu` in `src/`; each rendered in the **runtime's** zone, so an operator viewing a tenant's data from outside Nepal saw every time 5h45m out with nothing on the page saying so. HR is the ported module, so check attendance/roster/self-service time renders there — `rosterHelpers.js:fmtTime` is NOT affected (it formats a Postgres `time` string, not an instant), but anything formatting a `timestamptz` is. Two further traps came out of the fix: (1) pinning the time forces pinning the DATE beside it, since `adToBs()` reads a Date's local getters and a 00:15 Kathmandu instant otherwise renders under the previous BS day; (2) `.getHours()` used for hourly bucketing has the same fault and shifts a whole distribution. Crest's fix is `src/shared/nepalTime.js` — deliberately NOT in `bsCalendar.js`, because that file is mirrored byte-for-byte and a time helper there would have to land in both repos at once. Copy the module or write an equivalent; do not add it to the shared calendar file. | **OPEN** |
| — | — | — | Nothing open. | — |

## Closed

| Date | Found in | Affects | Item | Status |
| --- | --- | --- | --- | --- |
| 2026-09-02 | hss-suite | crest-suite | **The BS calendar's correctness check is four anchors; the round-trip sweep is not a second one.** `src/utils/bsCalendar.test.js` walks every day from 1943 to 2031 (>32,000) and requires `AD → BS → AD` to round-trip. That proves the converter pair is a **bijection**, not that the table is right: both directions read the same `BS_CALENDAR`, so a mistyped month length round-trips perfectly while every date after it is off by a day. The test's own comment calls the sweep "the real guard", which overstates it. Reality is checked only by the 4 `ANCHORS` (line ~124), of which two are Baisakh 1. A single non-compensating bad row between 1943 and 2022 *is* caught by the 2022 anchor, but it localizes nothing, and a compensating pair (+1 in one month, −1 in another) or a bad row after 2083 is caught by nothing. **Proposal: add Baisakh 1 for every BS year 2000–2083 against the published Nepali New Year AD dates** — 84 independent reality anchors, which localize a bad row to a single year. Leave 2084–2087 out: they are deliberate extrapolations in both repos. | **CLOSED 2026-09-14 (S753)** — `bsCalendar.test.js` now asserts Baisakh 1 for every BS year 2000–2083 (84 anchors), each read from five agreeing calendar publishers (hamropatro, rat32, ashesh, nepcal, englishtonepali; timeanddate/calendarific/officeholidays also for 2072–2083). All 84 match the table. Caveat recorded in the test: for 2000–2071 the publishers likely share one table, so those anchors prove agreement with the published calendar, not a primary gazette. |
| 2026-09-02 | hss-suite | crest-suite | **Display sites still call `adToBs()` instead of `adToBsSafe()`.** `bsCalendar.js:192` states the rule in its own comment — *"Anything that DISPLAYS a converted date must go through `adToBsSafe()`, not `adToBs()`"* — and `adToBsSafe` exists (line 201), but the call sites were left as a "separate follow-up" when the table was extended. Counted 2026-09-02: **65 raw `adToBs(` call sites against 11 `adToBsSafe(`**, across POS print HTML, credit notes, KOT/Covers reports, IMS vendor confirmations, Roster, Leave, TADA, Advances and self-service. Not all 65 are display — several are computation (`taxPoolCompute.js`, `useSalesPivotData.js`, `backfillPosToIms.js` bucket by BS day and are fine) — so this is an audit, not a find-and-replace. Out of range, `adToBs()` does not throw; it returns a confident wrong date. | **CLOSED 2026-09-14 (S753)** — 32 display sites across 21 files now use `adToBsSafe()` (an out-of-range date shows the AD date marked (AD)). Left on `adToBs()` by design: every grouping/filter/write site. Left as known both-shown-and-computed sites: `demandForecastMath.js` holiday lookup, `SalesReport.jsx`/`CoversReport.jsx` daily keys, `PosReservations.jsx` (feeds `bsDayBoundaryIso`), `SelfServiceHome.jsx` roster keys, `Roster.jsx` column keys. Also noticed, not changed: SalesReport/CoversReport build BS labels from `new Date('YYYY-MM-DD')` (UTC midnight — a day early for viewers west of UTC). |
| 2026-09-11 | hss-suite | crest-suite | **Payroll's stat grid can show every figure except what the month costs.** `payrollCashCost()` (gross + OT − absence + employer SSF) is shared HR code and identical in both repos, but in hss-suite it was read ONLY by Finalize's confirm and the Finance expense row, so the total cost of a payroll period was first visible inside the confirmation of the one action that needs a Reopen to undo. The MD asked why Finalize quoted Rs. 1,84,868 against a Net Payable of Rs. 1,76,572 — the gap being the employee SSF and TDS the company withholds and remits, plus employer SSF. hss-suite added a **Cost to Company** tile on Payroll and Payroll Calculation reading that same function with the same rounding, and extended the printed register's footnote. **Not a bug — the arithmetic was always right — so this is a port only if Crest's payroll screen has the same gap; check before copying.** | **CLOSED 2026-09-14 (S753)** — Crest had the same gap. Payroll Run now carries a Cost to Business card from `payrollCashCost()` in `payrollData.js` (pay earned = gross − absence + OT, plus employer SSF; travel claims shown apart, since they are reimbursements, not pay). Crest had no `payrollCashCost` before — the ledger's "identical in both repos" was wrong on this side. Tests in `payrollData.test.js`. |
| 2026-09-02 | crest-suite | crest-suite | **Settlement Reopen is gated on `isAdmin`, locking out the person accountable for it.** `src/modules/hr/settlement/FinalSettlement.jsx:1009` renders the Reopen button behind `{isAdmin && (`. Here `isAdmin` is `profile.role === 'admin'`, the **Crest platform operator** — the tenant's own Owner is `isOwner` — so a client cannot reopen their own finalized settlement and has to contact support. **This is a missed sibling, not a new discovery**: `PayrollRun.jsx:536`, `FestivalAllowance.jsx:282` and `IncentiveRun.jsx:287` were already fixed to `hasHrAccess('manager')` and each carries a comment explaining exactly this reasoning. `FinalSettlement.jsx` is the only bare `isAdmin` permission gate left in `src/modules/hr/`. hss-suite fixed the same class of bug in four files on 2026-07-29. | **CLOSED 2026-09-14 (S752)** — Reopen is now `reopen_final_settlement(id, reason)`, a SECURITY DEFINER function allowing the Owner or an HR manager (`hr_is_manager_rank()`), reason required; the employee stays marked as left. |
| 2026-09-11 | hss-suite | crest-suite | **An advance is deducted from every open payroll run from the moment it exists, including a month before it was issued.** `src/modules/hr/payroll/payrollData.js:151` `buildAdvanceMap()` filters `hr_advances` on `status === 'active'` alone and never compares `issued_date` with the run's BS period, so an advance issued on 1 Bhadra was deducted from a still-open Shrawan run (found in hss-suite on 2026-09-11 with real data; the function is byte-identical here). Finalize then allocates the slip's deduction onto the employee's oldest active advance, which can be one not yet in recovery, and the Calculation page agrees with the wrong figure because it shares the function. hss-suite's fix: `firstRecoveryMonth(issuedDate)` = issue BS month + 1 (Chaitra rolls into Baisakh), `advanceDueIn()`/`dueAdvances()` as the ONE filter every per-period reader uses (deduction, Finalize's allocation, the breakdown panel's count), `buildAdvanceMap(advances, repayments, period)` **throwing** without the period rather than returning an empty map, and the Advances screen saying which payroll the first cut lands on. The start rule itself is a business decision (hss-suite's MD chose *the month after*, day irrelevant); Crest's tenants may want it configurable, but the bug — deducting before issue — is not a preference. | **FIXED here 2026-09-14.** Crest adopted the SAME rule as hss-suite (owner's decision): first recovery is the BS month after the month of issue, day irrelevant, Chaitra rolls into Baisakh. Ported `firstRecoveryMonth` / `advanceDueIn` / `dueAdvances` into `payrollData.js`; `buildAdvanceMap(advances, repayments, period)` now throws without a period; Payroll Run's deduction and Finalize's allocation and Payroll Calculation's deduction and breakdown-panel count all read `dueAdvances()`; Advances & Loans names the payroll the first cut lands on. Final Settlement deliberately left alone — an exit settlement recovers every outstanding advance regardless of issue month. Tests in `payrollData.test.js`. |
| 2026-09-08 | crest-suite | crest-suite | **`billing-export` prices Crest Suite as a BUNDLE that replaces the module sum — the model this repo retired in S552 — and hss-suite bills off that payload.** `supabase/functions/billing-export/index.ts:26-30` holds a `SUITE_BUNDLES` table (starter 5,300 / growth 5,800 / pro 6,500) and `computeBilling` returns early on `pricing_basis = 'suite_bundle'`, ignoring IMS, HR and POS entirely. On this side `clientMrrBreakdown` has ADDED Suite to the module sum since S552, so for any client with `suite_plan = 'pro'` the two disagree outright: a Growth client on all three modules plus Suite is 8,700 here and 6,500 in the export. S701 made every other price live — the function already reads `settings.plan_prices` for IMS/HR/POS, and Suite now has a `plan_prices.suite` key it does not read. The function is pasted into the Supabase dashboard editor (no build step, no shared import), so it is a deliberate second copy and has to be edited by hand; that is exactly why it went stale. Fix is to delete `SUITE_BUNDLES`, read `planPrices?.suite ?? 2000`, add it to the per-module sum, and drop the `pricing_basis` early return. **Verify what HSS does with `pricing_basis`** before removing the value — if it branches on `'suite_bundle'`, that side needs the same release. | **FIXED here 2026-09-08** (S703). Suite is additive and priced from `plan_prices.suite`; `SUITE_BUNDLES` and the `pricing_basis = 'suite_bundle'` early return are gone; `next_renewal_at` now considers every active window rather than Suite's alone. `clientMrr.test.js` reads the file and asserts all of it, so the copy cannot drift silently again. **Deployed 2026-09-08** by hand in the dashboard (project `dkgusgktifwnqmqkkshz`), the only route it has — smoke-tested live afterwards: `POST` with no secret returns `401 Invalid secret` and `GET` returns `405 POST only`, so it booted and its auth path is intact. That proves the code is live, not that the figures moved; the sync itself is what shows that. |
| 2026-08-13 | hss-suite | both | `BS_CALENDAR` covered only BS 2079–2087. Outside that range the converters do not throw — `bsYearLength()` fell back to a flat 365 and `daysInBsMonth()` to a flat 30, so they returned a confident *wrong* date, worsening the further back you went. Surfaced there on a date of birth (`1980-01-04` displaying as `15 Poush 2036`, which is really 30 Dec 1979). This repo held the same table and the same bug. | **APPLIED here 2026-08-15** (`dfd785b`), extending the table to BS 2000–2087. Verified 2026-09-02: 88 year rows, all identical to hss-suite's. |

## What is shared, and what is not

Shared, so a fix on either side is a candidate for this ledger:

- `src/utils/bsCalendar.js` — the BS↔AD converters and `BS_CALENDAR` (**all 88 year rows identical
  across the two repos**, verified 2026-09-02). **Rows 2084–2087 are extrapolations in both** and
  must not be "corrected" against another library on either side. The FILE has diverged by one
  additive export since 2026-09-09 (`formatAdAsBs`, filed above); the table has not.
- `src/modules/hr/**` — payroll, settlement, leave, roster, TADA, advances.
- The payroll engine's Nepal constants (SSF, TDS slabs) — both companies are NPR/Nepal Pvt. Ltd.

**Not** shared, so a difference is intentional and not a defect to file:

- **Tenancy.** This repo is multi-tenant (`client_id` everywhere, `useScopedDb()`); hss-suite is a
  single company and keeps a shim with the same API that ignores the `client_id` argument.
- **Roles.** Here `isAdmin` is the platform operator and the tenant's Owner is `isOwner`; there,
  `isAdmin` is a compat alias for `isMd`, that one company's Owner. **A permission gate copied
  verbatim between the repos changes meaning** — which is precisely the first open item above.
- This repo's IMS and POS modules, and hss-suite's revenue verticals, Finance and Admin — neither
  exists on the other side.

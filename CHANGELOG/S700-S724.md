# Session log — S700–S723

24 entries, 2026-09-08 to 2026-09-10. Newest first, as in the original `README.md`.
Full range list: [CHANGELOG/README.md](README.md).

---

### S723 — 2026-09-10 — The payables reports re-analysed: half a bill valued as a whole one, and a letter whose headline could not be tied to its own schedule

**No migration, no Edge Function deploy.** `CACHE_NAME` and `APP_VERSION` both bumped to
`crest-v259`.

Outstanding Payables, Purchase 1L+ and Vendor Balance Confirmation — the three reports that answer
what is owed to a supplier and what was bought from them. Two of the three are sent outside the
business: the Annexure 13 disclosure is filed with the IRD, and the confirmation letter is signed by
the vendor and kept as NSA 17 audit evidence.

**A partial payment split a bill across both tabs, and each tab re-applied the whole bill discount
to its half.** `paid_at` is stamped PER LINE and `allocatePayment` settles a bill's lines
oldest-first, so paying part of a multi-line bill stamps some lines and not others — and Outstanding
Payables' two tabs are `.is('paid_at', null)` and its complement. The bill's grand total was then
recomputed from whichever lines that tab had loaded, and `calcBillTotals` is **not linear in the
bill-level discount**. Measured on a 2-line VAT-inclusive bill of NPR 2,034 carrying a 200 discount,
part-paid 1,017:

```text
Outstanding then showed   Bill Total 904.00   Remaining 904.00   Paid —   (no payment history)
truth                                          1,017.00          1,017.00 already paid
```

113 short of what was owed, the recorded payment invisible on the bill it belonged to, and the same
bill simultaneously in Paid History at 904 as a settled one. The tab filter now only SELECTS which
bills to show: the page refetches the rest of each bill by `purchase_group_id` and values it over
all its lines, so Bill Total, Paid, Remaining, the item count and the payment history are the whole
bill's on both tabs. Which tab a bill belongs to is a property of the bill — outstanding while any
line is unsettled — keyed on `paid_at` rather than on `remaining > 0`, since a bill settled before
`payable_payments` existed carries the stamp and no payment rows and would otherwise be dragged back
in as a full balance owed.

**The two reads that decide whether a bill is paid were unpaged, directly beneath a comment
explaining why the read above them is paged.** `payable_payments` and `vendor_returns` were both
bare `.in('purchase_entry_id', ids)` on Outstanding Payables and on Vendor Balance Confirmation.
`payable_payments` holds one row per LINE per settlement, so it outgrows the bills it hangs off; a
truncated payments read renders paid bills as unpaid and inflates Total Remaining, and a truncated
returns read overstates what is owed on every returned bill. Neither returns an error, so no guard
on either page would have fired. The id list also rides in the URL — on Paid History that is every
settled credit line the client has ever recorded, and on the letter every credit line ever billed to
that vendor. Both are `fetchAllRowsChunked` now. Same producer-and-consumer shape as S706/S708: the
read the page was named after was paged and the reads that hang off it were not.

**Money the vendor owes BACK was clamped to zero in two places and carried in a third.** Returning
goods against an already-settled bill leaves the supplier owing you a credit — `ReturnsTab` has no
guard against it, nor should it, since that is what a credit note is. Outstanding Payables clamped
it with `Math.max(0, value − paid)` per line, and `computeOpeningBalance` clamped it per bill, while
the FY schedule applies returns with no clamp at all — so the same credit note appeared in the
letter if it fell after Shrawan 1 and vanished if it fell before it. Decision, Aashish 2026-09-10:
show it. Both clamps are gone; a bill in that state wears a purple **Credit** badge with the amount,
Paid History carries a **Vendor Credits** card (rendered only when there is one), and the pay form
is replaced by a sentence naming the amount and what to do with it — previously Save on such a bill
was a button that did nothing silently, since `allocatePayment` skips every line at or under EPS.

**"Payments (FY)" on the letter counted money that appeared nowhere in the schedule below it.**
`totalPaymentsFy` added every Cash/FonePay bill's net settlement, but those bills printed in the
schedule only as *purchases* — so the headline box could not be reconciled against the supporting
table on a document whose entire purpose is being reconciled, and the schedule's own Amount total
summed cash purchases that never touch the balance, making it a figure that tied to nothing. A cash
bill now emits its settlement as its own **Paid on purchase** row, net of its returns and moving the
running balance by nothing. Every payment total is read off the schedule, and the Amount column
totals to exactly (Closing − Opening).

**A `.neq` on a nullable column, on the same letter.** The cash-bill read was
`.neq('payment_method', 'Credit')`, and `payment_method` is NULL on bills written before the column
existed — `NULL <> 'Credit'` is NULL, not true. Those bills appeared in NEITHER read, so a vendor's
legacy cash purchases were simply absent from the FY's Purchases total. Filtered positively now, and
`groupRawBills` resolves NULL to Cash once rather than at each of the four places downstream that
compare or print it (the schedule was rendering "Purchase (null)").

**`const TODAY = new Date().toISOString().split('T')[0]`, at module scope**, the last one in `src/`.
Wrong twice: `.toISOString()` is UTC, so between midnight and 05:45 NPT it returns *yesterday* —
the hours a restaurant closes its books — and a module constant never changes for the life of the
tab, which on an installed PWA left open across a service is longer than a day. It is the default
`paid_at` on every payment recorded here, and a settlement slipping across Shrawan 1 lands in the
wrong fiscal year on the confirmation letter. Now `formatAd(nepalCivilDate(new Date()))`, evaluated
when it is asked for, the helper `PosReservations` already uses.

**The Annexure 13 "Bills" column counted line items.** `buildVendorSummary` incremented per
allocated LINE, so a vendor with six seven-line bills read as 42 — on the figure an accountant ties
back to the purchase register. It counts distinct bills now, via a `billId` that
`allocateBillDiscounts` was already computing and discarding. Also fixed in VAT Report's and Non-VAT
Report's `# Bills` columns, which share the helper.

**The one-lakh threshold was tested on the ex-VAT net only**, so a vendor at 95,000 taxable plus
12,350 VAT — 107,350 actually invoiced, and what their own ledger shows — was disclosed by neither
column. Decision, Aashish 2026-09-10: show both and flag on either, the conservative filing
position. The report gains **VAT** and **Total Invoiced** columns (screen, totals row and workbook),
`Net` is relabelled `Net ex-VAT`, and the flag fires when either crosses.

Smaller, same pass: Vendor Balance Confirmation's four error paths ran their setters without
re-checking `isCurrent`, so an older vendor's failed read wiped the letter the reader had moved on
to; the 1L+ report's empty state said "No VAT purchases in FY" on a report that deliberately covers
VAT and non-VAT alike, and its stale error survived a switch to a fiscal year with no periods; and
Outstanding Payables' "Last Settlement" card and Paid History row order both relied on fetch order,
which stops being true once a bill's lines are merged from two reads — both are explicit now.

`vendorBalanceHelpers.test.js` is new: that ledger is the most intricate arithmetic in the reports
folder and its only check was a dev-only `console.assert` grading the walk against a restatement of
the walk, which cannot catch a figure that is consistently wrong — and every defect above was
consistently wrong. Seven tests, built on the two properties that actually matter for a letter
someone signs: the sentence in the body must equal the closing balance, and the schedule below it
must add up to the same movement. `purchaseTaxSplit.test.js` gains four for the bills count and the
invoiced total.

Docs: `.claude/rules/vendor-payables.md` takes the three new rules (a tab filter cannot be a
valuation boundary; a paged read whose follow-ups are bare has not been paged; a credit is money and
must not be clamped away). The Help page, the glossary's 1L+ entry — which said "NPR 100,000 or
more" where the code has always been strictly above — and all three module guide sections were
corrected, including the 1L+ formula still documenting `discountScope='all'`, a parameter S722
deleted.

**Files:** `src/modules/ims/reports/OutstandingPayables.js`,
`src/modules/ims/reports/VendorBalanceConfirmation.js`,
`src/modules/ims/reports/VendorBalanceConfirmationPrint.jsx`,
`src/modules/ims/reports/vendorBalanceHelpers.js`,
`src/modules/ims/reports/vendorBalanceHelpers.test.js` (new),
`src/modules/ims/reports/PurchaseOneLakhAboveReport.js`,
`src/modules/ims/reports/purchaseTaxSplit.js`, `src/modules/ims/reports/purchaseTaxSplit.test.js`,
`src/modules/ims/reports/supplierAttribution.js`, `src/pages/Help.js`,
`src/pages/settings/imsGuideData.js`, `src/shared/appVersion.js`, `public/service-worker.js`,
`.claude/rules/vendor-payables.md`

---

### S722 — 2026-09-10 — The tax reports re-analysed: one bill discount claimed twice across two filings, and a payment summary that tied to nothing

**No migration, no Edge Function deploy.** `CACHE_NAME` and `APP_VERSION` both bumped to
`crest-v258`.

A re-analysis of VAT Report, Non-VAT Report and Payment Summary — the three pages a client hands to
their CA. All three were internally coherent and each was wrong against the others.

**A bill's discount was claimed twice, across the two halves of one IRD filing.**
`purchase_entries.vat_inclusive` is PER LINE — `PurchaseBillForm` puts a checkbox on every row and
a toggle-all above them, which exists *because* mixed bills are ordinary — while `discount_amount`
is PER BILL, repeated on every line. VAT Report prorated that discount across the whole bill and
took the VAT share, correctly. Non-VAT Report ran its query with `.eq('vat_inclusive', false)`, so
it never saw the VAT lines at all and charged the **whole** bill discount against the non-VAT half.
On a 10,000 bill (6,000 VAT / 4,000 non-VAT) carrying a 1,000 discount the two pages between them
claimed **1,600**, and "non-VAT purchases this period" read **4,000 on one page and 3,000 on the
other**. Both figures get filed. The server-side filter is the whole defect and it is the shape that
looks most like careful scoping: a bill-level figure cannot be apportioned by a query that can only
see one half of the bill.

`src/modules/ims/reports/purchaseTaxSplit.js` is now the one place the split happens.
`splitPurchaseVat()` reads every line of the period, runs it through `allocateBillDiscounts()` — the
allocator Monthly Summary, Annual Summary, Period Comparison, Budget vs Actual, Consolidated P&L and
Supplier Contribution already share — and returns both halves. Each page reads fields off the *same
call over the same rows*, so they cannot fail to sum back to the bill. VAT Report's own "Non-VAT
Purchases" card was gross (no discount, no returns) against the other page's net; it is now the
identical property.

**Returning a whole discounted bill produced a negative input VAT claim.** `vendor_returns.rate`
stores the linked line's LIST rate, and it was subtracted from a base that had already lost the
discount — so a full return left the taxable base short by exactly the discount, and the report
filed a negative claim. Every return is now credited at its own line's post-discount rate; a full
return nets to zero. The lookup falls back to the list rate rather than dropping the row, because a
return nobody can price is still a return.

**`buildVendorSummary` lost its `discountScope` option rather than gaining a third caller.** Per-line
allocation makes "prorate across the bill" and "prorate across its VAT lines" the same arithmetic —
`discount × line / billGross` either way — so the only thing the option ever changed was which lines
the caller passed. VAT Report passes the VAT lines, Annexure-13 passes all of them, and the helper
moved out of `VatReport.js` (which was exporting it) into the shared module.

**Payment Summary's figures matched nothing in the app.** It summed `qty × rate` per line: ex-VAT
**and** pre-discount, which is neither the cost basis (net of discount, what the P&L uses) nor the
money owed (incl. VAT, what the supplier invoiced). So the Credit column never agreed with
Outstanding Payables and no column agreed with the Purchases register — while the page called itself
a payment summary. Its own module guide had carried this as a written gotcha for months. It now
values each bill through `calcBillTotals()`, the same helper behind the bill form's live total, the
printed voucher, Vendor Report and Outstanding Payables, so all four finally quote one number. A
bill is counted **once** in the new Bills column however many lines it has, since `payment_method` is
a bill-level choice. The subtitle, the tooltips and the workbook now all state the basis instead of
leaving it to be inferred.

**With no open period, Payment Summary rendered a complete report of a month it never chose.**
`periods.find(status === 'open')` finding nothing selected nothing, loaded nothing, and still drew
the full stat grid at NPR 0 with the total row reading 100% and `PeriodScope` showing "—". It falls
back to the most recent period, and `init()` now claims the page through `useLatestRequest` the way
the helper's own docs require of anything that auto-selects.

**Non-VAT Report's entries table did not add up to its own footer.** The rows were gross and the
single TOTAL beneath them was gross − discount − returns, with nothing in between to account for the
gap. It now shows GROSS TOTAL, the two deductions as their own lines, then NET TOTAL — the shape the
VAT half already had.

Smaller, same pass: both `vendor_returns` reads were unpaged while the `purchase_entries` reads
beside them were wrapped, so a truncated return list would have overstated a filed figure silently;
all three workbooks were bare `json_to_sheet` calls with no company name, PAN/VAT number or period
— on the one export in the app most likely to be handed to an accountant — and now carry
`sheetWithLetterhead` with a scope line that says outright when a period is still open and the
figures can change; and both statutory pages gained `NoPeriodState`, having previously blamed the
VAT toggle for a client simply having no periods yet.

The Help page described IMS Payment Summary as **"total revenue by payment method"** whose figures
"come from Sales Entry" — a purchases report documented as a sales report. The Annexure-13 tip still
claimed only VAT-inclusive entries counted, which stopped being true when that report was widened to
every purchase. Both fixed, along with the three module-guide sections, one of which had the old
Non-VAT formula written down as the spec.

`purchaseTaxSplit.test.js` pins all of it — 21 tests, half real arithmetic over the worked mixed
bill above (the two halves must sum to the bill, a full return must net to zero, `billPayables` must
agree with `calcBillTotals`) and half source-reading, because the query filter is the one defect that
correct arithmetic cannot catch: the split is right and it is still wrong if the page hands it half
a bill.

Docs: `.claude/rules/vendor-payables.md` takes the two new rules (a bill is mixed, so a query cannot
be the split; four pages value a bill and must all say `calcBillTotals`).
`.claude/rules/frontend-performance.md` gains the fifth `init()`-without-`begin()` instance and the
paging shape this found — the two statutory pages each paged `purchase_entries` and left
`vendor_returns` bare **in the same `Promise.all`**, one array element apart, while `PaymentReport`
next door paged both. `.claude/rules/report-pages.md` gains the state its existing gates do not
cover: `!loading && !loadError` both open when a page finishes loading *nothing*. `CLAUDE.md` was
left alone — nothing in it went stale, and it is 203 chars from its own ceiling.

---

### S721 — 2026-09-10 — Stock Movements re-analysed: a total that paid for a nested prep item twice, and a quantity rounded to nothing

**No migration, no Edge Function deploy.** `CACHE_NAME` and `APP_VERSION` both bumped to
`crest-v257`.

A re-analysis of Stock Movements, Dead Stock and FIFO / Expiry — the three pages S717 reworked and,
in the case of Stock Movements, reviewed and passed with no findings. That claim did not survive.

**The Sub-Recipes tab’s Value total double-counted nesting.** `computeRecipeCosts` is built on
`explodeRecipeIngredients`, so a sub-recipe’s cost per batch is fully exploded — a parent’s already
contains every child beneath it, which the page’s own Cost / Batch tooltip states. Meanwhile
`explodeRecipeTree` threads one `subs` array down the recursion, so `node.subRecipes` is a flat list
of every sub-recipe at every depth — also documented, one function up. Add the rows together and a
nested prep item is paid for twice. On the repo’s own nested fixture that is **NPR 40 against a
true raw-ingredient value of NPR 20**, exactly 2×, in the KPI card, the table footer and the Excel
sheet, while the card’s tooltip called the figure “a slice of the raw-item value on the Raw Items
tab”. Neither half was wrong on its own and both were correctly commented; the defect lived only in
the addition. The walk now reports `topBatches` — the share the DISH reaches directly — and every
total uses it, while the per-row `value` is unchanged because “what it cost to make this much of
it” is a real figure. A per-occurrence depth is required rather than an “is this ever nested?” flag,
since a sub-recipe can be used directly by one dish and nested inside another; both cases are pinned
by test, along with the total now tying to the raw-item figure exactly.

**A quantity is not money, and two recent sessions had forgotten that.** S717 and S719 routed
quantity columns through `nprInt()`, which is `Math.round` — so a 0.4 kg count printed as **“0”** in
five Dead Stock columns while its own Value at Risk stayed non-zero (the row contradicting itself),
and 0.75 kg of wastage printed as “1” on the report whose entire job is saying how much was thrown
away. `nepalMoney.js` is for money; applying its rule past its subject is its own defect.

**The “no BOM” banner named dishes whose recipes are fully configured.** `recipe_ingredients` was
read with a bare `.in()` over every recipe that sold — one row per ingredient per recipe, so ~130
dishes at the project’s own ~8-ingredient average is past the silent 1000-row cap. The truncation
lands on the wrong side of the comparison, so a recipe whose rows fell past the cut is listed as
having no ingredients and the owner is sent to Recipes to add ones that are already there.
`firstError` cannot see it, because truncation is not an error.

**`init()` never claimed the page, and here that is a tenant bug rather than a flicker.** Once
`handlePeriodChange` has run even once the ref is permanently non-null, so `isCurrent` stops failing
open — an admin switching client in the top bar re-runs `init()` on a still-mounted component with
the previous client’s period id in the ref, and every setter in `loadReport` is skipped: the new
tenant gets the old period chip over an empty ledger. Fourth instance after S698, S709 and S718.
Same page also had no fallback to the latest period when none is open (a full ledger of zeros
between closing one month and opening the next), and a KPI strip gated on `!loadError` but never
`!loading`, so every visit painted `Movements 0` and a gold `NPR 0` above a card reading “Building
report…” — S616’s positional rule again.

**The reconciliation note reported a gap that never existed.** `usage` was never cleared on a period
change, and because its load is deliberately fire-and-forget, `loading` goes false without it — so
after switching months the note compared the OLD period’s derived ingredient value against the NEW
period’s ledger total and printed the difference in NPR under the new month’s label. Cleared before
the load now, with its own `usageLoading` flag gating the note and the subs KPI strip: a second
async source needs a second flag.

**The Day range filter dropped every Bulk row.** Bulk manual Sales Entry writes `bs_day 0` and the
From dropdown starts at 1, so `0 >= 1` removed those rows from the table, all four KPI cards and the
export, unrecoverably — and on a period whose manual sales were all entered in Bulk it emptied the
page. The reasoning was already written twelve lines below, as the sub-recipe tab’s stated reason
for refusing the day filter outright.

**And the page told three lies about itself**, all the S696 recurrence: the Value Depleted KPI said
“POS sale + comp” while its own subtitle two inches above said “POS sales/comps and manual Sales
Entry”; the Help tip said the ledger “only reflects POS activity”; and the module guide called it a
ledger of “every POS-driven stock depletion”. Manual Sales Entry has written movements since
2026-07-30. Print and Export were also enabled while loading and after a failed read.

955 tests pass, 2 of them new. Help page and IMS module guide corrected, and four rules files
updated rather than one: `ims-figures.md` for the findings themselves; `recipes-and-subrecipes.md`,
because its "the two tabs are never additive" sentence was one clause short of covering the ROWS
within the Sub-Recipes tab and that omission is exactly what shipped; `frontend-performance.md`,
for the fourth `init()`-must-claim instance and the fourth producer/consumer truncation; and
`report-pages.md`, because S616's positional rule needed the second half nobody had written —
gating the stat-grid is not gating the page, and `loading` only covers what the loader awaits.
`README.md`, `CLAUDE.md`, `DESIGN.md`, `PRODUCT.md`, `docs/CROSS-REPO.md` and `DOCS-REMEDIATION.md`
were each checked against this work and correctly needed no change.

---

### S720 — 2026-09-09 — The summary family re-analysed: a discount three pages never subtracted, and a year that lost its stock past 85 items

**No migration, no Edge Function deploy.** `CACHE_NAME` and `APP_VERSION` both bumped to
`crest-v256`.

The nav's "Summary & comparison" group — Monthly Summary, Annual Summary, Period Comparison and
Budget vs Actual. They answer *what did this month cost* at four altitudes, which makes agreeing
with each other their entire job. Three of them disagreed, in two independent ways, and neither
disagreement had a symptom.

**The bill discount reached three pages out of six.** `purchase_entries.discount_amount` is a
BILL-level figure repeated on every line of the bill. S601 established that it belongs in COGS,
measured one month at **NPR 289,456** overstated, and fixed `ConsolidatedPnl.jsx`,
`MonthlySummary.js` and `get_group_pnl`. Annual Summary, Period Comparison and Budget vs Actual
were never touched — all three summed a raw `qty × rate` and charged the undiscounted price into
COGS and into a column literally headed **Net Purchases**. So the same month's cost was two
different numbers on two pages a client reads side by side: Annual Summary's row for Bhadra did not
equal Monthly Summary's Bhadra, and Budget vs Actual measured spend against a budget using a figure
higher than the one Monthly Summary shows for the same period — it could report **Over Budget on
spend that was not over**. All six route through `allocateBillDiscounts()` now.

Fixing it exposed that **Monthly Summary's own column had been mislabelled the whole time**:
`purchaseVal` was fed `lineNet`, the post-discount figure under a header reading *Gross Purchases*,
so `gross − net` read as "returns" when part of it was the discount. Gross, Discount and Returns
are three columns now, and Net Purchases always prints rather than showing `—` when it equals
gross — a column with a TOTAL under it whose cells cannot be added up to that total is unreadable.

**The paging sweep had never reached the two widest windows in IMS.** Every single-period page in
the module already pages `opening_stock`, `closing_stock` and `staff_meals` — Stock Count, Stock
Report, Reorder, Dead Stock, FIFO, Stock Ageing, Requisitions, and the three variance pages S719
swept. The *only* three unpaged sites left were the two pages that read those tables across
**12 and 24 periods at once**, plus Monthly Summary.

The multiplier is the finding. One row per item per period × 12 crosses PostgREST's silent
1000-row cap at about **85 items**, not 1000 — so a client far smaller than one that would trip a
single-period page loses whole months of opening and closing stock. A truncated closing read is
indistinguishable from an uncounted month: `computeUsed` collapses COGS to net purchases, the FC%
column reports it in confident type, and `firstError()` sees nothing, because truncation returns no
error rather than a short one. None of the three reads carried `.order()` either, so **which**
months lost their stock differed between loads: the same page showed two different years on two
visits and neither looked broken. Period Comparison's `sales_entries` read carries a comment naming
this exact cap "across up to 24 periods" while the three reads directly beneath it were bare — a
paging comment on one line of a `Promise.all` is not a claim about the others.

`summaryReads.test.js` pins both defects, the `salesReads.test.js` pattern: it reads the four files,
asserts every per-item-per-period read is wrapped with a unique sort tiebreaker, and asserts each
page imports `allocateBillDiscounts` and selects the five columns it needs — `discount_amount` plus
`purchase_group_id` and the `vendor_id`/`invoice_ref`/`bs_day` fallback key, which is the `a || b`
identity rule from `vendor-payables.md`. Verified by reintroducing both defects and watching it
fail; neither has any runtime symptom, so nothing else would catch them coming back.

**Budget vs Actual promised a save it could not keep.** A failed budget write reached only
`console.error`, under a banner reading *"Budgets are saved automatically"* — the spinner cleared,
the number stayed on screen, and the client had every reason to believe it landed until they came
back next month and found it gone. It names the category through `ActionError` now, says the figure
is still on screen and says not to reload before retrying, which is true because nothing on the
failure path reloads (S716's rule: the sentence and the recovery path in one edit). Blur also fired
whether or not anything was typed, so tabbing across untouched rows upserted `amount: 0` for every
category it passed; only an edited field saves.

**And it dropped spend it could not classify.** `items.category_id` is nullable, and the per-category
loop only ever claimed items belonging to a real category — so every rupee spent on an uncategorised
item fell out of the Actual column AND out of the Totals row, silently. The page reported Under
Budget on spend it had not counted, and its total could not be reconciled against Monthly Summary's
Net Purchases. There is an **Uncategorised / unbudgetable** row now, counted in Totals and excluded
from the variance (there is no budget for it to be over or under), with the two figures labelled so
they cannot look like a contradiction. Monthly Summary had already solved this with a synthetic
Uncategorized row; the fix had not travelled.

**Monthly Summary dropped a category whose only movement was a return.** The activity filter tests
opening, purchase, closing, wastage and staff meals — the fix that added the last two missed the
sixth column. Returning goods bought in an earlier period is ordinary (the bill is in Shrawan, the
spoiled case goes back in Bhadra); such a category has a return and nothing else, so the whole row
was dropped, `totalReturn` read **"None this period"** beside a real return, and `totalCOGS` was
overstated by the entire credit.

**Two thresholds still disagreed with the colours they explain.** S682 routed Monthly Summary's FC%
*figure* through `fcFigure(settings)` and left the box around it and the sentence under it on a
hardcoded 35/45, plus a third scale in the tooltip's "Target: 28–35%" — so a client on a 30%
warning level saw a red ▲ figure sitting inside a green box captioned **"✓ Within benchmark
(28–35%)"**. And Period Comparison's FC% column tooltip still said *"Green ≤30%, Amber 31–38%, Red
>38%"* while the cell colour, the chart's reference lines, its dots and its legend all read the
client's own thresholds; the comment beside those reference lines celebrates having fixed exactly
this drift, and the tooltip was the copy it left behind. When you route a figure through a band,
check what is touching it — a tint, a caption and a tooltip are all claims about the same threshold.

**Period Comparison had no overlapping-load guard**, on a page whose only reloading control is a
closed native `<select>` — the S601 case exactly. Arrowing 6 → 12 → 24 → All starts four concurrent
loads, and a stale *smaller* result landing last leaves `shown` at 24 periods with figures for 6:
eighteen rows of `—` that read as "nothing happened in those months". It also **stated a scope it
did not have**, with a subtitle claiming "across all BS periods" whatever the control said and a
printout and workbook naming no range at all, so a printed six-month sheet was indistinguishable
from a two-year one and the file was always `PeriodComparison.xlsx`. One `scopeLine` now reaches
the subtitle, the print header, both sheets (through `sheetWithLetterhead`, whose `scopeLine` is
required for this reason) and the filename.

**Annual Summary valued a deactivated item inconsistently within one row.** `rateMap` is built from
active items, so an item deactivated mid-year had its opening, closing, wastage and staff meals
valued at 0 — while `grossPurch`/`retVal` read the purchase row's OWN rate and kept counting its
spend in full. COGS was overstated by exactly the closing value the same row had just discarded.
Monthly Summary drops such an item from every column; this page does now too. An `is_active` filter
applied to a rate lookup and not to the rows it values is not a filter, it is a zero.

Smaller, from the same pass: Period Comparison's `fmt` returned `—` for a genuine NPR 0, so a
period with no wastage read identically to a period whose figures had not been computed (it is
`nprOrDash` now, which dashes only null and undefined, and takes that page's money off its own
`toLocaleString`); Annual Summary's trend column rendered a **green ↓ 0.0pp** for a month that did
not move, where Period Comparison's `trendIcon` has always had a 0.3pp dead zone; Budget vs Actual
gained the Print button the other three have; and both multi-period pages now group their rows by
period in one pass rather than running a `.filter()` per period per table — 24 periods × 7 arrays
on Period Comparison.

The Help page, its COGS-agreement FAQ (which claimed the pages agreed on the strength of the staff
meals fix alone) and the IMS module guide's Monthly/Annual/Period/Budget sections were all corrected
in the same change — the guide's Monthly Summary gotcha still told support that this page bands at
35/45 and Period Comparison at 30/38, "not a bug, just inconsistent thresholds worth knowing about",
which had not been true since S551 gave both pages `fcBand`.

---

### S719 — 2026-09-09 — The variance family re-analysed: a red Over flag built out of an uncounted shelf

**No migration, no Edge Function deploy.** `CACHE_NAME` and `APP_VERSION` both bumped to
`crest-v255`.

The four pages S717 deferred — Variance Report, Theoretical Variance, Shrinkage Report and Wastage
Report — completing the nav's "Stock & variance" group.

**`hasClosing` is a period-level answer to a per-item question.** `measured: false` has been the way
a caller says "no closing count yet" since S659, and both variance pages threaded it from
`(closing || []).length > 0` — does the month have ANY closing rows. So on a month where 900 of 1000
items were counted, the other 100 got `closeQty = 0`, actual usage came out as *everything on hand
plus everything bought*, and each of them wore a full red **Over** flag with a fabricated NPR value
behind it, on the page a client uses to decide whether staff are stealing. Those values were also
summed into the headline, so "Total Variance Value — potential loss" included every shelf nobody had
counted. Both pages now carry `hasCount = item.id in closeMap` per row (a count of 0 is a real
count, per S695, so never `> 0`), band each row on its own measurability, compute every headline
figure over measured rows only, mark the rest **not counted** in the table and in the export, and
name them in a banner. The fully-uncounted month already had a banner; the mostly-counted month —
which is what an ordinary close actually looks like — had nothing at all. `hasClosingRows` is a
local rather than the state value, because `setHasClosing` is async and a row builder reading the
state gets the previous period's answer.

**The 1000-row sweep had reached `wastages` and `purchase_entries` on these pages and stopped
there.** Every other per-item-per-period read was bare: `items`, `opening_stock`, `closing_stock`,
`staff_meals`, `vendor_returns`. The direction is what makes it serious — a truncated
`closing_stock` read is indistinguishable from an uncounted item, so it produces exactly the false
Over variance above, with no error for `firstError()` to catch. **Shrinkage was the worst case and
the least obvious**: its window spans several periods, so every one of those reads is multiplied by
the period count — six periods × 200 items is 1,200 opening rows, over the cap on a client far
smaller than one that would trip a single-period page. Multiply rows-per-item-per-period by the
window length before deciding a read is safe.

**Wastage Report was the one page in the product that did not page `wastages`.** The table is one
row per item per day once Daily Wastage is used — the documented 1000-crosser — and Stock Count,
Stock Report, Dead Stock, Reorder, FIFO and Stock Ageing all page it. The report whose entire job is
totalling the client's wastage was the only reader that did not, so past the cap its headline came
back short, in confident type. A table's own report is not automatically its most careful reader; it
is usually the oldest. Same page: `loading` started `false` so the first paint rendered the KPI strip
and "No wastage entries for this period" before any read was issued; there was no `NoPeriodState`;
money went through a local `toLocaleString` instead of `nepalMoney.js`; the Excel export carried no
letterhead or scope line; and the footer's "% of Total" was **asserted as `100%`** rather than
computed — true today because the two sums are identical, and precisely the shape that survives the
change which breaks it (S594's Supplier Contribution finding).

**The stale-failure guard, twice more.** Variance and Shrinkage both `catch` their recipe walk
without re-checking `isCurrent`, so a superseded load's error replaced the report the reader was
actually looking at. Third and fourth instances after FifoReport in S717 — when a loader has an
`isCurrent` check after its awaits, its `catch` needs one too.

909 tests pass, 9 of them new: the three variance pages joined `salesReads.test.js`, on both the
NULL-source assertion and S718's depletion-rule assertion. Rules file updated.

---

### S718 — 2026-09-09 — Stock Ageing re-analysed: a headline that could only say "all clear", and a dedup rule fed a whole year

**No migration, no Edge Function deploy.** `CACHE_NAME` and `APP_VERSION` both bumped to
`crest-v254`.

S717 swept six stock reports and gave this one two paging fixes without a full pass. This is the
full pass. Six findings.

**`selectDepletingSales` is a SINGLE-PERIOD function and two reports were handing it a fiscal
year.** The POS-supersedes-manual rule keys on `bs_day` — a day NUMBER inside a month, so day 5
exists in every one of them. Stock Ageing has always read a whole FY through it, and FIFO / Expiry
joined it in S717, when that session widened FIFO's window and kept the single-period call. On a
client running POS *and* manual entry — the exact population the rule exists for — a POS sale of a
dish on 5 Shrawan suppressed the MANUAL sale of the same dish on 5 Bhadra; and because a Bulk row
carries `bs_day 0` and is superseded by a POS sale anywhere in the period, one POS sale in month one
silenced every Bulk row for that dish for the rest of the year. The rule only ever DROPS manual
rows, so the error runs one way: consumption comes out short, stock that was eaten reads as still on
the shelf, and both reports move in the alarming direction — 90+ capital high, expiry exposure high.
`selectDepletingSalesAcrossPeriods()` partitions by `period_id` and applies the rule inside each
group. `ShrinkageReport` had been right all along, because it needed per-period totals anyway and
the grouping fell out of what it was already doing — the correct caller was correct by accident,
which is why nothing ever pointed at the other two. Ten new tests; the fixture shows the year-wide
form counting 36 against a true 46.

**"Capital in 90+ Day Stock" was structurally NPR 0, in green, with a ✓, for the first three months
of every fiscal year.** Stock carried into the window is modelled as one batch dated at the window
start — correct, and the page has always disclosed that its age is a floor — but the BANDING then
treats that floor as a measurement. Two months into a year the window is 60 days long, so stock that
has genuinely sat for three years is 60 days old to this report, cannot reach the 90+ band, and the
headline reports no stale capital at all. Same family as S713's zero-numerator food cost and S715's
Stars: an unknown rendered as a flattering known. `buildAgeing` now returns `totals.unknownAgeValue`
— carried-forward value sitting in a younger band — and while it is above zero the card reads
`≥ NPR X` with `△`, never the tick; the note names the window length and the amount.

**"Carried Into This FY" summed quantities across items and printed them as "units".** Kilograms of
flour plus litres of oil plus pieces of napkin. The table's own TOTAL row prints an em dash in the
On Hand column for precisely that reason, two hundred pixels below — the page contained both the
mistake and its own refutation. It shows the carried-forward VALUE now.

**The carried-forward batch is valued at the current Item Master rate, and three surfaces said
otherwise.** It has no purchase line behind it, so there is nothing else available — but
`stockAgeingCalc.js`'s header and the Stock On Hand tooltip both claimed the report values every
batch "at what you actually paid, not the current master rate", and `items.rate` is rewritten by
every purchase bill. So the oldest stock on the report, the stock it exists to surface, is the one
line valued on a moving basis. Now stated in the tooltip, the c/f card, the row badge, the workbook
notes and the module guide.

**The TOTAL row ignored the filter above it.** It rendered the whole report's figures under a
filtered table, labelled "(all items, by value)" — honest, and useless: a reader who has filtered to
Dairy wants Dairy's total, and a footer that cannot be reconciled with the rows directly above it
discredits those rows as much as itself (the S594 Supplier Contribution lesson). It follows the
filtered set now and says which. The KPI cards stay whole-report by their own documented decision.

**Two smaller ones.** `init()` never called `fyReq.begin()`, so changing fiscal year during the
first load left the dropdown snapping back over another year's table — the hook's own contract, and
the third page to miss it after S698 and S709, on the page this whole rule was written about. And
the empty state asserted *"every batch bought this year has been used, wasted or returned"* for a
year in which nothing was bought at all; it now distinguishes "used up" from "nothing to age".

900 tests pass, 15 of them new. Help page, IMS module guide and the two rules files updated.

---

### S717 — 2026-09-09 — Stock reports re-analysed: an expiry report that aged every batch to today, and a dead-stock report that was silent exactly when it knew nothing

**No migration, no Edge Function deploy.** `CACHE_NAME` and `APP_VERSION` both bumped to
`crest-v253`.

A re-analysis of the six stock reports — Stock Report, Reorder Report, Stock Movements, Dead Stock,
FIFO / Expiry and Stock Ageing. The two that had never been swept carried the findings; the four
that had carried a shared shape none of them had noticed.

**FIFO / Expiry aged every batch against `new Date()`, whatever period was selected.** This is
S594's bug — the one that pass found on Stock Ageing and fixed there — still live on the sibling
page that solves the same problem with the same data. Opening a month closed three months ago
reported the whole month's stock as "expired 90d ago", turned Value at Risk red over nearly all of
it, and highlighted every row: silent, and in the alarming direction. It stated an as-of date
nowhere — not the subtitle, not a print header (there was no print output at all), not the workbook.
`asOfForPeriod()` is now the twin of Stock Ageing's `asOfForFy`: today for the current BS month, the
last day of the period for any other, carried to screen, print and Excel by one `scopeLine`.

**The same report could only see batches bought in the month you were looking at, while netting
that month's WHOLE consumption off them.** Two errors compounding, on a report whose entire job is
finding stock about to go off. A tin bought last month and expiring next week was invisible; opened
on day 3 of an open period it showed three days of purchases under a confident "Items Tracked" —
and long-dated stock is exactly what this page exists for. Meanwhile the month's consumption
included what had come off stock carried in from before, so the new batches were over-eaten and
exposure was understated in entirely ordinary-looking rows. The window is now the fiscal year up to
the selected period, and the opening count is modelled as one **undated carried-forward batch
consumed first** — never displayed, because its whole job is absorbing the usage that was genuinely
its own. It also now reads every purchase in the window rather than only the dated ones: an undated
batch is still stock on the shelf and still absorbs its share, so reading only the dated ones made
them swallow the undated ones' usage too.

**And it was the last page summing `sales_entries` raw** — a title S695 (Stock Report) and S696
(Reorder Report) had each claimed in turn. Without `selectDepletingSales` a day sold in both POS and
manual entry consumed its ingredients twice, so batches still on the shelf were eaten and simply
vanished from an expiry report; a credit note (`pos_credit`, negative `qty_sold`) subtracted from
consumption and put stock back that never returned. Both directions were live.
`allocateFifo` is now imported from `stockAgeingCalc.js` rather than reimplemented, and
`salesReads.test.js` covers all four depletion consumers — asserting they reach the shared rule, not
merely that they select the column.

**Dead Stock treated "not counted" as "counted zero", so an uncounted month reported that nothing
was wrong.** The most important finding of the six, because of what it said instead. Consumption
there is the periodic residual — `computeUsed()`, i.e. `… − closing` — so the closing count is not
one input among several, it is the only thing separating "we used it all" from "none of it moved".
The old sum returned 0 for an item with no `closing_stock` row, which is the ordinary state of every
item in an open month, so an uncounted item computed as **fully consumed**, failed both the Dead and
the Slow test, and dropped out. An uncounted period therefore rendered zero rows under *"No dead or
slow-moving stock this period."* — the single most reassuring sentence the page can say, said when
it knew nothing at all. It also conflated the two states S695 spent a session separating: a
`physical_qty` of 0 is a COUNT, no row is not.

There are three states now and each has its own sentence: assessed; **not counted**, excluded and
counted in an amber notice above the figures, with an empty report saying *"This report needs a
stock count"* and linking to Stock Count; and **inconsistent** — counted higher than the stock
available to it, which used to clamp `used` to 0 through `Math.max` and therefore read as **Dead**,
the loudest verdict on the page, manufactured by a missing purchase bill. The KPI strip is gated on
`assessable > 0` for the same reason it is gated on `!loading`. Same page: `loading` started `false`
so the first paint rendered the strip and the empty state before any read was issued, there was no
`NoPeriodState`, the COGS arithmetic was inlined instead of importing `computeUsed` (and the module
guide's copy of the formula had drifted — no staff meals), and the per-item `sumField` walked all
six arrays once per item.

**A shared calculation is only as good as the reads feeding it.** S696 gave Stock Report and Reorder
Report ONE `buildStockRows`; S710 then paged Stock Report's inputs and left Reorder Report's bare,
so the shared function was being handed two different pictures of the same period and the two pages
could disagree about what is on the shelf — silently, past 1000 items, on the page that prints a
purchase list. `opening_stock`, `closing_stock`, `staff_meals` and `vendor_returns` are paged
everywhere they appear now, plus two producers S710 missed: **`items`** (the read that yields the
ids everything else joins against — in Stock Ageing `itemById` is what admits a batch into the
report at all) and **`par_levels`** (one row per item per client, and a truncated read turns "below
par" into "no par set"). Same shape as S706/S708: the consumer was paged and the producer was not.

`daysUntilExpiry` and `parseDateLocal` moved into `stockAgeingCalc.js` beside `ageInDays` so they
could be tested. `new Date('2026-09-09')` is UTC midnight — 05:45 local in Nepal — so the old
`Math.ceil(expiry − new Date())` returned `-0` for a batch that expired earlier the same day, which
is not `< 0`, so it rendered as in-date all afternoon. Both sides floor to local midnight now.
Twelve new tests; 885 pass. Help page, IMS module guide and glossary updated for both reports.

---

### S716 — 2026-09-09 — Overheads re-analysed: a save that could empty the period and say "✓ Saved", and four buckets of zero painted green

**No migration, no Edge Function deploy.** `CACHE_NAME` and `APP_VERSION` both bumped to
`crest-v252`.

A re-analysis of `Overheads.js` — the page an owner types their rent, wages and licence fees into,
and the one that answers "am I making money this month". Six findings, all shipped. The page is the
data-entry surface for a figure five other surfaces read (`ClientDashboard`'s `overheadTotal`, the
Owner Dashboard, the Monthly Owner Report, Recipes' True Cost allocation and `get_group_pnl`), so
its failures propagate rather than staying local.

**`save()` discarded both of its write errors, and could empty the period while saying "✓ Saved".**
It is a replace-the-period save — `scopedDelete` then `scopedInsert` — and neither call destructured
`error`. supabase-js **resolves** with `{ error }` rather than throwing, so a bare `await` throws the
only evidence away and the code below proceeds as if it worked. Both directions were live. Delete
fails and insert lands: the period holds both versions and every consumer that sums it
double-counts. Delete lands and insert fails: the period is **empty** — and `loadOverheads()` then
found nothing, fell into the carry-forward branch, and seeded the *previous month's* figures as an
editable draft. So the owner was shown plausible numbers under a success tick, over data that no
longer existed. Both halves are now checked; the delete goes first so its failure is a clean no-op
that can say so; the insert failure names the state the record is in ("cleared but not saved,
everything you entered is still on screen, press Save again, do not reload") per S619, and
**deliberately skips the reload** — after a failed insert the rows in state are the only surviving
copy of what the user typed, and reloading would replace them with a carry-forward draft.

The irony worth recording: `loadOverheads()` has carried a three-line comment about exactly this
class since S612 — *"on a data-entry page the silent-zero class is a data-loss class"* — while the
write path ninety lines below it had no guard at all. And S658, the session whose own subject line
was *"a failed write stops reading as a no-op"*, edited **line 242 of this function** to reword its
`alert()` and walked past 244 and 257.

**`.neq('source', 'pos_comp')` shortened revenue, which is this page's denominator for
everything.** `sales_entries.source` is nullable (`DEFAULT 'manual'`, no `NOT NULL`) and in SQL
`NULL <> 'pos_comp'` is NULL, so the server-side form silently dropped every legacy manual row.
`salesReads.test.js` deferred the remaining ~13 files on the grounds that each is display-only and
"needs its own answer to what its figure is supposed to mean". Overheads' answer is that it is not
display-only: revenue is the denominator of every percentage on the page while the numerator (food
cost, from purchases) stayed whole, so **every error ran one way** — Food Cost % and every "% of
revenue" read high, break-even read high, Net Profit read low, and Net Profit's sign is what picks
between a green "✓ Profitable this period" and a red "✗ Operating at a loss this period". Filtered
in JS over a selected `source` column, and added to the test's `FILES` so it cannot come back.

**An empty bucket was painted green as if it had beaten its target.** `trafficLight(0, 30)` returns
green — `0 − 30 ≤ 2` — so an untouched Labor tab rendered `NPR 0 · 0.0%` comfortably inside a 30%
target, the most flattering possible rendering of "we do not know what this costs". Same for Tax &
Fees (target 5) and Overhead (25). `seedBucket()` writes blank preset rows into every new period, so
the page manufactured its own examples, exactly as Menu Pricing's *+ Add Item* did at S713. The
absence is now carried as `null` from where it is computed to the cell — `pct()` returns null for a
null amount, an un-entered line renders "not entered" and "—" with a muted empty bar, the KPI cards
say "Not entered yet" instead of NPR 0, and a caveat above the Net Profit callout names every
missing line, because a statement that omits a cost overstates profit by exactly that much.

**Net Profit ignored Crest HR payroll entirely.** The page had no `clientModules`, no `hrEnabled`
and zero references to payroll — so an HR client who runs payroll properly and leaves the Labor tab
blank had a Net Profit overstated by their whole wage bill, in green. This is S526 seen from the
other side: that one double-counted labour, this one omitted it. Labour is now payroll **XOR** the
Overheads bucket, never the sum, using `ConsolidatedPnl`'s definition (gross + employer SSF, which
`get_group_summary` also uses) so the three cannot disagree; a finalized run whose payslips cannot
be read **refuses** rather than falling through to the bucket, which would quietly substitute a
different labour source for the one the page says it used. The ignored bucket is **named on screen
with its amount** rather than silently dropped, and the ranked pivot swaps the typed labour rows for
a single "Payroll" line so its own TOTAL still ties out to the 100% it prints underneath.

**"Covers" meant dishes here and bills everywhere else.** `covers` was `Σ qty_sold` — portions sold.
But `pos_orders.covers` is a real and different column, there is a whole Covers Report, the Demand
Forecast counts `o.covers || 1` per bill, and Help states plainly that *covers are counted from POS
bills and manual Sales Entries carry none*. At three dishes a guest, "Actual Covers" read three
times the real count and every per-cover figure was a third of the truth. Renamed throughout to
Dishes rather than switched to POS covers: this page reads `sales_entries`, so covers genuinely are
not available to it for an IMS-only client, and Recipes' True Cost allocation divides by the same
figure. The number was always honest; the label was not.

**The one disclosure sat on one card of four.** The "Purchase-based FC%" tip lived only in the P&L
Summary header, while the Cost Visualisation stack labelled the same figure "Food Cost" unqualified
and the break-even footnote printed a bare "FC%". A report that states a scope must state it
everywhere the report goes. Both now carry it, and the footer gained a plain-language note on all
three of the page's conventions — purchase-based food cost (and that it ignores opening and closing
stock, so a stock-build month reads worse than it was), dishes-not-guests, and payroll-XOR-bucket.

865 tests pass across 75 suites (2 new — the `salesReads` pair now runs over Overheads), build
clean with no ESLint warnings, `check-rules-globs`/`check-rules-stubs`/`check-claude-size` all OK.

Docs: this entry and the E-drive mirror, Help, the IMS module guide, and four rule files.
`ims-figures.md` had gone stale the moment the code changed — its "still open, deliberately" list
of files carrying the server-side `.neq` still named Overheads — so that list is now ~11 with the
reasoning for why this one came off it: **the question to ask a read is not only what its number
feeds, but what else is divided by it.** `report-pages.md` gains the write half of a page it
already cited three times for the read half; `error-messages.md` gains the rule that "press Save
again" is a promise the code has to keep; the root `CLAUDE.md` pointer described the overheads trap
as a double-count when it is now double-count *or* omission.

---

### S715 — 2026-09-09 — Menu Engineering re-analysed: a Star that meant "we never costed this", and a write-back that never once ran

**No migration, no Edge Function deploy.** `CACHE_NAME` and `APP_VERSION` both bumped to
`crest-v251`. `monthly_owner_reports` schema version 4 → 5.

A re-analysis of the whole Menu Engineering surface — the page, the frozen Monthly Owner Report
section that must agree with it, and the POS suggestion engine that reads what it writes. Some of
what the rules files claim was holding: the S682 `useLatestRequest` guard is on the page, the S714
NULL-safe `is_active`/`category` filters are on the recipes read, and `Q_HEX` is the one private
chart palette the S605 audit explicitly sanctioned. Eight things were not.

**Two of them had never worked at all, in opposite directions.**

**The `me_class` write-back has never sent a single request.** The line was
`scopedUpdate('recipes', { me_class: … }).eq('id', r.id)` inside a `forEach` — no `await`, no
`.then()`. postgrest-js issues the request **inside `then()`** (`PostgrestBuilder.then` is where the
`fetch` lives), so an un-awaited builder is an object that gets constructed and dropped. It has been
that way since the line was written in `e8e3d18` (S210). So `recipes.me_class` has been NULL for
every client, for every dish, always — and POS's Pro-tier Menu-Engineering suggestion ranking, which
`pricingPlans.js` sells and `PosOrders.jsx` reads on every menu load, has never had data to rank on.
Nothing failed and nothing logged. The tell in hindsight is S470's migration, which backfilled
`'plowhouse'` → `'plowhorse'` on that column and can only ever have matched zero rows.

**A failed cost read hung the page forever.** S711 gave `computeRecipeCosts` a `throwFirstError` and
recorded that "the callers that already catch it need no change". Four did not catch it at all —
Menu Engineering, Recipe Margin, Menu Repricing and Best Sellers — so a dead `items` read rejected
the loader's promise *before* `setLoading(false)`, and the page sat on "Loading…" indefinitely with
no error card and nothing to retry. All four wrapped. When you make a shared helper throw, "existing
callers already catch it" is a grep, not an assumption, and the symptom of getting it wrong is a
hang, which no error branch will ever report.

**A dish nobody had costed was the best thing on the menu.** `fcPct` was
`sellingPrice > 0 ? cost / price * 100 : 0` — and `0 / 400` is also a real `0`. So "we have not
priced this" and "we have not costed this" both reached `classify()` as **0% food cost**, cleared
the ≤35% cutoff, and came back **Star** or **Plowhorse**, printed as *"Keep on menu. Feature
prominently."* beside a green `0.0% ✓`. This is the S713 rule one level up: there a zero numerator
mis-coloured a cell, here it manufactures a verdict — and `Recipes.js`'s own **+ New Recipe** writes
a recipe with no ingredients, so the page produced its own Stars. Aashish took the recommended
option: `menuFcPct()` returns `null`, `classify()` returns `null` rather than guessing, and those
dishes appear in a fifth **Not rated** group — a summary card, a filter, a full-width panel under
the 2×2 matrix, a Category Breakdown column that only exists when it has something in it, and a
scatter footnote saying how many dots are not on the chart. The row's advice column carries the
reason, because the reason *is* the next step: "No selling price set" / "No costed ingredients".

**A dish that sold nothing could be a Star.** `highPop` was `qtySold >= median`, and the median
spans every active recipe including the unsold ones — so on a menu where under half the dishes sold
in the period, the median is **0**, `0 >= 0` holds for everything, and Plowhorse and Dog become
mathematically unreachable for that whole month. `qtySold > 0` is now required. The in-app guide had
promised the opposite ("never counts as high popularity **by definition**") since it was written;
that sentence is true now, and the guide says which release made it true rather than quietly
correcting itself.

The median deliberately still spans every recipe, rated or not, sold or not. Narrowing it would
re-classify large parts of a menu at once and pull new snapshots away from the historical ones for a
reason nobody asked for. Only zero-sale dishes moved.

**The classification is one function now, not two copies of one.** `MenuEngineering.js` and
`computeMenuEngineeringSection.js` each held their own `FC_CUTOFF`, `median()` and `classify()`,
with a comment on both saying they were mirrored "verbatim" and must never diverge — and this is the
worst file in the product to make that bet in, because the owner report's section is **frozen**: a
quadrant snapshotted wrong stays wrong, and nothing in the artifact says which of the two definitions
produced it. `src/shared/menuEngineering.js` is the one definition, imported by both, with
`menuEngineering.test.js` pinning each of the decisions above. Same move as `imsFormulas.js`,
`operatingBands.js` and `staffLevelBadge.js`.

**`sales_entries` was read bare, with no paging.** One row per recipe per day per source, so a single
month of POS service is thousands of rows and PostgREST returned the first 1000 with no error and
nothing in the data to say so. Best Sellers and the frozen owner report have both paged this same
read for a while; this page was the one left bare. The damage is not a short revenue column — a
truncated qty map moves the **median**, which is the popularity cutoff, so every dish on the page
can change quadrant.

That is also why this page came off `ims-figures.md`'s "still open, deliberately" list for the S699
`.neq('source', …)` trap. "Display-only" was doing too much work in that sentence: this figure sets
a threshold *and* gets written back to a column another module acts on. `source` is selected and
comps filtered in JS now, and `salesReads.test.js` covers this file alongside `Sales.js`.

**Four confident zeros sat above the error card.** The quadrant summary cards and the filter bar's
"N items · Revenue: NPR X · Qty: Y" line were rendered forty lines *above* the
`loading ? … : loadError ? …` ternary — the exact S616 shape, on a page whose zero state reads as
good news: `Star 0 · Plowhorse 0 · Puzzle 0 · Dog 0` over "Could not load this report", and the same
four zeros on every ordinary load before the data arrived. Both are inside the guard now. The cards
also became real `<button>`s with `aria-pressed` (they were click-only `<div>`s), laid out
`flex-start` so a stretched grid row does not sink their labels.

**Revenue was priced at today's menu price.** `sellingPrice × qty − discount`, for any period —
so a past month silently restated itself the moment anyone edited a price. It now uses the row's own
`unit_price` snapshot with the current price as a fallback for rows written before that column
existed, which is `Sales.js`'s `recipeRevenue()` verbatim and the basis Best Sellers records having
fixed for exactly this reason. Built in one pass over the sales array rather than a `.filter()` per
dish.

**Smaller, from the same pass.** The scatter chart's Y domain was pinned `[0, 100]`, so a dish
costing more than it sells for — a state `Recipes.js` warns about at save time — plotted below the
floor and off the chart; the domain follows the data now. `PeriodScope` was missing (both its
folder-mates have it) and the header printed a median it had not computed yet; the chip carries the
period and its open/closed status, and the thresholds stay in the already-gated legend.
`loadPeriods` now claims through `useLatestRequest` too, per that hook's own contract — it
auto-selects a period, so an admin switching clients could otherwise leave one outlet's month
labelling another outlet's figures. It needed its **own** guard rather than the page's existing
one, and that was very nearly shipped wrong: both effects fire on a client switch, in declaration
order, so on a shared `useLatestRequest` the period LOAD's claim — still carrying the previous
client's period id — supersedes the period LIST's, and the new outlet's dropdown never gets set at
all. That is exactly the failure `frontend-performance.md` records from Roster's two loaders ("a
shared one would have them cancelling each other"); adding a guard to a page that already has one
is not the same change as adding the first.

**What the write-back looks like now.** One request per distinct class rather than one per recipe
(a 300-dish menu was 300 concurrent PATCHes on every period change), chunked because the id list
rides in the URL, unrated dishes written back as `NULL` so a dish that loses its price stops
carrying a stale verdict into the till — and **only from the current period** (the open one, else
the latest). Browsing last Shrawan out of curiosity must not re-label what the till suggests
tonight; `computeMenuEngineeringSection.js` had refused to port this write for precisely that
reason while the live page it mirrors did it on every period change. Its failure is surfaced
non-blockingly below the report, named as the consequence — the quadrants on screen are correct,
what could not be updated is the copy POS reads.

`CURRENT_SCHEMA_VERSION` went 4 → 5: `quadrantCounts` gains an `Unrated` key and `items[].quadrant`
can be null, and a v4 matrix and a v5 matrix are not computed the same way. Both consumers guard on
the new key, because a pre-v5 snapshot does not have it and an absent count must not render as a
real 0. The Excel sheet writes "Not rated" rather than a blank cell that reads as an export fault.

**Docs.** The un-awaited-builder trap went into `CLAUDE.md` rather than a rules file — it is not
IMS-scoped, and it belongs beside the two siblings already there (`try/catch` catches nothing, a
bare `.select()` truncates), all three being supabase-js call shapes that fail by doing nothing.
That leaves the root file **200 chars under its 53,000 ceiling**, so the next session that wants to
add a line there cannot; it is due a `/doctor` pass. `.claude/rules/ims-figures.md` took the page's
own rules and dropped Menu Engineering from the S699 "still open, deliberately" list, with the
reason it was ever on it — "display-only" was doing too much work for a figure that sets a
threshold and gets written back to a column another module acts on. Two rows filed in
`docs/CROSS-REPO.md` for hss-suite (the builder trap; and "making a shared helper throw does not
make its callers catch", whose failure mode is a hang no error branch reports). `POS_DECISIONS.md`
gained an amendment rather than a correction: S713's argument for *not* building manual pairing on
the IMS branch rests on Pro clients already having the ME ranking — which had never once run, so
the decision was right for a reason that was not yet true. Six memory claims were wrong and are
fixed: four still carried the pre-S470 `'plowhouse'` spelling, and two recorded the write-back as
shipped in S210.

Verified: `npm run build:verify` clean (exit status read from the process, not through a pipe), 863
tests across 75 suites green including 11 new ones, `npm run check:docs` OK.

---

### S714 — 2026-09-09 — Sub-recipes re-analysed: the private read that made a shrinkage report lie, and a refusal nobody could read

**No migration, no Edge Function deploy.** `CACHE_NAME` and `APP_VERSION` both bumped to
`crest-v250`.

A re-analysis of the whole sub-recipe subsystem — the two cost walks, the mirror-item lifecycle,
every consumer of `is_sub_recipe` and `sub_recipe_id`. The invariants the rules file claims were all
holding: no third private cost walk (`grep subCostPerUnit` is clean post-S713), the path-set guard
intact, the S711 paging in place on both shared engines, 39 tests green. Nine things were not, and
one more surfaced while fixing them.

**Theoretical Variance read the client's entire recipe book through a bare `.in()`.** The one whole-
book `recipe_ingredients` read the S711 sweep never reached, because this page reimplements the
recursion locally (`expandIngredients`) — the same sentence S713 had to write about the cost walk,
about the read this time. Past PostgREST's 1000-row cap the dishes below the cut expand to nothing,
theoretical usage comes out LOW, and **low theoretical does not read as missing data — it reads as
over-consumption**, on the one report a client uses to chase shrinkage against their own staff. Now
`fetchAllRowsChunked` with `.order('id')`, and the per-recipe `.filter()` join beside it replaced
with a single grouping pass, since the read is now genuinely unbounded.

**Menu Pricing still costed a sub-recipe at zero, one layer below the walk S713 replaced.** The
shared walk landed; the LIST it resolves against was still seeded `.eq('category','Sub-Recipe')`, so
a recipe reached through `sub_recipe_id` whose own category had been changed away fell through
`subCost[id] || 0` — the same under-statement, on the same page, in the same direction, surviving
the fix that existed to end it. What a `sub_recipe_id` names is decided by what points at it, never
by how it is categorised.

**And the write that manufactures that state refused nothing.** `deleteRecipe` blocks while other
recipes reference the row; changing the *category* away from Sub-Recipe — same recipe, same screen,
a dropdown instead of a button — deactivated the mirror, nulled `linked_item_id` and left every
parent pointing at it. Nothing broke loudly, which is why it survived: the cost engines ignore
category, so Recipe Costing stayed right while the parents' ingredient picker rendered blank and
Menu Pricing costed the row at nothing. It refuses now, naming the recipes that hold it. The cycle
guard was gated on the same condition (`category === 'Sub-Recipe'`) and now runs for any existing
recipe — what makes a cycle possible is being *referenced*, which is independent of categorisation.
Both readers stay tolerant of rows written before the guard.

**`.neq('category','Sub-Recipe')` on a nullable column, ten times.** `recipes.category` is nullable
with no default, and a server-side `.neq` drops NULL rows as well as the named one — so an
uncategorised dish was missing from Sales Entry, **unorderable on the POS till**, and absent from
Best Sellers, Recipe Margin, Menu Engineering, Combo Builder, HSC assignment, the Dashboard's recipe
count, the guest-menu readiness counts, and permanently from the frozen Monthly Owner Report's menu
matrix. `.neq('is_active', false)` on two of those is the same shape. The decisive evidence was on
the server: every guest-menu function has always filtered `category IS DISTINCT FROM 'Sub-Recipe'`,
which is NULL-safe — so `AdminGuestMenu.jsx` was claiming to count "exactly the rows get_guest_menu
serves" while measuring a different population. MenuPricing and MenuRepricing carried the fix and
the comment since S683; it did not travel.

**The bulk importer had none of `save()`'s guards.** Two sheet lines naming one item fail with
`21000` after the recipe row is committed, leaving an ingredient-less recipe that costs 0 — which
`fcBand` renders as `0.0% ✓` in green (S713's rule, reached by a different road). Two lines naming
one sub-recipe fail at nothing and silently double the cost. Duplicates are refused in the preview
now, per recipe rather than per line, and a failed ingredient insert deletes the recipe row it just
created — the two writes cannot be reordered, so a compensating delete is the only way to leave the
sheet re-importable.

**Raising one depth cap and not its twin put the S602 silence straight back.** `explodeRecipeTree`
has two limits: the fetch loop's `MAX_DEPTH_ROUNDS`, raised 5 → 12 with a loud `console.error`, and
`explode()`'s own `depth > 10`, which stayed. That is the worst available disagreement — the loop
resolves levels the recursion then refuses to walk, so the frontier comes back empty, the loud error
cannot fire, and the deepest levels vanish silently. Tied to one constant now, reported once per
walk, with an eleven-deep fixture verified to fail against the old number.

**`subRecipeUsage.js`'s `fetchItemMap` was the last raw `.in()` on the walk** — every raw item under
every sold dish plus every sub-recipe's own ingredients, valuing and NAMING at once. A missing rate
is a zero in the reconciliation figure; a missing name drops the ingredient out of the search index,
so find-an-ingredient stops matching and reports nothing rather than reporting a failure.

**Then: every hand-written refusal in `Recipes.js`'s save was reaching the user as "that didn't
work".** Found while wiring the convert-away message, which would have been swallowed the same way.
`errorInfo` recognises Postgres *shapes*; an English sentence matches no rule and returns the
FALLBACK with the real text demoted to fine print — so the duplicate Product Code, both halves of
S707's mirror name clash and S711's "saved, but the previous ingredient list could not be removed"
all arrived as a generic apology. `ActionError` already draws this line (a plain string passes
through untouched); a throw/catch needs it drawn explicitly. A tagged `SaveRefusal` carries copy we
wrote; raw errors are rethrown as objects so their `code` survives into the detail line.

The remaining two were smaller: the sub-recipe picker offered hidden sub-recipes unlabelled while
`itemOptions` beside it labels hidden items, and rendered blank for a row already on the recipe
whose target had been converted away.

**One finding was documented rather than fixed, deliberately.** A mirror item's `items.rate` is
written only by a recipe save, while `PurchaseBillPage` rewrites every raw ingredient's rate on
every bill — so a sub-recipe's WIP valuation in Stock Count (which counts mirrors on purpose), its
closing stock and therefore COGS all ride on a cost frozen at the last save, while every other
sub-recipe figure recomputes live. The snapshot is defensible on the same document-versus-report
reasoning as `requisition_lines.rate`; what is not settled is that it happens by omission, with no
recost action and nothing on Stock Count saying a prep item's rate is older than its ingredients.
That is a product decision about what a WIP valuation means, not a bug fix.

---

### S713 — 2026-09-09 — Menu Pricing: the last private copy of the sub-recipe walk, and a food cost of zero wearing a green tick

**No migration, no Edge Function deploy.** `CACHE_NAME` and `APP_VERSION` both bumped to
`crest-v249`.

A re-analysis of `/menu-pricing` turned up nine things. Two of them move a number the reader prices
a dish from, three are dropped write errors, and the rest are the residue you get when a page grows
two branches and only one of them keeps being maintained.

**The private copy of the sub-recipe cost walk was the last one in the repo, and it was the only one
still wrong.** `MenuPricing.js` had its own `subCostPerUnit()` with a cycle guard that only ever
added to `seen` and never deleted on the way out. That is the correct shape for "have I been here
before in this whole traversal" and the wrong one for "am I inside myself right now", and the
difference is invisible at one level of nesting and silently under-costs a diamond:
`Sauce → Roux → Stock` costs the Stock, and `Sauce → Stock` then returns 0 because Stock is already
"seen". `recipeCostCalc.js` fixed exactly this in its own copy and left a twenty-line comment
explaining it; every other consumer in `src/` — Recipes.js, the printed cost card, the recipe
importer, Stock Count's sub-recipe usage, the nutrition roll-up — imports that fixed helper. Menu
Pricing did not, so it under-stated food cost against **both** other engines, on the one page where
that number decides a price. The copy is gone; the page now stitches its separately-fetched
ingredients back onto the sub-recipes and calls `calcSubRecipeCostPerUnit` once per sub-recipe.

**A dish with no ingredients printed `0.0% ✓` in green, and this page's own + Add Item created
them.** `fcPct` was `exVat > 0 ? (cost / exVat) * 100 : 0`, and `fcBand(0)` bands as *Healthy* —
so an unrecipe'd item read as the best-performing dish on the menu and sorted to the top of an
ascending FC% sort. Two tells that this was never intended: the **New FC % sort already guarded
`r.cost > 0`** while the cells and the FC% sort did not, and `fcBand(null)` has always returned
`—` with no mark. `fcPct` is now null when there is nothing to divide, both cells render through
`fcFigure()` (the shared "colour + mark + band name together" helper this page had taken apart into
three wrappers of its own), and the dash carries a title saying what to add. The Excel column
exports blank rather than `0.0%`, because that sheet is sent out and priced against.

Worth stating as a general rule: **a rate computed from a zero numerator is not a rate, and a
banding function will happily band it.** The absence has to be carried as null all the way to the
cell, or the first `?:` that defaults it to 0 hands the band the most flattering possible answer.

**Three writes whose errors went nowhere.** `savePairings` was delete-every-row-then-insert with
*both* results discarded — so a refused insert after a successful delete wiped every pairing on the
item, while the modal closed on "Saved" and wrote the intended list into state. It is now a diff
(only what the user actually changed is touched), additions go first (the same insert-before-delete
order `Recipes.js` uses on ingredients, so a half-completed save leaves more than it takes), and
each error is read and surfaced. `togglePos` dropped its error entirely: the 86 toggle snapped back
to its stored value and said nothing, which reads as a missed click rather than "this is still
selling". `saveRow` reported `'Save failed'`, which is the whole S619 rule in two words — the row
keeps a short marker so it stays findable in a table of ~100, and the sentence plus the technical
detail go to a new page-level `ActionError`.

**None of the new messages claims the write did not land**, and one of them is the reason to care:
`recipes` has no unique index on `(client_id, name)` — unlike `items`, which got one in S707 — so a
response lost after the row was committed turns the retry a "the item was not added" sentence
invites into a duplicate menu item. The Add/Edit modal now says the item *may* not have been added
and to check the list first. `errorText.test.js` asserts the library never makes that claim; it
cannot see a call site prepending its own.

**`.neq('category', 'Sub-Recipe')` also dropped every recipe whose category is NULL.** The column is
nullable and a server-side `.neq` excludes NULLs silently, so such a recipe vanished from the one
page that sets its price with no error and nothing missing from any count. Now
`.or('category.is.null,category.neq.Sub-Recipe')`. `MenuRepricing.js` had the identical line and
the identical fix. Neither is reachable from `Recipes.js` (its category control is a bounded
`<select>`), so this is legacy and restored rows only — which is exactly the population nobody
checks.

**The IMS branch rendered 45 lines of Pair With modal that nothing could open.** `openSuggestModal`
is called only from the POS-only table, so the IMS copy was unreachable, and the
`recipe_suggestions` read that fed it ran on every load for every IMS client. `Help.js` already
documents the split as deliberate — an IMS client's order screen derives suggestions from real
sales history instead — so the modal is deleted, the read is skipped for that branch, and a comment
where it stood says what would be needed if manual pinning were ever wanted there too (a Pair
control in the row, not this modal back).

**Smaller, same session.** `"VAT 13%"` was written out as a literal in the row captions, the column
tooltips and the Excel export while `vat_rate` is a plain numeric column each row carries its own
value of — one `vatLabel()` fed the row's own rate now. `+ Add Item` issues a Product Code from the
category exactly as Recipe Costing does, with the same recompute-and-retry on a `23505` that
Recipes.js uses, since the code is derived from in-memory state and a second tab can genuinely take
the number first; items added here before today have none, and Settings → Product Codes fills those
in. Both `recipes` reads are paged against the 1000-row cap. And Menu Pricing's `cost_price`
fallback was ported to Menu Repricing, which had none: the same dish showed a cost on one page and
0 on the other, and on Menu Repricing 0 meant 0% FC, never above target, and therefore **silently
absent** from the underpriced list and from the Monthly Opportunity total.

Docs in the same pass. The IMS module guide's Recipe Costing entry claimed `utils/recipeCost.js`
was shared by Menu Pricing — it never was; it now names both engines and which pages call each,
because sending someone changing the cost walk to the wrong file is how two screens come to cost
the same dish differently. The Menu Pricing entry gains the POS-only Pair split, the blank-not-zero
FC% rule and the Product Code note. Help's Menu Pricing tips lose a hardcoded `green ≤30%, amber
31–38%, red >38%` scale that the page itself stopped using on 2026-08-13 when the bands moved to
Settings → Thresholds, and gain the meaning of a `—` in the FC% column.

**The rule that would have caught the banding bug was scoped to the wrong files, and the check
cannot see that.** `.claude/rules/ims-figures.md` owns the food-cost banding rule and globbed
`stockcount/`, `reports/` and `sales/` but **not** `src/modules/ims/recipes/**` — where five files
print a banded food-cost figure (Menu Pricing, Menu Repricing, Menu Engineering, Recipe Margin,
Recipes). Every glob it had matched real files, so `check-rules-globs` ran clean, session after
session, while the rule never loaded for the module it is most about. `CLAUDE.md` already states
this limitation in the abstract — *"It still cannot tell you a glob is scoped to the wrong file,
only that it matches nothing"* — and this is the first worked instance, now recorded under T3 in
`DOCS-REMEDIATION.md` along with the cheap manual version: when a rule is edited, grep its subject
across `src/` and compare the hit list against its own `paths:` block. A subject with call sites
outside its globs is the tell. The glob is added; the rule now loads where it is needed.

**`README.md`'s line-endings convention was aspirational and read as descriptive.** It said
`.gitattributes` normalises the tree to LF, which is true *on commit* and not true of the working
tree: measured this session, `MenuPricing.js` is pure LF while `MenuRepricing.js` — its sibling in
the same directory — is pure CRLF, and several `.claude/rules/*.md` are mixed within one file. Git
never reports it, because it normalises on the way in. That cost two rounds here (a patch script
searching for `\n` matched zero occurrences in the CRLF file and, correctly, asserted rather than
corrupting), so the bullet now says to assert the match count and try `\r\n` before `\n` **per
replacement block, not per file** — and explicitly not to "fix" a file by normalising it, which
turns a 12-line diff into a whole-file rewrite.

Three shapes went to `docs/CROSS-REPO.md` for hss-suite, none of them the recipe bug itself, since
recipes are not shared: the zero-numerator banding (HR is full of banded ratios — labour cost %,
overtime share, attendance %), the replace-a-whole-set save that discards both write results (a
roster week's shifts, a payslip's components), and the one that is really about that repo's
existence — a documented fix does not reach a private copy, and hss-suite *is* a private copy of
the payroll engine, settlement and `bsCalendar.js`. The concrete ask there is a census, not a port.
And `POS_DECISIONS.md` records the Pair-with split as a decision rather than an absence, so the
next session does not "fix" the missing Pair button on the IMS branch. 74 suites / 847 tests.

---

### S712 — 2026-09-09 — /doctor: the first pass with nothing derivable left to cut, and six numbers that had rotted in place

**No service worker bump, no `APP_VERSION` change. No app code changed. No migration.** Docs only:
`CLAUDE.md` and `.claude/rules/item-master-rates.md`.

The sixth `/doctor` pass over the Claude Code setup — read-only diagnostics across the installation,
skills, MCP servers, hooks, permissions, transcripts and every loaded memory file, over a window of
the 50 most recent sessions (8.3 days, 41 from this repo and 9 from HSS).

**Nothing was unused and nothing was misconfigured, so nothing was disabled.** All five settings
files parse; there are no duplicate or leftover CLI installs; no agent definitions exist to collide;
all five skill frontmatter blocks are valid; all 167 `paths:` globs across 27 rule files resolve.
Auto mode is already the user-scope default with no project override shadowing it. The denial log —
10 records, at most 2 per pattern, nearly all `user-rejected` — held no read-only command that
clears the bar for pre-approval: every Bash denial carried an env-assignment prefix, a `cd …;`
compound, or `npm run`, all of which are arbitrary execution behind a read-only-looking rule.
`playwright` alone accounted for 399 tool calls in the window and its schemas are deferred, so it
costs tool names and nothing else.

**What the pass found was drift, not bloat.** Five earlier passes had already taken the derivable
content out and this one found none left — the first time that has been true. What it found instead
was six values embedded in `CLAUDE.md` that move, which is exactly what that file's own "never embed
a value that moves" rule forbids: the S440 bundle figures (`~931 kB → ~165 kB`, `~97 on-demand
chunks`), `all 37 pages with an Excel button`, `As of 2026-07-05` on the `scopedDb` adoption claim,
`The current adoption list (22 pages)`, and `The five pages this has recurred on` — that last one
already visibly wrong, sitting three lines under a sentence naming the sixth (S683). A date stamp is
the worst of them: it asserts a thing was true then and says nothing about now, while reading like a
citation.

**One section moved.** "Purchases: qty/rate storage convention" is reachable only from the IMS
purchases and items modules, so it went to `.claude/rules/item-master-rates.md`, which already loads
on those paths — and which already carried a cross-reference pointing *out* at the root file by
section name. That reference now points at the section directly below it, so the two conventions
S597 warned would be read for each other finally sit in one file.

The gating-wrapper section (S601) lost its duplicated `ConsolidatedPnl.jsx` worked example, which
`report-pages.md` already held in a fuller form, but **kept its first sentence resident**: "JSX
children are an ARGUMENT" is true of any wrapper, not only of report pages, and a trap that only
loads for the files where it has already been hit is not much of a warning.

`CLAUDE.md` 52,835 → 52,401 chars, 599 under its own ceiling.

**"Nothing warns" no longer held, and that line was itself the stalest thing in the file.** The
glob-rot paragraph still told the reader to grep `.claude/rules/*.md` by hand after moving a file.
`npm run check:docs` has since made that mechanical and `build:verify` runs it, so a rule that
silently stopped loading now fails a pre-push check rather than waiting for a `/doctor` pass. The
paragraph says so now, with the limit stated: the check proves a glob matches *something*, never
that it matches the file it was meant for.

**The pass then had to correct itself.** The first draft of these `CLAUDE.md` edits added 585 chars
of commentary about the session that produced them — the doctor pass narrating its own findings into
the always-loaded file — and left 97 chars of headroom, having spent the entire budget it had just
freed. That is the regrowth pattern the file's own opening paragraph describes, arrived at from the
one direction it does not warn about. Reverted; session narrative is what this changelog is for.

Two findings were reported and deliberately not acted on: the impeccable `PostToolUse` hook's
7,663 ms worst case (median 705 ms over 152 timed runs, timeout 15 s, no timeouts hit), and the VS
Code extension at 2.1.263 against 2.1.266 — which updates through the Extensions pane, not
`claude update`, there being no CLI install on this machine.

### S711 — 2026-09-09 — Recipe Costing: the walk that quietly stopped at 1000 ingredients, and a delete that emptied the recipe it could not remove

**Service worker `crest-v247` → `crest-v248`, `APP_VERSION` with it.** App code changed. No migration.

A re-analysis of recipe costing, following the S705–S710 sweep through Periods, Item Master,
Vendors, Purchase Orders and Requisitions. Recipe costing has three cost paths — `recipeCostCalc.js`
(in-memory, the page), `recipeCost.js`'s `explodeRecipeTree` (the shared walk behind ~25 call sites),
and `TheoreticalVariance.js`'s local re-implementation. S602 reconciled the first two on the diamond
bug. These are what was still open.

**The shared walk had no row cap and no chunking, so every figure derived from it could truncate
silently.** `explodeRecipeTree` read `recipe_ingredients` with a bare `.select().in('recipe_id', …)`
— no `fetchAllRows`, no `fetchAllRowsChunked`, no tiebreaker — and `Variance.js` seeds it with
**every recipe the client has** (`scopedFrom('recipes','id')`, unfiltered). A book of ~120 recipes
averaging 8 ingredients is already past PostgREST's 1000-row cap, and the rows past the cut are
simply absent: the dishes below them explode to nothing, theoretical usage comes out LOW, and low
theoretical does not read as missing — it reads as **over-consumption**. False variance flags,
overstated shrinkage, understated reorder need, understated COGS on both dashboards and in the
Monthly Owner Report. No error, no short-array tell. The frontier read one level down had the
identical shape, and `computeRecipeCosts`' `items`/`recipes` reads the same. All now paged and
chunked, with `.order('id')` as the unique tiebreaker. `Recipes.js`'s own `init()` read was the
fourth.

That last one was not only a display fault. `openEdit` seeds the edit form from
`recipe.recipe_ingredients` and `save()` writes the form back as the *complete* list — upserting
what it holds and deleting everything else on the recipe — so **opening and saving a recipe whose
rows fell past the cap deleted the missing ones permanently**, with nothing on screen
distinguishing a truncated recipe from a genuinely short one.

`recipeCost.test.js` grew two regression tests, and the shape of the fixture is the point: 200
recipes × 8 ingredients, so 150 recipes land in one `fetchAllRowsChunked` chunk and cross the cap
*inside* it. A fixture of 1200 one-ingredient recipes would pass on the chunking alone and prove
nothing about the paging. The stub now truncates at `SERVER_MAX_ROWS` exactly as PostgREST does,
silently and with no error, so the test reproduces the bug rather than agreeing with the code.

**Deleting a recipe that had ever sold destroyed its ingredient list.** `sales_entries.recipe_id` is
a plain FK with no `ON DELETE`, so Postgres refuses — but `deleteRecipeNow` cleared
`recipe_ingredients` *first*, then deactivated the mirror item, then attempted the recipe. The two
irreversible steps ran before the one that could still be refused, and for any dish with sales
history it always was: the recipe survived with an empty ingredient list and the error text told the
user to re-enter them. The order is inverted now — the recipe row goes first and
`recipe_ingredients.recipe_id`'s `ON DELETE CASCADE` takes its rows with it in the same transaction,
so a refusal leaves every row intact. The mirror deactivation moved after the successful delete.

The pre-check gained the half it was missing. Seven things reference `recipes` and, exactly as with
`items` in S706, they do not agree on what a delete means while the calling code cannot see the
difference: `sales_entries` refuses, `recipe_ingredients`/`demand_forecast_daily`/`recipe_suggestions`
cascade, `pos_kot_removals` sets null, and **`pos_order_items.recipe_id` has no FK at all**. The
guard now counts sales and POS lines up front and refuses with a sentence pointing at Hide, which is
one click away on the same row and loses nothing.

**The ingredient-replace delete dropped its error, and a sub-recipe row is what that costs.** The
upsert lands first by design (S375), so a failed prune leaves the recipe holding both lists. Item
rows survive it harmlessly — `onConflict: 'recipe_id,item_id'` updated the existing row rather than
adding one — but a **sub-recipe row has `item_id` NULL, never matches that conflict target, and
always inserts fresh**. So a silent failure doubled every sub-recipe ingredient and the recipe cost
twice as much from then on, on the page and in COGS, with the save reporting success. It now names
the consequence: the recipe saved, it holds both lists, its cost is overstated, and saving again
clears it — which is true, because the next successful prune removes them.

**The page had no load-error handling at all.** No `firstError`, no `loadError`, no
`ReportLoadError` — every read in `init()` dropped its error. A failed `recipes` read rendered the
"no recipes yet" empty state; a failed `recipe_ingredients` read was worse, rendering **every recipe
at 0 ingredients, NPR 0.00 food cost and 0% FC — which this page bands green**. A page telling an
owner their entire menu is perfectly costed. Now `ReportLoadError` replaces the list, the edit view
and the detail view. The overheads/sales reads behind the True Cost panel are handled separately and
non-blockingly: their absence otherwise reads as "no overheads recorded this period", a claim the
page had no evidence for.

**Two costs for one recipe, one click apart.** `items` was loaded `.eq('is_active', true)` and
`calcLiveCost` resolves an ingredient against that array, returning 0 for anything it cannot find —
so an ingredient whose item had been *hidden* in Item Master cost **nothing** in the edit form while
the detail view, which reads the joined `ri.items` row and applies no filter, cost it in full. The
cheaper number was on the screen you change the price from, and the row's picker rendered blank
besides. Inactive items are loaded now and the `is_active` filter moved to the picker, which still
offers only active items to *add* but keeps a hidden one visible, labelled, where the recipe already
uses it.

**Two labels, both about VAT and duplication.** The ingredient pickers list every item regardless of
what is already on the recipe, so adding one twice is two clicks — and the save upserts the whole
list in one statement, which Postgres rejects with `21000`, *"ON CONFLICT DO UPDATE command cannot
affect row a second time"*, verbatim to the user. It is refused up front now, naming the ingredient
and saying to combine the quantities; sub-recipes are checked too, where there is no constraint at
all and a duplicate saves happily and double-counts. And **"Suggested @ 30% FC" is VAT-INCLUSIVE**
while sitting two tiles from "Selling Price (ex. VAT)" — the label now says so, on the detail view,
in the edit form and on the printed cost card.

**Checked and left alone:** the Yield Quantity field and its "cost per unit = total ÷ yield qty"
tooltip render only on the sub-recipe branch of the form, where that is exactly what
`calcSubRecipeCostPerUnit` does — the initial analysis called this a mismatch and was wrong. A dish
never sees the field, and `yield_qty` is inert for one in every engine.

**Files:** `src/utils/recipeCost.js`, `src/utils/recipeCost.test.js`,
`src/modules/ims/recipes/Recipes.js`, `src/modules/ims/recipes/RecipeCostCardPrint.jsx`,
`src/modules/ims/stockcount/subRecipeUsage.test.js`, `src/pages/Help.js`,
`src/pages/settings/imsGuideData.js`, `public/service-worker.js`, `src/shared/appVersion.js`,
`.claude/rules/recipes-and-subrecipes.md`, `CLAUDE.md`, `docs/CROSS-REPO.md`, `POS_TODO.md`.

The Help tip for Del said the delete is blocked "if anything else uses it as a sub-recipe
ingredient" — true, and now half the story; it names the sales case too. The IMS module guide's
Recipe Costing entry gained the FK table, the delete ordering and the paging fact, since a guide
that describes the old behaviour is how a bug gets taught (S711 follows the S706/S708 pattern here).

**Docs, second pass.** `CLAUDE.md`'s FK-guard paragraph gained the ORDER half in one sentence —
put the step the FK can refuse first and let the cascade take the children — because that is the
one part of this reachable from anywhere; everything recipe-specific stayed in the rules file.
`docs/CROSS-REPO.md` gained a row: IMS is not shared, but **the delete-ordering shape is**, and
hss-suite's ported HR module has the same candidates (an employee with payslips, a settlement with
its components, a roster week with its shifts) — the tell is a second `.delete()` on the parent
further down the same function, or an error message apologising for a state the ordering created.
`POS_TODO.md` records `pos_order_items.recipe_id` having no FK at all as 🟡: the S711 page guard
covers the app path, but per S707's own lesson a guard in the page is not a guard on the table, and
closing it is a migration that has to choose between SET NULL and a plain FK and check for existing
orphans first. `README.md`, `DESIGN.md`, `PRODUCT.md` and `DOCS-REMEDIATION.md` were read and
deliberately left alone — nothing in them describes anything this session moved.

**`CLAUDE.md` is now at 52,835 of its 53,000 ceiling — 165 chars.** That is tighter than the 391
that triggered the S678 migration pass, so the next session that wants a line there has to migrate
something out first rather than trimming a word to fit. Flagged rather than fixed: choosing what
leaves the root file is a `/doctor` decision, not a side effect of a bug-fix session.

Full test suite 847/847, `npm run build:verify` clean.

---

### S710 — 2026-09-09 — Requisitions: an issue that could commit half of itself, and 38 policies that never learned about multi-outlet

**Service worker `crest-v246` → `crest-v247`, `APP_VERSION` with it.** App code changed.
**Three migrations, all applied 2026-09-09**: `20260909160000_rls_multi_outlet_inline_client_id.sql`,
`20260909170000_requisition_line_rate_and_qty_guards.sql`, and
`20260909180000_rls_multi_outlet_unqualified_client_id.sql` — the third written after the second's
own verification query found the first one incomplete.

A re-analysis of Requisitions, built in S60 and never swept since — S695/S696 touched only its
on-hand guard. Fourteen findings, all fixed. One of them turned out not to be about this page at
all.

**The issue path could commit half of itself, and the half it kept was the wrong one.**
`confirmIssue` flipped the header to `status = 'issued'` FIRST, then fired one `qty_issued` update
per line inside a `Promise.all` whose results were never destructured — the errors were discarded
entirely. A failure there left a requisition reading **ISSUED with `qty_issued = 0` on every line**:
worth NPR 0 in the list, worth 0 in Stock Count's Requisitioned column, and beyond repair, because
Issue is only ever offered on a draft. The header write itself was no better — `if (hErr) {
setSaving(false); return }`, a silent return with no message, so pressing Confirm Issue on a
refusal did nothing at all and said nothing about it. Now the **lines are written first and the
status last**, both errors surface through `ActionError`, and each message names the state the
record is actually in ("the quantities were saved but the requisition is still showing as a draft —
open it and press Issue again, the quantities are already as you left them"). A failure now leaves
a retryable draft.

**A double-click made two requisitions.** `setSaving(true)` sat *after* the shortfall check, which
is eight network reads deep — so both buttons stayed live and undisabled for the whole of it, and
two clicks on Save & Issue ran two complete inserts. The flag now goes up before the await in both
`saveReq` and `confirmIssue`.

**`deleteReq` threw its error away.** A bare `await scopedDelete(...)` with nothing destructured:
supabase-js RESOLVES with `{ data, error }`, so an RLS refusal reloaded the list and the row simply
reappeared with nothing on screen to say why. It reads the error now, and there is a page-level
`ActionError` above every mode to put it in — Issue and Delete happen on the list and detail
screens, neither of which had anywhere to show a failure.

**An issued slip re-priced itself every time an item was bought.** `requisition_lines` stored
quantities and nothing else, so all five places this page shows money — line value, slip total, list
column, Excel export, printed slip — multiplied by `items.per_uom_rate` read live at render.
`PurchaseBillPage` rewrites `items.rate` on every bill carrying the item, so a slip issued in
Shrawan, signed by whoever received the goods and filed, reprinted in Bhadra with a different total
and nothing on it saying why. `requisition_lines.rate` now captures the rate at write time the way
`purchase_entries` always has, read back through one `lineRate()` helper. The column is **nullable
on purpose**: rows written before today have no snapshot, and backfilling them from today's rate
would be fabricating a history rather than recovering one — they fall back to the live rate, exactly
as they already did.

**An issued requisition was terminal in every direction** — no edit, no delete, no un-issue — so a
mis-keyed quantity was permanent, and so was a slip raised against the wrong department. A
supervisor now gets **Correct Quantities** and **Delete** on an issued slip, both gone once the
period closes. Correcting deliberately does NOT re-snapshot the rate (that would reintroduce the
fault the column exists to prevent) and opens on what was actually issued, including a line issued
as 0 — the draft prefill would have pushed that back up to the requested figure and quietly changed
a number nobody touched.

**A negative quantity passed every check.** `min="0"` on a number input is a hint, not a
constraint; nothing enforces it on a paste, and the shortfall test (`issuing <= available`) waves a
negative straight through. A negative `qty_issued` reached Stock Count's Requisitioned column and
SUBTRACTED from a cross-check figure a month gets closed against. Refused in the page with a
sentence, and by three `CHECK` constraints in the database (added `NOT VALID`, so they bind every
write from here without a scan that could fail the migration on a legacy row).

**The stat strip ignored the department tabs directly beneath it.** Filter to Bar and the table
showed Bar while "Total Issued Value" above it still totalled every department. All four cards now
count `filteredReqs`, and each names the department it is on rather than leaving the reader to infer
it.

**The close dialog named four of the eight surfaces that actually lock.** `grep -rn
"periodClosed\|isLocked" src/modules/ims` returns eight — Purchases, PurchaseBillPage, ReturnsTab,
PurchaseOrders, Sales, Stock, Overheads, Requisitions — while the Dashboard's bullet and the module
guide both said "Purchases, Sales, Stock Count and Overheads". Requisitions had locked since it was
built and was named nowhere; Purchase Orders joined the set in S709 yesterday and the copy did not
move with it. This is S683's defect again, and the lesson is the same one level up: **an enumeration
in user-facing copy is a second definition of the lock set.** Periods.js is fine precisely because
it enumerates nothing.

**The module guide still taught the formula S695 deleted** — "− already-issued Requisitions" in the
on-hand line, removed two sessions ago for double-counting. Rewritten, along with the Help entry and
a new field note for the stored rate.

**Thirty-eight live RLS policies never called `my_client_id()` — they inlined its pre-S548 body.**
This is the finding that was not about Requisitions. `client_own` on `requisitions` reads
`client_id = (SELECT profiles.client_id FROM profiles WHERE id = auth.uid())`, which is exactly what
`my_client_id()` USED to be before multi-outlet redefined it as `coalesce(active_client_id,
client_id)`. The whole S548 architecture is "change one function and every policy follows"; an
inlined copy silently opts out. Sixteen came from the baseline and were carried through the
`20260812130000` initplan-wrap pass verbatim — that migration was rewriting the `auth.uid()` calls
*inside* those expressions and had no reason to read the rest of them. **The other nine were written
after multi-outlet shipped**: `pos_cash_movements`, `pos_kot_removals`, the loyalty pair and the
reservations pair, each copied from the table next to it. Between them they cover most of the
transactional book — Requisitions, POS (orders, items, payments, shifts, tables, KOT, credit notes,
customers, cash, loyalty, reservations), Purchase Orders, payables, staff meals, stock movements,
budgets, demand forecast, recipe suggestions.

**And the first fix caught only 25 of them, which is the more useful half of this finding.** The
migration was generated from a scan of the migration files for the string `profiles.client_id`.
Applied, the verification query still returned **13**. Those thirteen — the whole Fixed Assets module,
Gate Passes, guest order requests, parking slips, payment confirmations, all from `20260812130000` —
are written with **unqualified** columns:

```sql
client_id = (SELECT client_id FROM profiles WHERE id = (select auth.uid()))     -- the file
client_id = (SELECT profiles.client_id FROM profiles WHERE profiles.id = ...)   -- pg_policies
```

Postgres parses the expression and re-prints it qualified, so **the source text in the repo and the
stored text in the catalog are two spellings of one thing**, and a grep tuned to either is blind to
the other. `20260909180000` fixes those thirteen. The rule that comes out of it is worth more than
either migration: **a policy audit runs against `pg_policies`, not against the migration files** —
the files are what you edit, the catalog is what is true. The standing check earned its place on its
first run by finding the incompleteness of the fix that introduced it.

(Generating the second migration turned up a third thing worth naming: the substitution regex ended
`\)?\s*\)`, and against the unqualified spelling the greedy optional swallowed the enclosing
clause's own closing paren, producing statements that would not parse. It was caught by asserting
that every emitted clause is paren-balanced and starts and ends with one — the extraction is
balanced-paren scanning now, not a regex. **A generated migration needs a check on its output, not
only on its input.**)

Two things make this worth a standing check rather than a one-off fix. It is **invisible until the
first group exists** — with `active_client_id` NULL the inline expression and the function are byte-identical,
so nothing today can exercise it — and it fails in the **quiet** direction: a switched outlet's
reads come back `{ data: [], error: null }`, an empty POS floor and an empty requisition list being
indistinguishable from a new outlet, while writes raise 42501 on a row the user watched themselves
type. Same shape as the three defects that sat in multi-outlet from S548 to S617: a feature with no
users accumulates faults every review passes over. The migration is a textual, semantics-preserving
substitution generated from each policy's current definition — nothing else moves, not the admin
clause, not the permissive/restrictive split, not the command set — and this must now stay empty:

```sql
SELECT tablename, policyname FROM pg_policies WHERE schemaname = 'public'
 AND (qual LIKE '%profiles.client_id%' OR with_check LIKE '%profiles.client_id%');
```

**Smaller, in the same pass.** The over-issue guard's `opening_stock`, `closing_stock`,
`staff_meals` and `vendor_returns` reads were unpaged, where Stock Count pages all four and says why
— each is one row per item per period, and a silent 1000-row truncation returns no error for
`firstError()` to catch. `StockReport.js` had the identical gap and is swept with it, because the
guard's whole contract is to agree with that report about what is available. `excludeReqId` was a
dead parameter on `getOnHandMap`/`checkStockShortfall` since S695 removed the read it selected out
of. `init()` never called `periodReq.begin()`, which the hook's own doc requires of anything that
auto-selects a period — its trailing `setLoading(false)` could unveil the previous period's list
under the new period's label. And four of the eighteen departments carry a slash ("Pastry /
Bakery"), which went straight into the export filename.

**Files:** `src/modules/ims/sales/Requisitions.js`, `src/modules/ims/stockcount/StockReport.js`,
`src/pages/dashboard/ClientDashboard.jsx`, `src/pages/settings/imsGuideData.js`,
`src/pages/Help.js`, `src/shared/appVersion.js`, `public/service-worker.js`,
`supabase/migrations/20260909160000_rls_multi_outlet_inline_client_id.sql`,
`supabase/migrations/20260909170000_requisition_line_rate_and_qty_guards.sql`,
`supabase/migrations/20260909180000_rls_multi_outlet_unqualified_client_id.sql`,
`.claude/rules/multi-outlet.md`, `.claude/rules/supabase-sql.md`,
`.claude/rules/closed-periods.md`, `.claude/rules/ims-figures.md`. Cache `crest-v247`.

---

### S709 — 2026-09-09 — Purchase Orders: the un-swept sibling of the page it writes into

**Service worker `crest-v245` → `crest-v246`, `APP_VERSION` with it.** App code changed.
**One migration to apply**: `20260909150000_purchase_order_receipt_integrity.sql`.

A re-analysis of Purchase Orders, which had never had the sweep `Purchases.js` got in S698 — and
writes the same table. Eighteen findings, all fixed. The framing that made them easy to see:
**receiving goods IS a purchase entry**, so every rule the purchases module had learned applied
here, and none of them had been applied.

**A receipt was four round trips, and the last two could fail in silence.** `confirmReceive`
inserted the bills, fired one `qty_received` update per line, then updated the PO's status — and
that last write was a bare `await` with no error read at all. A dropped status write leaves the PO
on `draft` holding received quantities, and `draft` is exactly the status that still offers **Edit**,
whose save replaces every line row with a fresh one at `qty_received: 0`. The delivery then reads as
never received and can be received, and billed, a second time. Every link of that chain shipped.
`receive_purchase_order()` now does the lot in one transaction: it takes the PO row `FOR UPDATE`,
re-checks each line's remaining quantity, refuses a closed period, writes the bills, **increments**
`qty_received` rather than assigning the number this browser computed, and derives the status from
the table. Either all of it lands or none of it does.

That increment is the second finding. `qty_received` was a read-modify-write off the snapshot the
screen loaded with, so two people receiving one delivery — a storekeeper at the door and a manager
at the desk, which is how a delivery actually arrives — each banked it and the second erased the
first. The over-receive guard was computed off the same stale number, so it could not see it either.
The screen also re-reads the order's lines when it opens now, and **fails closed**: a failed read
does not open the receive screen at all, because "Remaining: 50" drawn from an error is the most
expensive wrong number this page can show.

**There was no closed-period lock, on the one page in the module that writes the locked table.**
`Purchases.js`, `PurchaseBillPage.jsx`, `Sales.js`, `Stock.js` and `Overheads.js` all spell
`const isLocked = !isAdmin && selectedPeriod?.status === 'closed'`; this page never had it, so
receiving was the way around a month-end close for anyone who happened to be here. It is the sixth
copy now, with both banners the others carry — and the first with a **server-side** half, since a
receipt is an RPC and an RPC that can be called directly is a door the page does not control. Why it
went unnoticed is worth keeping: it does not look like an entry page. It is a document workflow.
**Ask which tables a page writes, not which shape it has.**

**Nothing linked a bill back to the order it came from.** `purchase_entries` had no `po_id`; the
only trace was `invoice_ref = po_number`, free text that Purchases lets anyone overwrite. So
deleting the bill left the PO reading Received with no way back, editing its quantity diverged
ordered from billed silently, and no screen could answer "what did we order, and what actually
arrived". The column exists now, the receipt writes it, and the receive screen says what the order
has already produced. `save_purchase_bill` was amended in the same migration to **carry that link
through an edit** — without it the first correction anyone made to a received bill in Purchases cut
it loose. A link that does not survive an ordinary edit is not a link.

**The delete guard lived only in the page**, which is S707's sentence two tables over.
`if (!isAdmin) return` was the whole of it, and `purchase_orders` has no `no_ims_staff` fence — so
`DELETE /rest/v1/purchase_orders?id=eq.<uuid>` succeeded for every IMS account of any rank,
including a `staff` who cannot open the page and never sees the button.
`purchase_orders_guard_delete` refuses that, and refuses any order with bills against it for anyone,
operator included: `po_id` is `ON DELETE SET NULL`, so that delete would have SUCCEEDED and stripped
the link off the bills — the `confdeltype` trap, on a column added the same day. The page pre-checks
and words its own refusal; Cancel is the way through, and on a part-received order it is labelled
**Close Short** now and says what happens to the part that did arrive. Its old tooltip promised "no
purchase entries will be created" on orders whose entries already existed.

**The delivery was dated to today's day number inside the order's month.** Receive a Shrawan PO on
Bhadra 5 and the bill was filed as Shrawan 5, with nothing on screen to say so; the day box was
bounded 1–32, the range the column's CHECK allows rather than the 29–32 days a real BS month has. It
is a `BsCalendarPicker` locked to the order's own period now — it can only offer days that month
has — prefilled with today only when today falls in that period, and the screen states the month the
bill will land in whenever it differs from the one on screen.

**The VAT toggle was sticky.** `openReceive` reset the payment method, the error and the day, and
never `receiveVatInclusive` — so ticking it for one vendor left it ticked for the next order opened
in the same session, and every rate on that delivery was quietly divided by 1.13.

**The edit path could double a PO.** The orphan-line delete was `.not('id','in',(<new ids>))` with
its error discarded: an empty id list renders as `in.()`, which PostgREST rejects outright, and any
failure left the order holding BOTH sets of lines. A doubled PO does not look broken — it looks like
an order for twice as much, and it prices and receives that way. It deletes the ids the form was
opened on now, checks the result, and says what state the order is in if that fails. Editing also
requires draft **and** nothing received, belt-and-braces with the RPC.

**Every read on the page dropped its error** (S594/S612, for the fourth session running). A failed
periods read wore `NoPeriodState` — "no periods yet, create one" — and a failed PO read wore the
empty state: *"No purchase orders raised for Bhadra yet. Use + New PO to raise one"*, which invites
raising a second order for one that already exists. `firstError` + `ReportLoadError` now, and the
last good rows stay on screen under the error card.

**Editing an order whose vendor had since been archived painted "— Select vendor —" over it** —
state kept the id, so it saved correctly right up until someone touched the dropdown, at which point
the vendor was gone with no way back. Same for a deactivated item on a line. This is the repair S698
made in `PurchaseBillPage` for the same reason, on the page it was never carried to; both are
fetched and labelled *(inactive)* now.

**Smaller, and all real.** `getNextPoNumber()` scanned unpaged, so past 1000 orders the max could
sit outside the window and mint a duplicate — paged, and a failed read stops the save now rather
than numbering off a short list (sorting by `po_number` instead is not the shortcut it looks:
`PO-1000` sorts below `PO-999`). Expected Delivery was picked in BS, stored as AD and displayed as
AD — in the list and on the printed order the supplier receives; `formatAdAsBs()` joins
`bsCalendar.js` as the one home for that composition, which two HR files still hand-roll. The
printed order rounded each line to the rupee and took its total off the unrounded figures, so a
document a vendor invoices against need not add up; both are 2dp now and the total is the sum of the
printed lines. Quantities round to 3 decimals — the precision the column stores — so 0.3 ordered and
0.1 received twice stops leaving `0.09999999999999999` still on order, printed at the user in the
refusal. The `items` read pulled a `categories(name)` join nothing on the page renders. Saving an
order into a period other than the one on screen made it vanish, which reads as a save that did not
happen; the list follows it there. `init()` never claimed the page through `useLatestRequest` (its
own contract, the same miss S698 fixed next door). And every save is wrapped in `withTimeout` — a
supabase call can hang forever, and the `saving` flag with it.

**One thing deliberately not built.** Deleting a PO-created bill in Purchases still does not
un-receive the PO; the order keeps its received quantities. Un-receiving is a stock correction, and
stock corrections belong in Stock Count, not in a delete. The receive screen's "already billed" line
is where that divergence is now visible.

Six new `errorText` rules, with tests: these refusals are raised inside the one transaction, so
unlike a dropped fetch they genuinely do prove nothing landed — and saying so is what makes the
delivery safe to re-enter. `formatAdAsBs` has tests of its own, including the local-midnight parse
that keeps a bare `YYYY-MM-DD` off the previous day at +05:45.

Docs: Help → Purchase Orders (rewritten — it described an "approved" status the product does not
have), the IMS module guide's Purchase Orders section, `closed-periods.md` (six copies, and why this
page hid), `supabase-sql.md` (the third atomic RPC, the fourth delete trigger) and
`vendor-payables.md` (the S709 decisions, and that everything on this page is in BASE units).

---

### S708 — 2026-09-09 — Vendors: archiving a supplier removed it from its own reports

**Service worker `crest-v244` → `crest-v245`, `APP_VERSION` with it.** App code changed.
**One migration to apply**: `20260909140000_vendors_delete_reference_guard.sql`.

A re-analysis of the vendors surface — the page, its four referencing tables, the DB layer, and the
two reports that read vendor history. Ten findings; three were worth acting on today.

**Archiving a vendor deleted it from the two reports built to keep it.** S671 kept the `vendors` row
precisely so history stays readable, and the page says so in those words: "every past record keeps
its supplier", and Confirm Balance stays on an archived row "because reading its history is the
reason the row was kept." Both reports filtered `.eq('is_active', true)`, and archiving *forces*
`is_active = false` — so using the feature was what broke the promise it made.

On **Vendor Report** that filter decided the row set of a historical report. `vendorSummary`, the
Daily Breakdown matrix, the vendor combobox and the Excel export all mapped over active vendors,
while the footer TOTAL, the four KPI cards and `% of Net Total` were computed over *every* purchase
row in the period. A vendor deactivated mid-year therefore had its spend inside the total with no
row above it: the rows visibly failing to sum to their own footer, which discredits the rows that
were right (the S594 lesson). The fix is to drop the filter — `vendorSummary` already ends in
`.filter(r => r.gross > 0 || r.returned > 0)`, so an inactive vendor appears only if it genuinely
traded in the month being read, and the combobox is driven off that same list.

On **Vendor Balance Confirmation** it was a dead end rather than a wrong number. The `?vendor=` link
Vendors.js puts on every row preselects only if the id is in the loaded list (`(v || []).some(...)`)
— correct, and it is what stops a stale id selecting a vendor the client cannot see — but with the
list filtered to active vendors an archived one failed that test silently, so the button landed on
"Select a vendor to generate a balance confirmation" with that vendor absent from the dropdown too.
The picker now loads every vendor and splits the inactive ones under a **No longer active** optgroup:
a supplier still being bought from and one archived two years ago are both legitimate on a letter
about history, but they are not equally likely to be the one being looked for.

**`loadVendors` dropped its read error — the same bug Items.js fixed and wrote up, one directory
over.** `const { data } = await scopedFrom('vendors')` set the list to `[]` on a failed read,
rendered "No vendors yet. Add your suppliers to get started." over a client's entire supplier book,
and **wrote that empty array into the session cache**, so the next visit repainted it instantly with
no read in flight to correct it. It has a knock-on Items does not: `getNextVendorCode()` takes its
max over this array, so on an empty one it mints `VND-001` again on top of the real `VND-001`, and
nothing enforces vendor-code uniqueness in the database. Now it destructures `error`, keeps the last
good list up under a "may be out of date" banner, and shows a failed-read block instead of the empty
state. Paged with `fetchAllRows` in the same change and for the same reason: a read silently
truncated at PostgREST's 1000 rows mints the same duplicate code off the visible slice. A search
matching nothing also gets its own line — the empty state keyed off `viewing`, not `filtered`, so it
used to render a header row over an empty tbody.

**The delete guard moved into the database** (`vendors_guard_referenced_delete`, a BEFORE DELETE
trigger over `vendor_reference_counts()`). S671's browser guard is thorough — all four tables, a
click-time re-read, failing closed when that re-read errors — and none of that is a guard, for the
reason S707 had written down one table over. `vendors` carries no `no_ims_staff` fence, so every IMS
account of any rank can issue `DELETE /rest/v1/vendors?id=eq.<uuid>` with its own JWT, including an
`ims_role = 'staff'` account that cannot open the Vendors page at all and never sees the admin-only
Delete control. Two of the four referencing tables refuse that delete; `vendor_returns` and
`ims_gate_passes` are `ON DELETE SET NULL`, so it **succeeds** and strips the supplier off them. The
loss on `vendor_returns` is unrecoverable in a specific way worth recording: it keeps no name of its
own, its `log_audit` trigger records the SET NULL as an UPDATE carrying the old `vendor_id`, and
that id now resolves to a row that no longer exists — `vendors` itself has no audit trigger, so the
deleted row leaves no snapshot anywhere and the name is simply gone.

The trigger also closes a race the page cannot: its re-check and its DELETE are two round trips, so
a bill entered on another till in between is checked before it exists and deleted around afterwards.

**There is deliberately no `force_delete_vendor()`**, which is where this departs from its S707
sibling. `force_delete_item` exists because an item can be a genuine mis-entry someone needs gone.
A vendor with history is not that — it is a supplier the client really did buy from, and the only
thing a force-delete could do is destroy the record proving it. Archive already loses nothing, so it
is the way through, with no bypass for a client account of any rank, operator included; the service
role stays the escape hatch that keeps Danger Zone working.

**Left open, and named so they are decisions rather than omissions**: `vendors` still has no
`UNIQUE (client_id, lower(name))` and no client-side duplicate check either, so it sits where
`items` was before S706 — a duplicate splits one supplier's payables across two rows in Outstanding
Payables, two lines in Vendor Report and two balance letters each carrying half the balance, and a
lost response on Add Vendor retried makes the second row silently. `VENDOR_REF_TABLES` is inline in
the page with no migration-reading test of the kind `itemRefTables.test.js` is. There is no
`loadedClientRef` guard, so switching client leaves the previous one's vendor rows on screen until
the new read lands. The vendor-code prefix goes into `new RegExp` unescaped (as it does in Items.js).
Settings' vendor renumber loop checks no error yet reports "✓ Renumbered N vendors". And `loadUsage`
transfers every purchase line the client has ever entered just to count them, where
`vendor_reference_counts()` — added by this migration — is one round trip.

---

### S707 — 2026-09-09 — Item Master: the two guards S706 left as advice

**Service worker `crest-v243` → `crest-v244`, `APP_VERSION` with it.** App code changed.
**Two migrations to apply**, in order: `20260909120000_items_client_name_unique.sql`, then
`20260909130000_items_delete_reference_guard.sql`.

S706 fixed Item Master's duplicate-name check and its delete guard. Both fixes were real and both
lived entirely in the browser, which is a different thing from being enforced. This session closed
the gap on each.

**`items` now has `UNIQUE (client_id, lower(name))`.** The S706 check tests an array held in React
state, so two tabs both pass it — and worse, `loadItems` filters `.eq('is_sub_recipe', false)`, so
the array does not contain sub-recipe mirror rows at all. Stock Count deliberately does not filter
them either, so a real item and a mirror sharing one name are two rows in the same count: the exact
split-the-ingredient harm the check exists to prevent, reached by the one route it could not see.
Three of the four write paths into `items` never ran the check in any form — the Recipes.js mirror,
`push_master_data`, and the Export/Import restore.

The index is case-insensitive (the push's own adoption already matches on `lower(name)`), covers
mirrors, and covers hidden items — hiding is what every delete refusal offers as the alternative,
so hidden rows accumulate by design and still carry the history their name refers to. A looped
dedupe pass renames pre-existing duplicates `-DUP2`, `-DUP3` before the index is created, ordering
real items ahead of mirrors so a real item always keeps the plain name, and asserts it converged
rather than letting `CREATE UNIQUE INDEX` fail on data nobody could see beforehand. **Review
anything it renames**: a `-DUP2` is a genuine pre-existing split that someone has to merge or
retire.

Three call sites had to learn the constraint before it could be added safely. `push_master_data`
gained a fourth plan action, **`conflict`** — without it one colliding item name would raise 23505
and abort an entire multi-outlet push with a constraint name for a message; it is now reported in
the preview, per branch, naming the record in the way, and skipped by the apply pass while the rest
lands. `Recipes.js` checks the mirror's name in both directions and says what a clash means (a
sub-recipe is stock-counted, so the two would split that stock) instead of surfacing a raw 23505.
`restoreClientData` deduped the artifact on the way in — the restore loop breaks a table on its
first failing chunk, so a single duplicate pair in a pre-index backup would have abandoned the
client's entire item book and then every table referencing it.

**The delete guard moved into the database.** `items` carries one permissive policy and restrictive
fences for POS PIN staff, HR self-service and HR-role staff — and is correctly absent from the
`no_ims_staff` list, because IMS needs the item book. So every IMS account of any rank could
`DELETE /rest/v1/items?id=eq.<uuid>` with its own JWT: including `ims_role = 'staff'`, which cannot
open Item Master at all. Five of the eleven referencing tables hold that delete. The three
`ON DELETE CASCADE` tables go with it, and two of those three carry no `log_audit` trigger, so
`requisition_lines` and `staff_meals` leave no trace anywhere — staff meals being inside
`computeUsed()` and therefore inside COGS. There is no server-side closed-period write guard, so a
closed period was not protection either.

It is a **BEFORE DELETE trigger**, not an RPC, per privilege invariant #3: an RPC protects only the
callers that choose to call it and leaves the open policy exactly as wide. Same shape as S698's
`purchase_entries_guard_paid_delete` — SECURITY INVOKER, keyed on `current_user`, so the service
role and SECURITY DEFINER bodies pass through. `force_delete_item()` is that one way through:
admin-only under `COALESCE(is_admin(), false)`, and **atomic**, which the twelve separate HTTP
requests it replaces could never be. That loop is precisely what left eight tables emptied and the
item standing in S706; a function body is one transaction, so the failure message can now say
nothing was removed and mean it.

Smaller things in the same pass: `getNextItemCode()` minted from the filtered, cache-seeded array,
so a mirror's code never counted toward the max and the next item could be minted onto it — it
reads the unfiltered book now (`item_code` is deliberately left without a unique index; nothing
keys off it, and pushing HQ's codes into a branch that minted its own would make every such push an
abort). `errorText.js` gained four rules ahead of the generic 23505 one, since "that already
exists" says nothing about what splits if you proceed. `itemRefTables.test.js` now also reads the
new migration and asserts the SQL table list matches `ITEM_REF_TABLES` in membership *and* order,
and that the INVOKER/DEFINER pair has not been swapped — the list exists twice now, because the
server cannot import a `.js` module.

Deliberately not done: a completion report after a force-delete. The function returns what it
cleared per table, and the only page-level channel on Item Master is `ActionError`, which is red
and carries `role="alert"` — a success report rendered through it reads as a failure, and a neutral
notice is its own piece of design work.

**The IMS module guide had drifted twice and been half-corrected twice.** Its `workflow` array still
taught the Add/Edit modal as "Purchase Qty, Rate … Purchase Unit / Base Unit / Conversion Factor" —
`Purchase Qty` was removed in S597 and `Base Unit` in S706, and both of those sessions updated the
`fields` and `gotchas` arrays directly below it while leaving the workflow line describing a form
that had not existed for two sessions. The same line's neighbour still said delete is blocked
"across 8 tables" while the gotcha S706 added four lines later said eleven. Both corrected, plus a
Help tip for the name rule. This is the drift the module-guide rule exists for: a guide is prose
about a screen, so nothing fails when it stops being true, and fixing the paragraph you came for
is not the same as fixing the section.

Rules updated, and the list is longer than the change because a constraint on a shared table is a
fact several rules had written down: `item-master-rates.md` (two new sections), `error-messages.md`,
`multi-outlet.md` (which asserted in so many words that `items` had no `UNIQUE(client_id, name)`,
and now carries the `conflict` action and the latent double-adopt fault the index closes),
`recipes-and-subrecipes.md` (the mirror shares one name space with the item book, and why the
constraint could not be scoped away from it), `data-export.md` (a unique index added after a backup
was taken is a restore hazard, and this loop makes it a whole-table one), `supabase-sql.md` (the
`BEFORE DELETE` guard now has a second instance, so what generalises from it is written down), and
the two places in `CLAUDE.md` that asserted the same missing index.

---

### S706 — 2026-09-09 — Item Master: a delete guard the database only half backs up

**Service worker `crest-v242` → `crest-v243`, `APP_VERSION` with it.** App code changed.

A re-analysis of the Item Master surface — `Items.js`, its route/nav pair, the eleven tables that
reference `items`, and the IMS guide entry — found twelve things. The first three are one defect
wearing three faces, and the face nobody could see is that **the database is a backstop for five
of the eleven referencing tables and not for the other three.**

**Force-delete destroyed history and then failed, telling the operator to try again.** The clearing
loop knew eight tables. `par_levels`, `purchase_order_items` and `stock_movements` reference
`items` too, all with plain FKs — so Postgres refused the final delete *after* the other eight had
been emptied, and the item survived with its purchases, stock counts, wastage, requisitions,
vendor returns and recipe lines already gone. The message said "Try the delete again", which could
never work: retrying repeats the same refusal and clears nothing further. Any item with a par
level, a purchase-order line or POS depletion hit this. The list is now one exported constant,
`itemRefTables.js`, read by both the usage scan and the delete loop, so the two sets cannot
diverge again — and `itemRefTables.test.js` reads the migrations and fails when a new `item_id` FK
does not reach it (verified to fail against the pre-fix list, on both the omission and a wrong
cascade flag).

**Three of those FKs are `ON DELETE CASCADE`, so a wrong guard does not fail safely.**
`requisition_lines`, `staff_meals` and `vendor_returns` are deleted *with* the item rather than
blocking it — the `confdeltype` rule this repo already learned on `vendors`, in the one place where
the consequence is destroyed history. The guard was reading `usageMap`, which only counts a row
when `qty > 0`, and **`staff_meals.qty` defaults to 0** — so a zero-quantity staff meal earned no
badge, passed the guard, and cascaded away under a confirm dialog reading *"Nothing references this
item, so no purchase, count or recipe changes."* One set of reads now builds two maps: `usageMap`
(live usage, the chip) and `refMap` (any row at all, the only thing the guard may consult).

**And the scan itself could fail open.** Every item id went into one `.in()` — the reference
client's 254 items are ~9 KB of uuids per request, past what proxies accept — and each table's
failure was *skipped quietly*, which blanked the whole Used In column and left every item looking
unreferenced. Chunked through `fetchAllRowsChunked` now, and a table that could not be read is
recorded rather than treated as clear: the column says "not checked" instead of showing a dash,
a banner names what failed, and the delete refuses outright. An absent chip means "no records" or
it means nothing at all.

**Two reads stopped at 1000.** `loadItems` was a bare select, so past 1000 SKUs the page showed a
partial item book as the whole one — and `getNextItemCode()` takes its max over what it can see,
while `item_code` carries no unique index, so it would have minted duplicates. The id read feeding
the usage scan was capped the same way, meaning items past the cap were never checked for usage at
all. Both paged, with `.order('id')` as the tiebreaker.

**A failed read rendered as an empty item book, and then cached it.** Both loaders dropped `error`,
so `data || []` printed "No items yet. Add your first ingredient to get started" over a client's
entire master list — and `writePageCache` stored that empty array for ten minutes, so the next
visit repainted it instantly with no read in flight to correct it. Neither loader now caches a
result it did not get; a failed read renders as a failed read, and a failed *refresh* over a list
already on screen says the rows may be out of date rather than looking freshly loaded.

**`yield_pct` was unvalidated at save.** `min="1" max="100"` on the input is decorative — the
dialog is not a `<form>` and Save is a plain `onClick` — while the column is `numeric(5,2)` and
every recipe cost divides by it, so a typed `500` makes every dish using that item cost a fifth of
what it does, silently and everywhere at once. A `0` became `100` with no message, which is a
different number from the one the user typed.

**`base_unit` was a field nothing reads.** The Purchase Bill scales `qty × conversion_factor` into
the item's `uom` and never looks at `base_unit`, so a base unit disagreeing with the UOM was always
a mistake — and the only screen showing it, this form's own preview, labelled the rate
"per {base_unit}" when the rate is per UOM. The box is gone; the tab states the UOM instead, the
value is derived on save, and a legacy row repairs itself the next time it is edited. Same move as
S597's `Purchase Qty`: a field with one correct answer must not look like a choice.

**Hide's stated consequence was incomplete.** Six report reads filter `.eq('is_active', true)` per
the S436 rule that stock is never valued off an inactive item, so hiding an item that still holds
stock takes its value out of the stock and monthly figures. Every refusal offers hiding as the
gentle alternative and none of them said so. And `toggleActive` — the action all of them point at —
was a bare `await` with nothing destructured, so a refused Hide reloaded the row unchanged, which
reads as the item already being in that state.

Smaller, from the same read: the FK check matched on `error.message` text rather than code `23503`;
an admin switching clients kept the previous client's items on screen, filterable and editable,
because `loading` was only raised when the list happened to be empty; Print emitted the *filtered*
subset under a bare "Item Master" while every filter control is `no-print`, and now carries a scope
line; `clearAllConversions` counted non-sub-recipe items and updated sub-recipe mirrors too; a
duplicate item name saved happily (`items` has no `UNIQUE(client_id, name)`) and split an
ingredient's purchases across two master rows; and the actions cell was `display: flex` on the
`<td>` itself. The IMS guide's Conversion entry, its preview formula and two new gotchas were
updated in the same change — it had been teaching the `base_unit` shape.

---

### S705 — 2026-09-09 — Periods: a gate that was no gate, and two reads that stopped at 1000

**Service worker `crest-v241` → `crest-v242`, `APP_VERSION` with it.**

A re-analysis of the period surface — `Periods.js`, `closePeriod.js`, the two notes files and the
route/nav pair — found six things. Two of them were silent-wrong-number bugs of the kind this repo
already has a rule for, and one was an access hole that only exists for the clients the page's
guard was written to accommodate.

**A gate conditional on a module is no gate at all for the clients without it.** The page read
`if (clientModules?.ims && !hasImsAccess('supervisor'))`. The carve-out is necessary —
`hasImsAccess()` returns false for everyone but admin once `ims_enabled` is off, so the IMS rank
alone would lock a POS-only client's own Owner out of their own periods — but skipping the check
entirely when IMS is off left the page **role-less** for exactly those clients. The nav item is
tagged `minImsRole: 'supervisor'`, so this is the S601 shape again, and this time a WRITE sat
behind it: a `pos_role: 'staff'` waiter who typed `/periods` got the "Post POS bills to Inventory"
button, which writes `sales_entries` and `stock_movements`, and `backfillPosToIms.js` carries no
check of its own. Now `!isAdmin && !isOwner && !hasImsAccess('supervisor')`, falling back to the
rank the module system always has an answer for instead of to nothing. Nothing regressed: an IMS
client's sub-supervisor was already redirected, and the Owner reaches the page either way.

`MenuPricing.js:339` is the same line and cites this one as precedent. It exposes no write in that
state, and its two branches are a documented ask-first file, so it was left and is filed here.

**`carryForwardOpeningStock` read one row per item with a bare `.select()`**, so a client past
1000 SKUs carried forward only the first 1000 — no error, nothing in the data to say so. The bite
is that `closingCountPreflight()` counts with `head: true`, which is **not** capped: the close
dialog said *"All 1,203 active items have a closing count"* while 203 of them entered the new month
at zero. S616 aligned those two populations for exactly this reason and the cap silently un-aligned
them above 1000. Paged through `fetchAllRows` with `.order('item_id')` as the tiebreaker
(`closing_stock_period_id_item_id_key` makes it unique per period), and pinned by a test that was
verified to fail against the unfixed code.

**The admin all-clients read was the same shape, one level up.** Every period of every client,
`select('*')`, ordered `bs_year desc, bs_month desc` — rows are clients × months, so 30 clients
over three years is already past the cap. Truncation drops the OLDEST rows, which means the client
it silently erases is **the dormant one whose open period is months behind** — precisely what
`needsAttention` and the amber "N need attention" count exist to surface. It would have rendered as
`NO PERIOD` with a `+ Create Period` button, and every Total in the table short.

**`adminCreatePeriod`'s reactivate branch was a bare `await` with nothing destructured** — the
insert branch surfaced its error and the update branch did not, so a refusal there was a click, a
"Working…", a reload, and a row that had not changed. Both branches now say what state the client
is in and what it costs them.

**The Owner's month-end button ran a ten-second preflight with no feedback.** Both preflights are
bounded at 10s by `withTimeout` and they run *before* the dialog appears; the admin paths show
"Working…" through it, the client's "End Bhadra & Start Ashwin →" showed nothing. A `closeBusy`
flag now covers the preflight and the commit on both client-view close buttons. The double-click
risk turned out to be benign and was left alone — `saveGeneratedReport` is insert-only with 23505
swallowed, so a repeat close cannot overwrite a frozen snapshot.

**And the notice outlived its action.** `setNotice(null)` ran at the top of `reopenPeriod` and
nowhere else, but a close, a resync or a create that SUCCEEDS writes no notice at all — so the red
`ActionError` from the failed attempt before it stayed on screen and read as the verdict on the
action that had just worked.

Smaller, from the same read: the three edit/create paths S682's alert sweep did not reach still
printed raw `error.message` (now `errorLine`, sentence first and `code · message` as fine print,
with `.message` guarded — a fetch that never reached Postgres has none, and `.includes` on
undefined throws inside the very handler meant to report it); `createPeriod` was the one path that
minted a period without validating its year, so a cleared field posted `NaN`; the year bounds were
`2070–2100` typed out in five places, where the ceiling is `BS_YEAR_MAX` and moves; and the
`Created` column was the page's one AD, browser-locale, browser-timezone date, now `nepalBs` +
`formatBsDay` per the S670 rule. The `openCount > 1` banner is unreachable under
`monthly_periods_one_open_per_client` and was kept with a comment saying why rather than deleted.

**Two things deliberately left, recorded here because T5b's `IMS_TODO.md` does not exist yet and
there is nowhere else to put them.** `MenuPricing.js:339` carries the identical conditional-module
guard and cites this page as its precedent — it exposes no write in that state, and its POS-only
and IMS branches are an ask-first file, so it was filed in `access-control.md` rather than swept.
And the nav side of the same finding is still open in the other direction: `/periods` is tagged
`minImsRole: 'supervisor'`, and `hasImsAccess()` is false for a non-IMS client's **Owner** too, so
an HR-only or POS-only Owner has no Periods nav item and can only reach the page by URL — even
though they genuinely have periods, since payroll runs and POS bills are period-scoped and the
expired-month close button is theirs. Pre-existing, unchanged by this session, and a product
decision about their sidebar rather than a defect.

Cross-repo: the count-vs-read asymmetry is filed for hss-suite in `docs/CROSS-REPO.md` — the HR
module was ported, and a payroll run validated on an attendance or payslip count before summing
those rows is the same shape on data that crosses 1000 inside one company-year.

---

### S704 — 2026-09-08 — The Quick Calculator stops being a modal, and can be dragged

**Service worker `crest-v240` → `crest-v241`, with `APP_VERSION` moved to match.** Worth noting how
that second half got remembered: not from the bump itself, but from re-reading `README.md`'s
Conventions section while updating the docs — which is the argument for the pairing being a test
(`appVersion.test.js`) rather than a habit.

Reported from a screenshot: the calculator was open over a Purchases bill list and the page behind
it was unreachable. It was not a stacking bug or a missed `pointer-events` — it was the shape the
component had been built on. `Calculator.js` opened with `Modal.js`'s overlay verbatim: a
`position: fixed; inset: 0` scrim at `rgba(0,0,0,0.6)` covering the whole viewport, with
`onClick={onClose}` on it. Every pixel outside the 380px panel belonged to that div, so the page
underneath could not be scrolled, clicked or typed into, and the dimming was the scrim doing
exactly what a scrim is for.

**Which made the tool prevent the one thing it exists for.** You open Alt+C *to check a figure that
is on the page* — a rate, a bill total, a count — and the scrim both dimmed that figure and, being
the close affordance, dismissed the calculator the moment you clicked it. A scrim is the correct
control for a dialog that owns a decision: it says *finish this before doing anything else*. This
owns nothing. So the backdrop, the click-outside-to-close and the focus trap are gone, and the
panel is a floating one the user positions.

**Drag is `setPointerCapture` on the header, not `mousedown` plus document listeners.** Capture
routes every subsequent move and up back to the handle even when the pointer outruns it or leaves
the window, and one code path covers mouse, touch and pen. `touchAction: 'none'` on the row, or a
touch drag scrolls the page underneath instead of moving the panel. Position survives close and
reopen for the session — you put it where it does not cover what you are reading, and reopening
should not undo that — but is deliberately **not** persisted: a position saved on one monitor is
how a panel gets stranded off-screen on another. `clampPos()` measures the live element rather than
the constants (the panel's height changes with the tape, its width shrinks on a phone) and runs on
open and on resize as well as on every drag frame, so the header is always reachable.

**Two rules already in this repo decided the markup.** The drag row holds the `×` button, and
interactive content inside a `role="button"` container is invalid and unfocusable (S653) — so the
row carries the pointer handlers and the grab cursor, and the keyboard affordance is a separate
focusable grip holding only an icon. That grip takes the arrow keys, because the reason to move
this panel is to uncover the figure underneath it and a drag-only affordance puts that out of a
keyboard user's reach entirely (WCAG 2.5.7). `:focus-visible` has no inline form, so the grip is
`.calc-drag-grip` in `Layout.css` with the product's `--theme-focus-ring` + `--theme-focus-outline`
pair, rather than the UA outline an inline-styled element would have fallen back to.

`zIndex` went 400 → **2500**: above every `Modal` (2100 is the highest any caller passes) and below
`Tip`'s 9999. The purchase-bill form is exactly where someone reaches for Alt+C, and a floating
tool a dialog can bury is a tool you cannot use where you most need it. The panel also picked up
`no-print` — without a scrim it is far easier to leave one open over a report someone then prints —
and `role="dialog"` **without** `aria-modal`, since nothing behind it is inert and claiming
otherwise would tell a screen reader the rest of the page is unavailable when it is exactly as
available as before.

The capture-phase Escape handler stays and its long comment stays with it. It was written so the
calculator could sit inside a `Modal` without Escape closing the modal first; now that the two can
genuinely be open together it matters more, not less.

**Docs.** The general rule went to `.claude/rules/design-system.md` as **"A scrim is a control, and
it says finish this before anything else"** — ask what an overlay OWNS before giving it one, plus
the four things a non-modal floating panel needs that `Modal` supplies for free and the three rules
a drag needs. `component-library.md`'s `Calculator` row no longer calls it a modal.
`input-arithmetic.md` — the file that auto-loads when you open `Calculator.js` — got a pointer at
both, since it covers the evaluator and says nothing about the shell. Help → *Quick Calculator &
Inline Math* and the IMS module guide's Purchases entry now say the page underneath stays live and
that the panel can be dragged; the old Help copy already claimed it "floats over any page", which
was true of its position and false of everything else.

---

### S703 — 2026-09-08 — The billing export stops pricing Suite as a bundle, and the copy gets a guard

**No service worker bump (no app code changed). `billing-export` has no deploy step — which is the
whole reason it drifted — so it was pasted into the dashboard by hand and deployed the same day.
Smoke-tested live afterwards: `POST` with no secret returns `401 Invalid secret`, `GET` returns
`405 POST only`. A boot error would have returned neither, and a missing `HSS_CREST_SYNC_SECRET`
would have returned `500`.**

**Then the sister app's sync confirmed the arithmetic, which is the only place it could be
confirmed.** CASA ACAI CAFE moved 6,500 → **7,900** (`3,500 IMS Pro + 2,400 HR + 2,000 Suite`) and
BHATTI CHOILA 6,500 → **10,000** (`3,500 + 2,400 HR + 2,100 POS + 2,000 Suite`); the two untouched
rows stayed untouched, Longtail e-media at 7,100 and a switched-off client at 0. Both new figures
carry the admin's EDITED HR and POS prices, and both take Suite at the shipped 2,000 because the
platform row has no `suite` key yet — so this is `resolvePricing`'s per-field fallback working on
production data, not only in the tests.

Worth stating what the old number actually was: **one flat 6,500 on two clients with different
subscriptions.** Casa Acai was 1,400 short and Bhatti 3,500 short, by the same wrong figure — which
is what a bundle price does when the thing it bundles stopped being a bundle. A single value
appearing on two rows that should differ is the cheapest tell there is, and it sat on that screen
for months.

`supabase/functions/billing-export/index.ts` is a deliberate second copy of `clientMrr.js`: it is
pasted into the dashboard editor, so it cannot import anything from the bundle. It held a
`SUITE_BUNDLES` table — starter 5,300 / growth 5,800 / pro 6,500 — and `computeBilling` returned
early on it, **ignoring IMS, HR and POS entirely**. That is the pricing model this product retired
in S552, when Crest Suite Pro became an add-on bought per outlet on top of the modules rather than a
bundle containing them. The app side moved and the copy did not.

**So the two repos disagreed outright on every Suite client.** A Growth client on all three modules
plus Suite is 8,700 in Crest and synced as 6,500 — and hss-suite bills off this payload. S701 made
every other price live and gave Suite a `plan_prices.suite` key this function did not read, which is
what surfaced it.

Suite is additive now, priced from `plan_prices.suite` with the shipped 2,000 as the fallback, and
`pricing_basis` no longer emits `'suite_bundle'`. **Checked before changing the value rather than
after**: hss-suite prints `pricing_basis` in one detail panel (`CrestClients.jsx:305`) and never
branches on it, and the column is plain `text` with no CHECK — so the string simply stops appearing.

**`next_renewal_at` was wrong for the same reason and was easy to miss.** It took Suite's date ALONE
whenever Suite was on, which is right only while Suite replaces the modules — so a Suite client whose
IMS expired next month reported the Suite date and the earlier renewal went unseen. Every active
window is a candidate now. **When a model changes, every derived field computed under the old one
changes with it**; the money was the reported symptom and the date was sitting right beside it.

**The real fix is that the copy is now asserted, not trusted.** A comment saying "mirror any change
here by hand" is not a mechanism — it is the thing that failed. `clientMrr.test.js` reads the Edge
Function's source (the technique `nepalMoney.test.js` already uses) and asserts no `SUITE_BUNDLES`,
every price read from `planPrices?.*`, and each hand-copied fallback equal to `DEFAULT_PLAN_PRICES`
— so repricing in `pricingPlans.js` fails in the suite instead of going unnoticed for months.
Verified by perturbation: changing the function's Suite fallback to 2,500 fails the assertion.
Full-line comments are stripped before matching, so the prose *about* the retired bundle does not
trip the assertions *against* it.

Filed on the hss-suite side of `docs/CROSS-REPO.md`: every Suite client's `monthly_amount` moves
upward on the next sync and `next_renewal_at` may come earlier. Nothing over there has to change to
accept the new payload.

**Then a screenshot of that screen settled it, and corrected this entry.** It was written claiming
anything already invoiced off the old figure was understated — a commercial question to go and ask.
It is not: all four synced clients sit in `Unlinked (4)`, and both `crestBillable` and Send to
Finance refuse without a `customer_id`, so no `revenue_ledger` row can exist. The understatement is
**prospective only** and there is nothing to correct. Worth naming the habit: the consequence of a
pricing bug is a question about the other system's STATE, and it is cheaper to look than to warn.

The same screen is the proof this fix was needed and the proof S701 works, in one frame. BHATTI
CHOILA and CASA ACAI CAFE both read **Rs. 6,500.00** — the retired `pro` bundle price exactly —
while Longtail e-media reads **Rs. 7,100.00**, which is `2,600 + 2,400 + 2,100`: IMS Growth plus the
admin's EDITED HR and POS prices. The function was already carrying live `plan_prices` through to
the sister app for the modules; Suite alone was frozen.

And one number on it belongs to an older row: that screen's *Crest Suite MRR* tile said **Rs.
13,000.00**, of which **6,500 is the dummy BHATTI CHOILA client** the ledger has had open since
2026-09-02. Half the platform revenue that screen reports is fictional, and it sits in the headline
KPI rather than a stray row — the row was filed as a tidy-up and reads as one until you see where
the number lands.

---

### S702 — 2026-09-08 — Plan Pricing shows monthly, annual and ARR at once, three modules to a row

**Service worker `crest-v239` → `crest-v240`. No migration, no Edge Function deploy.**

Rearranged from a hand-drawn layout, and every item on it was a real complaint about the screen
S701 had just built.

**Monthly and annual were two tabs, for a figure that is not a second price.** Annual is
`annualOf(monthly)` — one expression, ×0.75 — so putting it behind a toggle made a derived number
somewhere you have to go and look, and the Annual tab was a grid of read-only boxes with the Save
button disabled under it. Both are on screen together now: the input is the monthly price and the
annual rate sits under it, with the **ARR beside it** — the yearly figure is what an operator is
actually deciding with and it used to be a multiplication done in the head. Only Monthly is
editable, which is still what stops the two from drifting apart. `pricingCycle` is gone.

**HR, POS and Suite were three full-width cards holding one field each**, so a tab with four
numbers on it stood a screen and a half tall. One `form-grid-3` row now — the same class the IMS
tiers use, which collapses to a single column at 768px on its own.

**The header card's paragraph became a `Tip`.** Six lines of explanation above the fields it
explains is a paragraph nobody reads twice; the facts that matter (it is the platform's list, not
the viewed client's; it reaches the public page, Help, the module picker and every MRR figure;
annual is calculated) are on hover over the heading, with the Save button in the space the prose
had. The Save stays on this card rather than the page header for S684's reason — the page-level
button writes the viewed client's row — and now that the tab fits one screen, everything it commits
is visible with it.

**Three fields shared one accessible name.** HR, POS and Suite each labelled their input "Monthly
price", so a screen reader announced three identical controls. Each input takes an `aria-label`
naming its module and still containing the visible text, which is what WCAG 2.5.3 asks for. The
card headings also moved from `MODULE_COLORS` to `MODULE_INK` — a signal colour used as TEXT takes
the ink variant, and these had been using the fill token since before that rule existed.

**`Settings.planPricing.test.jsx`** is the first component test for this page: it asserts the tab
calls `savePlatformPlanPrices` and never `saveSettings` (S701's defect, which no unit test could
have caught), that the seed falls back per field over the live row's dead legacy keys, and that
both derived lines re-render as a price is typed.

Two traps worth recording from writing it. A `jest.mock` factory may only close over variables
whose names begin with `mock` — and the rename that fixes that will happily rename
`DEFAULT_PLAN_PRICES` too. More useful: **a context mock returning a fresh object literal per call
hangs the suite rather than failing it.** Settings reseeds local state from `settings` and
`recipeCategories` in effects keyed on those objects, so a new identity every render is an infinite
loop; in the app both are React state and stable, so only a mock can get this wrong. Hoist every
mocked object to a module constant.

Same rule as ever for the price field itself: `priceField` is a plain function returning JSX, not a
component declared inside render — a component declared there is a new type on every keystroke, so
React would unmount the input and the field would lose focus after one digit.

---

### S701 — 2026-09-08 — A price changed in Settings now reaches the page a buyer reads

**Service worker `crest-v238` → `crest-v239`. No migration, no Edge Function deploy.**

Reported from a screenshot: Settings → Plan Pricing said Crest HR 2,400 and Crest POS 2,100, and
`/pricing` said 2,600 and 2,000. Both were telling the truth about different things. The admin tab
writes `settings.plan_prices`; the public page, the Help page's Plan & Pricing tab and the admin's
own module picker each did `import { HR_PRICING } from '../data/pricingPlans'`. **The override
reached every internal revenue figure and nothing a customer could see** — which is the wrong half
to get right. The live platform row had carried 2,400/2,100 since the day it was typed.

**Everything that prints a price now resolves it first.** `resolvePricing(planPrices)` in
`pricingPlans.js` returns the four sellable things — the three IMS tiers, HR, POS, Suite — with the
admin's figures laid over the shipped ones. The constants are still the fallback and still hold the
feature lists; they are simply no longer what anyone renders. Two properties it needed:

- **It falls back per FIELD.** The live row has no `ims` key at all (it still carries the JSONB
  column's original DEFAULT — flat `starter`/`growth`/`pro` at 5,000/8,000/12,000, dead since IMS
  tiers moved under `ims`). An all-or-nothing fallback would have thrown away the HR and POS prices
  that ARE set, or resolved the tiers to undefined.
- **0 survives.** A module priced at 0 is a real configuration — `clientMrr.js` deliberately lists a
  zero-priced live module rather than dropping it — so the test is "is this a number", not
  `|| fallback`, which would quietly restore 2,000 over a deliberate zero.

**Annual is one expression now.** `annualOf()` (monthly × 0.75, rounded) replaces the copy inside
Settings and the published `annual` field on every constant; a test asserts each of those fields
already equalled the derivation, which is why no default-priced figure moved. Live check against
the real database, signed out: HR 2,400/mo and 1,800 annual, POS 2,100 and 1,575 — the annual
figures could only be right if they were re-derived, since the constants say 1,950 and 1,500.

**Three more surfaces were reading the wrong row, not just the wrong file.** `settings` is whichever
row the CURRENT session read, and a client session reads its own, where `plan_prices` is null. So
Admin → Clients' per-client MRR, the Admin Dashboard's platform MRR/ARR **and the Subscription
Agreement an Owner signs** all silently reverted to the shipped prices the moment a client was
selected. `SettingsContext` now reads the platform row's prices alongside the platform support
contact it already fetched, and exposes `planPrices` (the raw table) and `pricing` (resolved). One
extra column on a read that was already happening.

**The tab could also save into a row nothing reads.** Plan Pricing edited `form` and committed
through the page-level `save()`, which targets the viewed client — so an admin who had a client
selected changed the price, got "✓ Saved", and moved nothing. It saves through
`savePlatformPlanPrices()` to the `client_id`-NULL row now, with its own button beside its own
fields (S684's rule), and the page-level button no longer renders on that tab. `saveSettings()`
strips `plan_prices` from its payload for the mirror-image reason: **a column with a dedicated
writer must not also ride along in the general one**, or saving any other tab after a price change
puts the stale table back.

**Crest Suite Pro is priced in the tab now.** It was the one sellable thing with no override at
all, so the pricing page could track three of its four cards and printed a stale figure on the
fourth. It reads from `plan_prices.suite`, discounts annually like every other line, and a client
already on the default price bills exactly what it billed before — the published 2,000/1,500 pair
was already the 25% derivation.

**A price that is not known yet is a shimmer, not a default figure.** The numbers arrive with the
platform row a beat after first paint, so rendering the shipped price and swapping it when the read
lands would let a visitor read one price and act on another, on a page headlined "Simple, honest
pricing". The `.skeleton` placeholder is em-width so the swap does not move the layout.

Not touched, and worth knowing: `supabase/functions/billing-export/index.ts` keeps its own ported
copy of this arithmetic (it is pasted into the dashboard editor, so it cannot import), and its
`SUITE_BUNDLES` table still models Suite as a bundle that REPLACES the per-module sum — the pricing
model from before S552. It reads `plan_prices` for IMS/HR/POS, so those follow; Suite does not.

---

### S700 — 2026-09-08 — The dashboards get one vertical rhythm, and the KPI card stops being four cards

**Service worker `crest-v236` → `crest-v238`. No migration, no Edge Function deploy.**

Started from a screenshot: the weekday initials under Daily Purchases vs Sales were cut off. They
were not cramped — Recharts places tick text at `axisY + tickSize(6) + tickMargin(2)` even with
`tickLine={false}`, so in a 160px card with a 26px axis band the second line's baseline landed at
**162px inside a 160px SVG** and the SVG's own edge clipped it. Band to 34, card to 184.

That prompted a spacing analysis of all five dashboards, which found **14 distinct vertical
intervals** against a documented 4/8/16/24 scale — and four real defects.

**Group Console and Owner Dashboard butted shadowed cards edge to edge.** `.stat-grid` is `gap: 0`
because `.stat-card` collapses each pair of adjacent borders into ONE drawn line, via `-1px`
margins, with its shadow dropped to do it. GroupDashboard put `.card` in that grid and
OwnerDashboard put a hand-rolled box in it — neither carries the seam pull or the shadow reset, so
thirteen KPI cells rendered 2px double rules with their shadows overlapping at every join. Exactly
the "two depth models arguing" the class comment warns about, in the two files that had not read it.

**"Show 6 more" sat flush against the chart row.** The disclosure toggle had `marginTop: 10` and
nothing below it, and `imsChartsAndTables` opens with a row carrying `marginBottom` and no
`marginTop` — so the gap under a control was **0 collapsed and 14 expanded**. The space under a
control must not depend on what the control says.

**The three module columns did not start on the same line.** HR carried `marginTop: showIms ? 6 : 0`
and POS the same — written when the module blocks STACKED. Once S438/S439 made them grid columns
that 6px stopped being a separator and became a column OFFSET, so Inventory's heading sat 6px above
Human Resources' and Point of Sale's. On the phone the same margins compounded the other way:
14 + the grid's own 16px row-gap + 6 = **36px** between modules against 14 everywhere else.

**Sections did not read as sections.** ClientDashboard put 10px between a module heading and its
own content and 14px between one section and the next — a **1.4x** contrast. HrDashboard states the
right target in a comment and hits it: 8px label→content, 28px group→group, 3.5x. Below that ratio
the heading floats free of the block it names. `.dash-section` (28) and `.dash-row` (16) are the
pair now, both self-zeroing on `:last-child` — which is what makes them safe to apply
unconditionally, since dashboard JSX is conditional almost everywhere and which block renders last
depends on which modules a client bought. They step to 16/8 under 768px, where `.main-content`
drops 32 → 16 and a 28px group gap would separate sections by nearly twice the page's own margin.

**The Admin Dashboard shifted layout on load**: its skeleton grid was `minmax(190px)` / `gap: 14` /
`alignItems: start` against the real strip's `158` / `8` / `stretch`. A skeleton that does not match
its own content is a layout shift with extra steps.

**And the KPI card was four cards.** ClientDashboard, OwnerDashboard and AdminDashboardOverview had
each typed `.stat-card`'s five box properties out inline and then landed on their own padding —
`10px 14px`, `14px 16px`, `12px 14px` against the class's `20px`. `stat-card--compact` is the dense
tier as a real class (`8px 16px`, no seam pull, shadow kept, for a strip that has a real gap), and
`card--compact` (16px) replaces the six inline `.card` padding overrides, ChartCard's included. The
declared 8px gutters in `.stat-grid--compact` had also been rendering as **7px**, because
`.stat-card`'s seam pull is meaningless once there is space between cells.

Everything else was normalisation: 14 distinct vertical values down to 4/6/8/16/28 across the five
files, with `.btn`'s own `8px 16px` and 6px icon gap taken instead of five hand-rolled button
paddings. Two values were deliberately left alone — the adoption pills' measured `gap: 3` (S642
sized that row against the grid floor; changing it re-opens a wrap that was measured) and the
`4px 6px` / `-4px -6px` hit-area pair, whose halves must match.

The cascade was verified by measurement rather than by reading: `.stat-grid dash-section` resolves
to 28 at 1440 and 16 at 390, `.dash-row` inside a `.stat-grid--compact` resolves to 16 (the later
declaration wins the tie, which is why `.dash-section`/`.dash-row` are declared after both grids),
and both zero on `:last-child` at each width. The hover transition the three `kpiCard()`s were each
declaring inline moved onto `.interactive-card`, where the border it animates lives.

---

# Session log — S500–S524

25 entries, 2026-08-03 to 2026-08-05. Newest first, as in the original `README.md`.
Full range list: [CHANGELOG/README.md](README.md).

---

### S524 — 2026-08-05 — Modal focus-steal bug fix; `/impeccable colorize` pass on IMS

**Modal focus-trap bug (affected all 19+ create/edit modals app-wide, not just one page).** User reported "can't edit, type" in the Vendors "Edit Vendor" modal — typing in any field but the first kept getting interrupted. Root cause was in the shared `Modal.js`, not `Vendors.js`: its focus-trap `useEffect` depended on `[onClose]`, but every caller passes a fresh inline arrow function (`onClose={() => setShowForm(false)}`) on every render. Each keystroke in a controlled form field triggers a parent re-render → new `onClose` reference → effect re-runs → cleanup yanks focus back to the button that opened the modal → effect re-fires and refocuses the panel's *first* focusable field, regardless of which field the user was actually typing in. Fixed by tracking the latest `onClose` in a ref and making the effect mount-only (`[]` deps), so it no longer re-arms on every parent re-render. Confirmed fixed live.

**`/impeccable colorize` pass on the IMS module.** The module was already extensively color-coded (55/61 files reference the semantic tokens), so this was an audit for genuine gaps against DESIGN.md's own rules rather than a from-scratch color pass:

- **Warning states wrongly rendered in brand gold (`.badge-yellow`/`var(--theme-accent)`) instead of amber**, diluting the "needs-attention" signal DESIGN.md already reserves for amber: `Variance.js`'s "Under" variance badge + variance-direction text/number color + legend text, `StockReport.js`'s "Low" stock badge + two matching numeric-cell colors, `Requisitions.js`'s "Draft" status badge (2 call sites), `FifoReport.js`'s "Expiring soon" badge/color.
- **A second brand color riding on accent gold instead of the rationed purple slot** (a real One Accent Rule violation): `PaymentReport.js`'s FonePay legend/chart color, and the non-Cash/Credit payment-method badge in `Purchases.js` + `ReturnsTab.jsx`. Added a `.badge-purple` class to `Layout.css` for this (formalizes a pattern `AuditLog.js` already used inline).
- **A real cross-theme bug in `MenuEngineering.js`**: its scatter/bar charts hardcoded Dark-preset-only grid/axis hex (`#2d3240`, `#6b7280`, `#4b5563`, `#9ca3af`), effectively invisible against the five light presets — switched to the `useTheme()`-resolved `colors` object, matching how `PeriodComparison.js` (same directory) already does it. Also fixed a Recharts tooltip cursor fill (`rgba(255,255,255,0.04)`) to `colors.tableHover`, DESIGN.md's own named trap for exactly this pattern.
- **A 6th occurrence of the tracked `#60a5fa` undocumented-indigo bug** (see DESIGN.md Named Rules, first found S521): `MenuEngineering.js`'s `Q_HEX.Plowhorse` scatter-dot color was `#60a5fa` (blue) while the same quadrant's legend/badge already correctly used `var(--theme-purple)` — the dot and its own legend swatch disagreed. Fixed to `#a78bfa` (theme-purple's actual hex).
- Minor consistency fix: `DeadStock.js`'s category cell was plain text where every sibling stockcount/variance table wraps it in the category badge.
- DESIGN.md updated in place with two new notes (the badge-yellow/badge-amber distinction, and the 6th `#60a5fa` recurrence) — matches this project's existing habit of recording these incidents inline rather than just fixing silently.

**Follow-up UX request:** Menu Engineering's "Plowhorse" quadrant icon changed from a plain black square (`⬛`) to `🐴` (horse emoji) — matches the icon convention used in menu-engineering matrix visualizations elsewhere online; the category is literally named after a plow horse.

Production build verified clean (`CI=true npm run build`, exit 0) after all edits.

**Files:** `src/components/Modal.js`, `src/components/Layout.css`, `src/modules/ims/variance/Variance.js`, `src/modules/ims/stockcount/StockReport.js`, `src/modules/ims/stockcount/DeadStock.js`, `src/modules/ims/sales/Requisitions.js`, `src/modules/ims/reports/FifoReport.js`, `src/modules/ims/reports/PaymentReport.js`, `src/modules/ims/purchases/Purchases.js`, `src/modules/ims/purchases/ReturnsTab.jsx`, `src/modules/ims/recipes/MenuEngineering.js`, `DESIGN.md`

---

### S523 — 2026-08-05 — Category filter on Best & Worst Sellers; full-app `/impeccable layout` audit; two more `#60a5fa`/`#818cf8` recurrences fixed

Started from a small ask (`BestSellers.js` had no way to narrow the Top/Bottom 10 tables to one recipe category) and grew into a full-app pass once the layout fix on that one filter row surfaced a repeatable pattern worth checking everywhere. Category filter itself: a plain `<select>` populated from the categories actually present in the selected period's sales data (not the client's full category list, so an unused category never shows), defaulting to "All Categories," resetting on period change, filtering the chart/top10/bottom10/summary strip together. Verified live against a real client's Shrawan 2083 data (74 items, Food/Beverage/Other categories) via Playwright, read-only — no data mutated.

**Full-app `/impeccable layout` audit** — user explicitly asked for the thorough option after a mechanical `detect.mjs` scan came back clean app-wide, so this covered all ~81 remaining route-level pages across IMS/HR/POS/Admin in 14 batches (parallel background agents per batch where practical; three wave-3 agents hit the account's monthly spend limit mid-run and were finished directly instead). One real, repeatable structural issue turned up on roughly a third of pages: a filter/action row where scope filters, action buttons, and a passive trailing status/count readout all sat on one flat, undifferentiated `gap` value, reading as equally-weighted controls with no grouping. Fixed the same way everywhere it appeared - same-kind controls grouped tightly (6-8px), different-kind groups separated by a looser gap (16-20px), passive trailing text pushed to the far edge via `marginLeft:'auto'` instead of trailing inline. ~20 pages got this fix (`Stock.js`, `Purchases.js`, `Roster.jsx`, `PayrollRun.jsx`, `OutstandingPayables.js`, `MonthlyOwnerReport.jsx`, `AuditLog.js`, and others); the other ~60 pages were already well-grouped and were deliberately left untouched, including `ClientDashboard.jsx` and `PosOrders.jsx` which got an extra-conservative pass given how much already-tuned, deliberately-documented structure they carry. No business logic, data-fetching, or colors changed anywhere in this pass - pure spacing/grouping, every touched file re-verified clean against `detect.mjs`.

**Two more recurrences of the `#60a5fa`/`#818cf8` undocumented-accent bug** (see DESIGN.md's Named Rules section - this is now the 4th and 5th occurrence of the identical pattern, first found S521) surfaced incidentally during the layout pass and were fixed in place, not left for later: `AdminDashboardOverview.jsx`'s IMS/HR/POS module-count pills (both the KPI card and the table-row copy) hardcoded `#60a5fa`/`#a78bfa` for IMS/POS instead of theme tokens; `HolidayCalendar.jsx`'s "Optional Holidays" stat hardcoded `#818cf8` sitting directly next to "Public Holidays," which already correctly used `var(--theme-accent)`. Both fixed to the same IMS=accent/HR=green/POS=purple mapping already established in `AdminClients.js`'s module pills.

**Files:** `src/modules/ims/reports/BestSellers.js`, plus ~26 more page files across IMS/HR/POS/Admin for the layout grouping fix, and `src/pages/dashboard/AdminDashboardOverview.jsx` + `src/modules/hr/holidays/HolidayCalendar.jsx` for the color fix - see `DESIGN.md`'s Named Rules section for the color-bug history and `CLAUDE.md` is unchanged this session (no new architectural pattern, pure UI maintenance).

---

### S522 — 2026-08-05 — `/impeccable optimize`: defer the 138kB xlsx library to click-time on all 37 pages that had it

Measured before touching anything: a production build's gzip size report showed `xlsx` (SheetJS) as a single 138 kB shared chunk — the largest chunk in the entire app after `main.js` itself, larger even than `recharts` (102 kB). A grep found the cause: 37 files did `import * as XLSX from 'xlsx'` at the top of the module, so every one of those already-lazy route chunks (Stock, Purchases, PayrollRun, SalesReport, AuditLog, and 32 others) forced a parallel fetch of that 138 kB chunk on page load — merely visiting the page, not clicking its Export/Import button, paid the cost. `recharts` was deliberately left alone: it's core above-the-fold chart content on the pages that use it, not a deferred action, so eager-loading it is correct.

Fixed by making each export/import handler `async` and adding `const XLSX = await import('xlsx')` as its first line, so the chunk now only loads the moment a user actually clicks Export or Import. Two files needed a different shape: `SalesReport.jsx`/`CoversReport.jsx` share a `withLetterhead()` helper across every tab's export branch, so `XLSX` became its first parameter instead of a closed-over module import; `MonthlyOwnerReport.jsx`'s dedicated `monthlyReportExcel.js` helper uses `XLSX` throughout, so the page now dynamically imports that whole module at click time rather than touching every function inside it. Verified in the actual built output, not just assumed: the compiled chunk for a fixed page now shows `r.e(1238).then(r.bind(r,1238))` (webpack's lazy-chunk-fetch) sitting inside the button's `onClick`, where it used to be evaluated at module load.

One unrelated finding surfaced along the way and was fixed in place: the design-detector hook flagged `AuditLog.js`'s `ACTION_STYLE.UPDATE` badge for the same undocumented `#60a5fa` blue that S521 had just fixed in two other files — a third recurrence, fixed to `var(--theme-purple)` (a genuine 4th-category case here, since Added/Deleted already own green/red).

**Files:** 37 files across `src/modules/hr/`, `src/modules/ims/`, `src/modules/pos/`, `src/pages/AuditLog.js`, plus `src/pages/dashboard/MonthlyOwnerReport.jsx` — see `CLAUDE.md`'s Stack section for the full list and the two non-standard cases.

---

### S521 — 2026-08-05 — Full `/impeccable audit` pass: fixed all findings (a11y, theming, dead code)

A whole-project (not single-page) `/impeccable audit` — Accessibility 2/4, Performance 3/4, Responsive 3/4, Theming 2/4, Implementation Integrity 3/4, 13/20 overall — followed by fixing every finding. Full findings list and reasoning were reported inline; summary of what shipped:

**Accessibility — `Modal.js` and `SearchableSelect.js`, the app's two most-reused interactive primitives, had zero ARIA semantics.** `Modal.js` (backs 19+ create/edit flows: `Vendors.js`, `Items.js`, `Purchases.js`, `PurchaseBillModal.jsx`, `AssetFormModal.jsx`, etc.) now has `role="dialog"`/`aria-modal`/`aria-labelledby`, moves focus into the panel on open, traps Tab within it, restores focus to the trigger on close, and gives the `×` button an `aria-label="Close"` (previously its only accessible name was the literal glyph text). `SearchableSelect.js` (the documented drop-in for long `<select>` lists) now carries the standard combobox/listbox ARIA pattern (`role="combobox"` + `aria-expanded`/`aria-controls`/`aria-autocomplete`/`aria-activedescendant` on the filter input, `role="listbox"`/`role="option"`/`aria-selected` on the panel) — it was always keyboard-operable, just invisible to screen readers before this.

**Theming — `AdminClients.js` had drifted onto three of DESIGN.md's own named rules,** undetected because the design-detector hook only runs on files as they're edited and this one hadn't been touched since before the relevant rules were written. Its Trial Accounts panel used a `linear-gradient()` header (the one confirmed instance in the codebase of the no-gradient Cards rule being broken) and solid `background`/`color:'#fff'` fills on its count pill and "Wants to Subscribe" badge (fixed to the standard `.badge`/`.badge-red` alpha-tint pattern); its "Annual" badge hardcoded `color:'#000'` on `var(--theme-accent)` — the exact accent-text-pairing bug the codebase already shipped and fixed once (S424), recurring; and its module pills / "Features" button carried a second, undocumented accent color (`#60a5fa`/`#818cf8`), independently duplicated in `SuiteGate.js`'s upsell card. All fixed to theme tokens in place — same layout, same components, no rework. See DESIGN.md's Named Rules and Cards/Badges sections for the full before/after reasoning.

**Dead code — `shared/constants/shiftTypes.js` deleted.** Looked at first glance like a second, drifted copy of `Roster.jsx`'s own shift-color constant (their "Night" colors disagreed), but a grep for the actual export names (`SHIFT_TYPES`/`SHIFT_BY_CODE`, not the substring `shiftTypes`) found zero real importers anywhere — every apparent "consumer" was a same-named local variable or DB-query result, not an import. Deleted rather than reconciled, since there was nothing live to reconcile against.

**Verified-as-false-positive, documented instead of changed:** the two layout-property CSS transitions in `Layout.css` (already a named, reasoned-through exception for the sidebar's `position:fixed` collapse animation); `ImsGuideTab.jsx`'s off-scale border-radius/grayscale (a fully standalone print-window HTML string with no access to the app's CSS custom properties at all, same structural reason the print-color-ramp already gets a blanket exception — now named alongside it in DESIGN.md); the Courier New monospace font on 7 thermal-receipt print templates (functionally correct, was just missing a DESIGN.md entry, now added as `print-monospace`).

**Files:** `src/components/Modal.js`, `src/components/SearchableSelect.js`, `src/components/SuiteGate.js`, `src/pages/AdminClients.js`, `src/shared/constants/shiftTypes.js` (deleted), `src/shared/constants/index.js`, `DESIGN.md`, `CLAUDE.md`

---

### S520 — 2026-08-04 — Non-operational pages audit: 6 gaps closed across auth, admin, and guest menu

Follow-up to a research report comparing every "non-operational" page (public/auth, dashboards, admin/settings, docs — the account/config/marketing pages, not the daily transactional workflow) against industry references. Two larger items from that report were deliberately deferred after asking: a full guest-menu language switcher (would need new per-item translation storage and a client-facing data-entry UI — real schema/scope decision) and proactive push alerts on Client Dashboard (needs new always-on scheduled infrastructure — a separate, focused task). The rest shipped this session:

**Login: Forgot Password flow** (`src/pages/Login.js`, new `src/pages/ResetPassword.js`, new `/reset-password` route in `App.js`) — a locked-out owner previously had no self-service recovery at all. Uses Supabase Auth's native `resetPasswordForEmail()`/`updateUser()` — no new backend. The request form always shows the same "if an account exists…" confirmation regardless of outcome, matching the sign-in form's existing generic-error convention (Supabase itself already avoids leaking whether an email is registered, so this doesn't reopen that hole). `ResetPassword.js` waits for the `PASSWORD_RECOVERY` auth event (fired once supabase-js parses the recovery token in the email link's URL hash) before showing the new-password form.

**Help: search box across all tabs** (`src/pages/Help.js`) — the highest-content-volume page in the app (85 feature entries, 33 glossary terms, 18 FAQs) had no search at all. Required first hoisting the POS and Admin Tools feature arrays — previously anonymous array literals inline inside their `.map()` JSX — into named `POS_FEATURES`/`ADMIN_FEATURES` consts (mirroring the existing `IMS_FEATURE_TIERS`/`HR_FEATURES` pattern) purely so a search index could be built from the same data every tab already renders from, with nothing to fall out of sync. Search results reuse the existing `FeatureCard` component directly rather than a separate render path. Tier-locking is deliberately not applied to search results — showing a locked feature's guide text in a search hit is closer to how Pricing already advertises locked features openly than something that needs gating.

**Guest Menu: veg-only + allergen filters** (`src/modules/pos/guestmenu/GuestMenu.jsx`) — `is_veg` and `allergens` were already captured per item (used only for read-only display before this) and are now a real filter via a ⚙ Filters button/sheet, only shown when at least one dish actually has that data set. Deliberately scoped to Veg/Non-Veg + allergen exclusion, not a language switcher (see above) — Veg/Non-Veg is the primary dietary distinction in this market and was the highest-value, lowest-risk piece of that recommendation.

**Search/filter added to 5 admin/staff tables** — `AdminDashboardOverview.jsx`'s All Properties table, `AdminClients.js`'s client list, and the three near-identical staff-role pages (`ImsStaff.jsx`, `HrStaff.jsx`, `PosStaff.jsx`). All client-side text filters over already-loaded data, no new queries — these only matter once a table has enough rows to need one, which none of Crest's current pilot-scale data does yet, but the gap would have compounded silently as the client base grows.

**Owner Dashboard: Cost & Margin trend chart** (`src/pages/dashboard/OwnerDashboard.jsx`) — the page's four headline percentages (Food Cost %, Labor Cost %, Prime Cost %, Net Margin %) were snapshot-only with no trend. Reads the last 12 closed periods' `monthly_owner_reports.snapshot.combined` directly — the exact same computed shape `computeMonthlyReport.js` already writes and the report page's own Trend section already reads — rather than re-deriving figures live, so the chart can never disagree with what the Monthly Owner Report itself shows for the same periods. Zero new queries beyond the one `monthly_owner_reports` read.

**Pricing: "Most Popular" badge on Suite Bundles** — the IMS 3-tier section already had this (Growth tier); the Suite Bundle section below it didn't, despite being the same 3-tier shape. Added the identical badge treatment to the Bundle's Growth card without touching the existing Pro-tier button-gold styling, which reads as a separate, likely intentional "Pro is the premium option" signal.

**Files:** `src/pages/Login.js`, `src/pages/ResetPassword.js` (new), `src/App.js`, `src/pages/Help.js`, `src/modules/pos/guestmenu/GuestMenu.jsx`, `src/pages/dashboard/AdminDashboardOverview.jsx`, `src/pages/AdminClients.js`, `src/modules/ims/staff/ImsStaff.jsx`, `src/modules/hr/staff/HrStaff.jsx`, `src/modules/pos/staff/PosStaff.jsx`, `src/pages/dashboard/OwnerDashboard.jsx`, `src/pages/Pricing.js`

---

### S519 — 2026-08-04 — Period Comparison: growth/YoY deltas, trend charts, revenue-by-category breakdown

Prompted by a request to check `PeriodComparison.js` (`src/modules/ims/reports/PeriodComparison.js`, Pro, IMS Supervisor+) against industry references (QuickBooks/Xero P&L comparison reports, restaurant food-cost tools MarketMan/Apicbase) before extending it. The gap: the page's only period-over-period delta was FC%'s pp change — Net Purchases and Revenue, the two figures a "sales vs purchase comparison" is actually about, had no growth indicator at all, and the page had zero chart despite `ChartCard` being an established convention on every comparable report page (Dashboard, Menu Engineering, Best Sellers, Covers Report, Sales Report).

**Inline % change lines** — `DeltaRow`/`PpDeltaRow` render a small "↑ 12.3% vs prev" line under the Net Purchases, Revenue, and FC% cells (`pctDelta()`/existing pp-diff logic), colored by whether an increase is good (Revenue: green-up), neutral (Net Purchases: more spend isn't inherently bad), or FC%'s existing down-is-good convention. No new table columns — kept the existing 7-column layout, enriched cell *contents* instead.

**Year-over-year toggle** — "Compare vs last year" checkbox adds an italic "vs LY" delta line to the same three cells, comparing against the same BS month one year back (`findYoy()`, matched against the full unsliced `periods` list so a period outside the current 6/12/24 window is still found). `fetchData()` now always fetches each shown period's YoY twin's stats too (folded into the same bulk `.in('period_id', ids)` queries, no extra round trips) so toggling the checkbox never needs a re-fetch.

**Two new trend charts**, `ChartCard`-wrapped to match every other report page: "Revenue vs Net Purchases" (a `ComposedChart` Area/Line combo, deliberately the same visual language as `ClientDashboard.jsx`'s existing daily Purchases-vs-Sales chart — accent for purchases, green for revenue) and "Food Cost % — Period Trend" (`LineChart` with 30%/38% reference lines matching this page's own `fcColor()` thresholds, not `ClientDashboard`'s different 35%/45% cutoffs — stayed consistent with this page's own established convention rather than copying the other page's numbers). Both show `StatPill` summary rows only in the expanded/modal view, the same big-vs-small split every other `ChartCard` consumer uses.

**Revenue by Category** — a new stacked-bar trend chart reusing the exact `recipes.category` taxonomy and color convention (`Food`=green, `Beverage`=purple, fallback hex rotation) `FoodBeverageSplit.jsx` already established for the Dashboard's Sales Mix pie, excluding `'Sub-Recipe'` rows the same way. No new query — the existing bulk `sales_entries` fetch already joins `recipes`, so this only widened that one select to also pull `category`. Deliberately scoped to revenue only, not purchases — items (raw ingredients) use a separate, unrelated `categories` taxonomy from recipes' Food/Beverage/Dessert/Snack/Other, and there's no existing per-item-to-recipe-category mapping to build a comparable purchases-by-category view from without a much larger new feature (that's what Menu Engineering's per-recipe costing is for).

**New stat cards**: Highest Revenue Period and Highest Purchases Period, alongside the existing Best FC% Period — same "call out the standout period, don't make the reader scan the table" pattern.

**Excel export** gained `Δ% vs Prev` columns for Purchases/Revenue (and the YoY equivalents when the toggle is on) plus a second sheet, "Revenue by Category" — a period × category pivot, not just the main comparison table.

**Files:** `src/modules/ims/reports/PeriodComparison.js`, `src/pages/Help.js`

---

### S518 — 2026-08-04 — Audit Log: generic field-level diff, POS/client/feature-flag coverage, search/export/pagination

Prompted by a screenshot showing several "Updated / [name]" rows with zero detail — traced to `AuditLog.js`'s `getSummary()` being a hand-written `switch` that only diffs the specific fields someone remembered to add per table, so every `profiles` column added since (`pos_team`, `pos_discount_limit`, `pos_allow_void`, `pos_job_title`, `ims_role`, `hr_role`) changed silently with no summary text. Researched industry references (OWASP Logging Cheat Sheet, WorkOS/Okta/Auth0's actor-target-action-context schema, GitHub's org audit log old_value/new_value export) before redesigning.

**Generic field-level diff replaces the per-table switch** — `diffFields()` in `AuditLog.js` compares every key in `old_data`/`new_data` (both already captured in full by the existing `log_audit()` trigger) and renders "Field: old → new" automatically, skipping a shared `IGNORE_KEYS` set (housekeeping timestamps, PIN-lockout counters, `pos_device_secret`). This is the actual fix for the screenshot's blank rows, and it means any future column is covered the moment it exists — no more per-table code to remember. The Summary column (renamed **Details**) shows the first 3 changed fields inline with a "+N more" count; clicking the row expands a Field/Before/After table with every changed field, not just the capped preview.

**New DB coverage** (`supabase/migrations/20260804040000_audit_log_expanded_coverage.sql`) — the entire POS module and `clients`/`feature_flags` were previously unaudited entirely, the highest-risk gap being void/discount/comp abuse with zero trail. Added triggers for `pos_orders` (status/close_type/discount/void/invoice transitions only — a table-specific noise-skip clause in `log_audit()` ignores `covers`/`print_count`/`comp_print_count` so an item edit or bill reprint doesn't fire a row), `pos_credit_notes`, `clients` (plan/module toggles — the Admin Clients page itself, previously invisible), and `feature_flags`. Also fixed two dead entries: `hr_attendance` and `hr_payslips` had labels in the frontend's filter dropdown and help panel since the original build but **no trigger ever existed** for either — the UI was claiming coverage that didn't exist.

**Two noise-skip clauses added to `log_audit()` itself**, not just the frontend — a `profiles` UPDATE where only PIN-lockout counters (`pos_pin_failed_attempts`/`hr_pin_failed_attempts`/`*_locked_until`) or `last_seen_at` changed now returns `NULL` before insert, same idea as the pre-existing `monthly_periods` status-only check. Without this, the new generic diff would have surfaced a real live problem: every failed PIN attempt already fires a `profiles` UPDATE (via `record_pos_pin_attempt`/`record_hr_pin_attempt`), previously invisible only because the old hand-written summary didn't check those fields — the new generic diff would have turned that into visible spam on every mistyped PIN if left unfiltered. `clients` rows also get `pos_device_secret` stripped from both `old_data`/`new_data` before insert — the unguessable per-client secret gating the anonymous `get_pos_staff` RPC (S372) has no reason to ever land in a table, even an admin-only one.

**Tier 3 UX** — User filter and free-text search (client-side over whatever's currently loaded, same 500-row page window the table already worked within), Excel export via `XLSX` (matches the existing SheetJS convention used elsewhere) of the currently filtered rows, and keyset pagination (`Load next 500`, `.lt('created_at', cursor)`) past the previous hard 500-row ceiling.

**Files:** `src/pages/AuditLog.js`, `src/pages/Help.js`, `supabase/migrations/20260804040000_audit_log_expanded_coverage.sql`

**⚠ Migration not yet applied to the live DB** — per this project's manual-apply convention, `20260804040000_audit_log_expanded_coverage.sql` needs to be pasted into the Supabase Dashboard → SQL Editor and run before the new pos_orders/pos_credit_notes/clients/feature_flags/hr_attendance/hr_payslips triggers and the noise-skip clauses take effect. Confirmed via `supabase db push --dry-run` that a real `db push` is not viable here — the remote's migration-history table isn't in sync with local files (this project has always applied migrations by hand), so a real push would attempt to replay all ~85 historical migrations from scratch.

---

### S517 — 2026-08-04 — Per-staff POS Discount Limit + delegated Bill Void; optional negative-stock block and below-cost pricing warning

Prompted by screenshots of a competitor's ("IMS WebPOS") much more granular per-user permission system — analyzed the gap against Crest's coarser 3-tier rank model and shipped four of the highest-value, lowest-risk items; the rest (table merge/split/transfer, a full per-menu-action ACL, multi-branch privilege lists) went to a roadmap instead, since Crest has no multi-branch concept at all and the others are larger, separate undertakings.

**Per-staff Discount Limit** (`profiles.pos_discount_limit`, nullable % — null = unlimited, preserving current behavior for everyone until a manager sets a cap): editable per staff row in POS Staff, enforced in `PosOrders.jsx`'s discount calculation regardless of whether the cashier types a flat ₨ amount or a % (both convert to an effective % of the subtotal for the clamp), with an inline "Capped at your X% limit" note when it kicks in. Admin/Owner logins are always unlimited.

**Delegated Bill Void** (`profiles.pos_allow_void`, boolean, default `false`): Void was previously `isAdmin || isOwner` only — no PIN-based role, regardless of rank, could ever void a bill. A manager can now grant "Allow Void" to a specific trusted staff member in POS Staff so mistakes don't have to wait for the owner. Default-off preserves current behavior for every existing account.

Both new fields had to flow through `get_pos_staff_list()` (profiles' self-or-admin-only RLS means the staff list is read via this `SECURITY DEFINER` RPC, not a raw query) — dropped and recreated with 2 new OUT columns (`CREATE OR REPLACE` can't change return shape, same `42P13` constraint noted in past sessions) and `admin-user-ops`'s `update_pos_role` action, following the same "only touch a field if the caller actually sent it" pattern `pos_team` already used, so PosStaff.jsx's background role-sync `init()` call doesn't silently reset either field on every page load.

**Real bug found and fixed live smoke-testing this against Casa Acai Cafe's actual staff data**: `update_pos_role`'s `updatePayload` unconditionally set `pos_role`/`pos_job_title` (`pos_role || null`) regardless of whether the caller's request actually included them — so `updateTeam`/`updateDiscountLimit`/`updateAllowVoid`, which each call the action with only their own one field, were silently wiping the target staff member's role to "No Access" on every single Team/Discount Limit/Allow Void change. This is the exact mirror image of the bug the function's own comment already guarded against for `pos_team` (S431) — the reverse direction was never protected. Caught immediately: setting Sarita Bishwokarma's discount limit reset her role to "No Access" on next page load, reproducible even via the pre-existing `updateTeam`. Fixed by making `pos_role`/`pos_job_title` conditional on `pos_role !== undefined`/`pos_job_title !== undefined` too, matching the pattern the other three fields already used — verified live afterward (role, discount limit, and void grant all now persist independently through a reload), then reverted the test staff member back to her original state.

**Negative-stock block** (`settings.block_negative_stock`, default `false`) and **below-cost pricing warning** (`settings.warn_below_cost_pricing`, default `true`) — both client-level Settings → Thresholds toggles rather than per-user, since they're business policy, not something that varies staff-to-staff. Stock Count's `saveAll()` already computed and red-highlighted negative `Used` (more used than was ever bought or on hand) but never blocked saving; turning the setting on now hard-blocks for non-admin and prompts a `window.confirm` override for admin. Recipes shows a non-blocking red warning under the selling-price field when it's set below the recipe's live computed cost (reuses `liveCost`/`liveFcPct`, already in scope at that point — no new query) — doesn't block save, since a below-cost price can be intentional (loss-leader).

Two small follow-up fixes caught by user review after the initial ship: (1) the Discount Limit column showed a bare number with no unit — ambiguous whether "15" meant 15% or NPR 15 — fixed by shortening the header to "Discount %" (matching the table's terse single-word style, e.g. "Team") and adding a `%` suffix next to every row's input; (2) `PosStaff.jsx`'s outer container had a leftover `maxWidth: 900` sized for the original 6-column table — the 2 new columns pushed the table's natural width to ~1310px, which now overflowed that fixed cap **regardless of actual screen size** (confirmed via a layout-only repro replicating the real sidebar+padding structure: fits with no scroll at 1280px+ once the cap is widened, only needed the artificial squeeze away). Bumped to `maxWidth: 1350`, matching this codebase's convention of sizing each page's container to its own content (other POS table pages already use 1000–1150).

**Files:** `supabase/migrations/{20260804020000_pos_staff_discount_limit_and_void.sql,20260804030000_stock_and_pricing_guard_settings.sql}`; `supabase/functions/admin-user-ops/index.ts`; `src/modules/pos/staff/PosStaff.jsx`; `src/modules/pos/orders/PosOrders.jsx`; `src/context/{AuthContext.js,SettingsContext.js}`; `src/pages/Settings.js`; `src/modules/ims/stockcount/Stock.js`; `src/modules/ims/recipes/Recipes.js`; `src/pages/Help.js`

### S516 — 2026-08-04 — Audited page indexing/caching + Security Advisor findings; fixed a missing command-palette entry

Two verification passes, no user-facing feature work:

**Code-splitting + page-revisit caching audit**: cross-checked every route-level component in `App.js` against its `lazy()` const (all 93 resolve correctly, only `Layout`/`RootRedirect` are intentionally eager) and confirmed no CSS import sits below the lazy-const block (would fail CI's `import/first`). Checked all 5 pages using `sessionDataCache.js` (`Sales.js`, `Purchases.js`, `Recipes.js`, `dashboardCache.js`, and the new `AssetRegisterTab.js`) against the documented "never cache a save-time baseline a bulk save trusts" rule — all correctly scoped (e.g. `Sales.js` caches only `periods`/`recipes`, never the sales grid; `Stock.js` still has zero caching, exactly as documented). Also confirmed `Ctrl+K` has no separate static search index to maintain — it reads directly off `Layout.js`'s `NAV`/`REPORTS`/`HR_GROUPS`/`POS_GROUPS` arrays.

**Supabase Security Advisor findings reviewed**: went through every `SECURITY DEFINER` function flagged as `anon`/`authenticated`-executable (re-derived the full list from `supabase/migrations/*.sql` directly rather than trust a partial/truncated dashboard export). Zero new gaps — every function is either a zero-arg helper safe by construction, a trigger-only function immune to direct RPC abuse, a genuinely pre-auth function with real internal authorization (device secret, table→client resolution), a `get_my_*`/`submit_my_*` function correctly deriving identity from `auth.uid()`, or a `p_client_id`-taking function with the standard `is_admin() OR own-client` guard. The only two real gaps this class of scan has ever found (`admin_clear_audit_logs`, `get_cooccurrence`) were already fixed in S293.

**Real gap found and fixed**: Admin's **Guest Menu** page (`/admin/guest-menu`) — a real, clickable sidebar link for every admin login — was missing from `paletteItems`, the array that feeds `Ctrl+K` search, so it was reachable but not searchable. It was hand-rolled directly in the admin sidebar block (`renderNavItem({ to: '/admin/guest-menu', ... })`) rather than sourced from `NAV`/`REPORTS`/a `*_GROUPS` array, which is why it fell through the palette's otherwise-automatic `.flatMap()`/spread coverage. Added it to the admin-only block in `paletteItems` alongside Clients and Audit Log.

**Files:** `src/components/Layout.js`

### S515 — 2026-08-04 — Fixed Asset Register hardening, plain-language Tax Pool UX, app-wide tooltip audit, IMS Guide gaps

Follow-up session on S514's Fixed Asset Register, smoke-tested live against production (Casa Acai Cafe) via Playwright: found and fixed a reload-unmount bug in `FixedAssets.js` (`load()` always called `setLoading(true)`, collapsing every child tab's local state on each reload — gated to first-mount-only via a `hasLoadedOnce` ref) and a missing `assets_register.disposal_reason` column (migration `20260804010000_assets_register_disposal_reason.sql`) that 400'd on disposal.

**Admin-only asset delete**: added a genuinely server-enforced (not just UI-hidden) `deleteAsset` action to `admin-user-ops` — deletes the posted depreciation schedule then the asset via the service-role client, which is the same `auth.uid() IS NULL` bypass Danger Zone already relies on to get past the immutability trigger. `AssetRegisterTab.js` only renders the button for `isAdmin`; no client login, including Owner, can reach it even directly.

**Plain-language rework on the Tax Depreciation (IRD) tab**: user flagged that a café owner has no idea what "Pool A" means. Researched UX guidance for domain jargon (plain language + concrete examples over jargon-only tooltips) and reworked `taxPoolConstants.js`'s `POOL_LABELS`/`POOL_EXAMPLES` with F&B-specific examples (e.g. Pool D → "ovens, refrigerators, dishwashers…"), added dynamic per-selection helper text under every pool picker (`TaxPoolTab.js`, `AssetFormModal.jsx`, `AssetCategoryModal.jsx`), and a plain-language intro paragraph explaining what the tab is for and how it differs from the Depreciation Runs tab. Saved as a standing principle (`feedback_plain_language_for_domain_jargon` memory) for the next domain-jargon-heavy feature.

**Tooltip coverage audit**: swept ~30 recently-added files across IMS/HR/POS/dashboard/owner-report for CLAUDE.md's "every non-obvious label needs a `Tip`" rule. Fixed real gaps: `DisposalReportTab.js` had no `Tip` import at all (added tips for NBV at Disposal / Gain-Loss); `DepreciationRunTab.js`/`AssetCard.jsx`/`ValuationReportTab.js` were missing Opening/Closing/plain NBV tips despite sibling columns being tipped; `PayrollCalculation.jsx`'s comparison table had bare `OT`/`SSF`/`TDS`/`TADA` headers with zero tooltips next to a fully-tipped KPI row above it; `PayslipBody.jsx` (a print/self-service document, where hover tooltips don't apply) had SSF/TADA spelled out directly in the label text instead; `SelfServiceHome.jsx`'s TADA tab got a plain-language subtitle; `MonthlyOwnerReport.jsx`'s Comped Bills / Voids-Write-offs / Avg Check-Cover / Days on Hand rows were untipped next to tipped siblings.

**IMS Guide gaps**: Admin Settings → Guides → Crest IMS (`imsGuideData.js`) was missing entries for both S514's Fixed Assets and S513's Vendor Balance Confirmation — added full sections for both (workflow, fields, formulas, gotchas, connections, matching the existing guide's depth). Cross-checked every `App.js` route against the guide's `route:` values afterward — now a 1:1 match, no other gaps.

**Files:** `supabase/migrations/20260804010000_assets_register_disposal_reason.sql`; `src/modules/ims/assets/{FixedAssets.js,AssetRegisterTab.js,DisposalReportTab.js,DepreciationRunTab.js,AssetCard.jsx,ValuationReportTab.js,TaxPoolTab.js,taxPoolConstants.js,AssetFormModal.jsx,AssetCategoryModal.jsx}`; `supabase/functions/admin-user-ops/index.ts`; `src/modules/hr/payroll/{PayrollCalculation.jsx,PayslipBody.jsx}`; `src/modules/hr/selfservice/SelfServiceHome.jsx`; `src/pages/dashboard/MonthlyOwnerReport.jsx`; `src/pages/settings/imsGuideData.js`

### S514 — 2026-08-03 — Fixed Asset Register (Pro IMS): book depreciation + Nepal statutory tax pools

New Pro-tier module (`/fixed-assets`, single tabbed page — Register / Depreciation Runs / Valuation Report / Disposal Report / Tax Depreciation (IRD)) tracking tangible fixed assets (equipment, furniture, vehicles, kitchen machinery) separately from Item Master's stock/inventory system. Four new tables (`assets_categories`, `assets_register`, `assets_depreciation_runs`, `assets_depreciation_schedule`) plus three more for Nepal's statutory side (`assets_tax_pool_runs`, `assets_tax_pool_lines`, `assets_repair_expenses`).

**Gating**: extended the existing ad hoc mechanism (no new plan-tier tables) — `fixed_asset_register` added to `PRO_KEYS`/`feature_flags`/`FeatureAccessModal.js`'s Pro group, same `ModuleGate`+`PremiumGate` composition every other Pro IMS route uses.

**Book depreciation** (`depreciationCompute.js`): straight-line, per-asset, pro-rated by days actually held in a period, never depreciating below salvage value. Preview computes nothing; Post writes via one atomic RPC (`post_asset_depreciation_run`, deliberately not `SECURITY DEFINER` so the tables' own RLS keeps applying, mirroring `save_sales_day`'s precedent) and locks the resulting rows — a real DB trigger (`enforce_asset_schedule_immutable`) blocks any further UPDATE/DELETE, deliberately stricter than `hr_payroll_runs`' app-layer-only finalize/reopen: a correction here is a brand new adjustment run, never an edit to a posted one. Disposal computes and freezes a gain/loss (proceeds vs. NBV at disposal date) on the register row.

**Nepal statutory tax depreciation** (`taxPoolCompute.js`/`taxPoolConstants.js`) is a genuinely separate system, not a variant of the above — Schedule 2 of the Income Tax Act 2058 pools assets into 5 government-defined categories (A: buildings 5%, B: computers/furniture/office equipment 25%, C: vehicles 20%, D: everything else 15%, E: intangibles straight-line) using declining-balance math on each pool's shrinking total, with disposal proceeds reducing the pool directly (no per-asset gain/loss). Verified the exact rates against the Act's own text (Schedule 2 + Section 16) via web research before building this — found and corrected one figure: the Section 16 repair/maintenance cost cap is **5% of a pool's closing depreciation base**, not the 7% some secondary accounting sources (and the original request) stated. Ships with a visible on-screen disclaimer ("verify with your accountant") and the rates as versioned code constants (mirroring `payrollConstants.js`/`tds.js`'s existing convention for Nepal statutory figures that change with the Finance Act), not a client-editable setting. Mid-year acquisitions get a 3-tier proration (full/2-3/1-3 rate) based on which third of the BS fiscal year they fall in, per Schedule 2's own Poush-cutoff rule.

RLS on all 7 new tables: standard same-client policy + 3 of the usual 4 RESTRICTIVE staff-isolation policies, deliberately excluding `no_ims_staff` — since `is_ims_staff()` checks `ims_role IS NOT NULL` regardless of rank, applying it would incorrectly block a manager-rank IMS *staff account* (as opposed to the plain Owner login) from a feature whose own spec has staff/supervisor viewing and only manager posting; mirrors `ims_gate_passes`' precedent, not `monthly_owner_reports`' full 4-of-4.

**Files:** `supabase/migrations/20260803110000_fixed_asset_register.sql`, `20260803120000_fixed_asset_register_feature_flag.sql`, `20260803130000_post_asset_depreciation_run.sql`, `20260803140000_asset_tax_pools.sql`, `20260803150000_post_tax_pool_run.sql`; `src/modules/ims/assets/*` (new); `src/context/AuthContext.js`, `src/context/SettingsContext.js`, `src/pages/adminClients/FeatureAccessModal.js`, `src/components/Layout.js`, `src/App.js`, `src/shared/scopedDb.js`, `supabase/functions/admin-user-ops/index.ts`, `src/pages/Help.js`

### S513 — 2026-08-03 — Vendor Balance Confirmation: Share via WhatsApp button

Added a "Share via WhatsApp" button next to Print — pre-fills a `wa.me` text message (no phone number, so WhatsApp opens its own contact/group picker) with the letter's Opening Balance, Purchases/Payments/Returns (FY), and Closing Balance summary, same convention as Reorder Report's existing WhatsApp share. The printed/PDF letter remains the actual document to sign; this is just a faster way to hand the vendor the headline number.

**Files:** `src/modules/ims/reports/VendorBalanceConfirmation.js`, `src/pages/Help.js`

### S512 — 2026-08-03 — Outstanding Payables: Payment History dates displayed in AD instead of BS

Found live: the user noticed a Payment History row's date read "2026-07-24" instead of a Bikram Sambat date — every other date in the app, including the identical `payable_payments.paid_at` value shown on the Vendor Balance Confirmation letter for the same row, displays as BS ("8 Shrawan 2083"). `paid_at` is stored as a plain AD `date` column (Postgres has no BS type) and every other reader already converts it for display — this page's own Payment History row date, "Settled On" column, and "Last Settlement" stat card were the one place still rendering the raw AD string untouched. Fixed with a small `fmtBsDate()` helper (`adToBs()` + this file's existing `BS_MONTHS`) applied at all three spots; the date actually used for input/storage (the `BsCalendarPicker` in the pay forms) was never affected, since it already displayed BS correctly.

**Files:** `src/modules/ims/reports/OutstandingPayables.js`

### S511 — 2026-08-03 — Outstanding Payables: retroactive "Edit Payment Mode" for historical settlements

Follow-up to S510's Payment Mode dropdown: that only covers *new* payments going forward, leaving every pre-existing settlement with a blank Payment Mode. Added an "Edit Payment Mode" bulk action to Paid History (same checkbox-selection + modal pattern as the existing "Edit Note", added next to it) so historical payments can be tagged after the fact — useful in particular for rows whose *note* already documents the method in free text (e.g. "Fonepay on 8th Shrawan 2083") but never had the structured column set, since that column didn't exist yet when they were recorded. Also added a Payment Mode column to the Payment History table itself so tagged/untagged rows are visible without opening the modal. Verified live: bulk-set a 9-row historical Mahalaxmi Store settlement to FonePay and confirmed it now shows correctly on the Vendor Balance Confirmation letter.

**Files:** `src/modules/ims/reports/OutstandingPayables.js`

### S510 — 2026-08-03 — Payment Mode dropdown for bill settlements; found + fixed a real line-value rounding bug live

Added a Payment Mode dropdown (Cash / FonePay / Bank Transfer / Cheque) to both "Pay this bill" and the bulk-pay form in Outstanding Payables — distinct from `purchase_entries.payment_method` (Cash/Credit/FonePay), which describes the *original purchase*, not how a Credit bill's *settlement* was later paid. New `payable_payments.payment_mode` column (`20260803100000_payable_payments_payment_mode.sql`); the Vendor Balance Confirmation letter's "Payment Mode" column (previously blank on every Payment row) now shows it. `vendorBalanceHelpers.js`'s payment-grouping key folds `payment_mode` in alongside date/note so genuinely different settlements on the same day don't silently merge. Insert goes through a small `insertPayments()` wrapper that retries once without `payment_mode` on a `PGRST204` ("column not found in schema cache") response, so Save never breaks for a client whose DB predates the migration — note this is a **different** error code than the `42703` this file's existing `paid_at` setup-check uses: `42703` is a raw Postgres code that only surfaces on a SELECT reaching the database; an INSERT with an unrecognized column is rejected by PostgREST's own schema-cache validation before that, as `PGRST204`. Confirmed live via a smoke test against the (pre-migration) DB that the fallback actually engages rather than surfacing the raw error.

That same smoke test surfaced a real, pre-existing bug unrelated to payment_mode: a 5-line bill with two lines priced at 3-decimal rates (`0.20915`, `0.21795`) has a true net total of NPR 1400.00175, not a clean 1400.00 — invisible everywhere in the UI since every display path rounds to 2dp, but `OutstandingPayables.js`'s own `l.value`/`l.remaining` computation carried that sub-paisa fraction unrounded. "Pay in full" pre-fills the editable amount via `.toFixed(2)` (1400.00), and `Math.min(amount, bill.remaining)` then caps the actual payment a hair below the true (unrounded) remaining — the shortfall lands entirely on whichever line `allocatePayment()` processes last: its *written* amount still rounds to a clean figure (so the payment looks complete in Payment History), but the *raw* allocation used for the settle check falls just short of that line's `e.value`, so it never gets `paid_at` stamped and the bill stays stuck "outstanding" with nothing wrong visibly displayed. Fixed by rounding `l.value`/`l.remaining` to currency precision the moment they're computed — the exact same fix (and same root cause) `vendorBalanceHelpers.js`'s `billGrandTotal()` already needed in S502. The live bill this surfaced on (Mahalaxmi Store #3066) was corrected directly (its one stuck line's `paid_at` set to match its already-correct, already-paid-in-full sibling rows) since the underlying payment was genuinely complete — this was a display/settle-detection bug, not a missing payment.

**Files:** `supabase/migrations/20260803100000_payable_payments_payment_mode.sql`, `src/modules/ims/reports/OutstandingPayables.js`, `src/modules/ims/reports/vendorBalanceHelpers.js`, `src/modules/ims/reports/VendorBalanceConfirmationPrint.jsx`

### S509 — 2026-08-03 — Outstanding Payables: drop the redundant per-row Delete button

Follow-up to S508: once checking a row activates "Delete Selected (N)" at any count including 1, the per-row "Delete" button on every Payment History row was pure duplication — same action, two places, and with several rows per bill (a 9-row split payment renders 9 red buttons) the redundancy was real visual noise. Dropped the per-row button and the now-fully-superseded `deletePayment()` single-row function entirely (bulk delete already covers the single-row case with the same one confirm + one DELETE call). Payment History rows are now checkbox → date → amount → note, no trailing action column.

**Files:** `src/modules/ims/reports/OutstandingPayables.js`

### S508 — 2026-08-03 — Outstanding Payables: bulk-edit note across checked Payment History rows

Paid History previously had no way to add or correct a payment's note after the fact (only "Delete"). Added an "Edit Note" button next to "Delete Selected" in a bill's Payment History block — appears when at least one row is checked, opens a modal, and overwrites the note on every checked row with one value (checkbox-scoped, not "all rows for the bill" automatically — a deliberate choice: a bill's Payment History can in principle hold rows from genuinely separate settlement events, and forcing every row to share one note regardless of selection would be wrong for that case).

This closes a real gap the Vendor Balance Confirmation letter already depended on: it groups payments into one ledger line per (bill, date, note), so rows that were saved with different notes render as separate "Payment" lines even when they were really one settlement, and rows saved with no note at all never showed the settlement method the vendor letter is supposed to convey. Verified live: selected all 9 of a Mahalaxmi Store bill's split payment rows, set one note, and confirmed both the Payment History table (all 9 rows now show it) and the Vendor Balance Confirmation schedule (rows merged into a single "Payment" line with the note beneath the ref) picked it up correctly.

**Files:** `src/modules/ims/reports/OutstandingPayables.js`

### S507 — 2026-08-03 — Vendors: payment terms field, quick-editable from Outstanding Payables

New `vendors.payment_terms` free-text column (e.g. "Net 30", "COD", "50% Advance") — manageable from the Vendors page (new form field + table column) and, per direct request, quick-editable right from Outstanding Payables' vendor group header row via an "Edit Terms" button next to the vendor name, without navigating away.

**Migration not yet applied to the live DB** — needs a one-time manual step (see `supabase/migrations/20260803090000_vendors_payment_terms.sql`): paste `ALTER TABLE public.vendors ADD COLUMN IF NOT EXISTS payment_terms text;` into Supabase → SQL Editor and run it. Until then, Outstanding Payables' small separate `payment_terms` fetch 400s (`42703 undefined_column`) and is caught — same `setupNeeded` pattern this file already uses for `paid_at` — so the page keeps working normally with the terms display hidden and the Edit Terms modal showing the exact SQL to run instead of failing silently.

**Files:** `supabase/migrations/20260803090000_vendors_payment_terms.sql`, `src/modules/ims/vendors/Vendors.js`, `src/modules/ims/reports/OutstandingPayables.js`

### S506 — 2026-08-03 — Vendor Balance Confirmation: show Payments and Returns as two separate headline figures

Follow-up UI request: the combined "Payments + Returns (FY)" headline box blended two different figures into one number, even though the supporting schedule below it already listed Payment and Return line items separately. Split into two boxes — "Payments (FY)" and "Returns (FY)" — using the exact same `totals.totalPaymentsFy`/`totals.totalReturnsFy` values already computed in `vendorBalanceHelpers.js` (no computation change, display only), and updated the letter's opening-balance sentence to match: "Opening Balance + Purchases − Payments − Returns = Balance Payable" instead of "− Payments/Returns". Headline grid went from 4 to 5 columns.

**Files:** `src/modules/ims/reports/VendorBalanceConfirmationPrint.jsx`

### S505 — 2026-08-03 — Outstanding Payables: fix a rounding-remainder bug in payment allocation; bulk-select-and-delete for Payment History; re-enter Gourmet Bites' real payment

Follow-up to S503's cleanup. After deleting Gourmet Bites' 10 bad payment rows, the plan was to re-enter its one real payment (NPR 97,823.77 in full, paid 30 Ashadh 2083) via the normal "Pay in full" button — which surfaced a second, unrelated bug in the allocation logic itself: `payBill()`/`paySelectedBills()` computed each line's proportional share as a raw float (`Math.min(e.remaining, left)`) and let each of the 10 inserted `payable_payments.amount` values round independently to 2dp on write. Rounding 10 shares independently can each round in a different direction and lose fractions of a paisa in aggregate — verified live: the resulting rows summed to NPR 97,823.76, one paisa short of the real NPR 97,823.77, leaving a permanently uncollectable phantom balance with no way to ever fully clear it (the same failure family as S504, but in the allocation math itself, not the display layer — confirmed distinct by re-deriving from first principles rather than assuming the earlier fix covered it, since an initial rounding tweak to `vendorBalanceHelpers.js`'s `billGrandTotal()` did *not* resolve this when checked live).

Fixed by extracting a shared `allocatePayment(entries, amount, date, note)` used by both callers, using a running-cumulative rounding technique instead of independent per-line rounding: round the *cumulative* allocated-so-far total at each line, then take the difference from the previous cumulative rounded total as that line's share. This guarantees the inserted rows always sum to exactly the rounded payment amount, no matter how many lines it's split across.

Also added bulk-select-and-delete to Payment History (checkbox per row + "select all" in the header, a "Delete Selected (N)" button, one confirm dialog and one `DELETE ... IN (...)` call regardless of row count) — used live to remove Gourmet Bites' second batch of provisional-fix rows in one action instead of one-by-one. Re-entered the real payment (NPR 97,823.77, 30 Ashadh 2083, note "Siddhartha Bank") via the now-fixed allocation logic; verified directly against `payable_payments` that the 10 resulting rows sum to exactly NPR 97,823.77, and the Vendor Balance Confirmation letter for FY 82/83 now shows a clean Opening Balance NPR 0.00 + Purchases NPR 97,823.77 − Payments/Returns NPR 97,823.77 = Balance Payable NPR 0.00.

**Files:** `src/modules/ims/reports/OutstandingPayables.js`

### S504 — 2026-08-03 — Vendor Balance Confirmation: show Purchase (gross) and Return as separate lines, fix headline arithmetic

Live smoke test (fresh test vendor, Credit bill with VAT+discount, a partial return against it, a Cash bill, a cross-fiscal-year payment) surfaced two related bugs in `vendorBalanceHelpers.js`. First: a return posted against a bill from the *same* confirmed fiscal year was silently netted straight into that bill's displayed total — so the printed schedule showed one "Purchase" row with no visible trace of the return, even though the headline "Payments/Returns" figure did count it. Second, and worse: this made the headline sentence's own arithmetic wrong on screen — "Opening Balance NPR 0.00 + Purchases NPR 1,830.60 − Payments/Returns NPR 430.00 = Balance Payable NPR 1,830.60" doesn't actually compute to 1,830.60 (it's 1,400.60); Purchases was already net-of-return, so subtracting the return again in the displayed formula double-counted it even though the real Closing Balance — computed independently via the schedule walk — happened to still be correct.

Fixed by always showing a bill at its GROSS total, with any return against it as its own separate ledger line, for both a return against a bill from *this* FY and a return during this FY against a bill carried over from a *prior* FY. A return's line is never a flat `qty × rate` — new `walkBillReturns()` processes a bill's returns chronologically from its gross total through `calcBillTotals`, since VAT recalculates on the shrinking post-return base each time. This makes "Purchase" (gross) + "Return" (VAT-adjusted effective value) always sum to exactly what a single netted line would have shown, so the headline arithmetic now genuinely reconciles: hand-verified live as `0 + 2,316.50 − 485.90 = 1,830.60`. `totalPurchasesFy`/`totalReturnsFy`/`totalPaymentsFy` were all recomputed on this same gross/effective basis so the four headline stat boxes stay consistent with the detailed schedule below them.

**Files:** `src/modules/ims/reports/vendorBalanceHelpers.js`

### S503 — 2026-08-03 — Outstanding Payables: add a Delete Payment action; fix Gourmet Bites' bad payment data

Testing Vendor Balance Confirmation against real data surfaced a genuine data problem: vendor "Gourmet Bites Pvt. Ltd." had 10 `payable_payments` rows that were each a raw, pre-discount/pre-VAT line-item amount lifted straight off the paper invoice, instead of a real payment against the actual (discounted + VAT'd) amount owed — inflating the bill's recorded "paid" total to NPR 115,426 against a real NPR 97,824 bill, producing a nonsensical NPR -17,602.53 "advance" balance in the new report. Cross-checked this wasn't a code bug: `OutstandingPayables.js`'s own `payBill()` always allocates from the VAT/discount-adjusted `e.value`, never raw `qty × rate`, so these rows couldn't have come from that button — they were entered directly, bypassing it.

The actual gap: there was no way anywhere in the app to remove a mis-entered payment (only "add," never "delete"). Added a **Delete** button to each row in a bill's Payment History (Outstanding Payables, both tabs), with a confirm dialog; deleting a payment that had settled its line correctly clears `purchase_entries.paid_at` so the line reappears as outstanding/partial instead of staying falsely marked "paid." Used it live to remove Gourmet Bites' 10 bad rows — found and fixed a real bug in the paid_at-clearing logic in the process (it compared against `entry.value`, which becomes unreliable — even negative — once a fixed bill-level discount is left dividing an ever-shrinking subset of still-"paid" lines mid-cleanup; now it always clears `paid_at` unconditionally, since a false "still fully paid" is trivially recoverable via Pay Bill, but a silent $0-paid line stuck "paid" is a real reporting bug).

**Files:** `src/modules/ims/reports/OutstandingPayables.js`

### S502 — 2026-08-03 — Vendor Balance Confirmation: on-screen preview fixes + print polish

Follow-up to S501, from live testing of the printed output. The letter's on-screen preview is always-visible (not `.print-only`-hidden, so the user sees exactly what will print) but its text used print-page colors (`#000`/`#555`/`#777`) with no light backdrop behind them — invisible against the app's dark theme. Fixed by wrapping the letter in a white "paper" background (`.vbc-paper`) and changing the remaining grey (`#555`/`#777`) labels to plain black to match. That wrapper's drop-shadow/rounded corners are an on-screen-only affordance (it reads as a floating page against the dark UI) — added a `@media print` rule stripping them on actual paper/PDF output, same pattern S498 already uses for `.card`/`.stat-card`. Also increased the signature strip's `marginTop` (64px → 110px) to leave room for a physical company stamp above each signature line, not just a pen signature.

**Files:** `src/modules/ims/reports/VendorBalanceConfirmationPrint.jsx`, `src/components/Layout.css`

### S501 — 2026-08-03 — Vendor Balance Confirmation report (Nepal IRD Annexure 13)

New Pro-tier report (`/vendor-balance-confirmation`, Reports → Menu & Vendors) generating a printable per-vendor, per-BS-fiscal-year balance confirmation letter — Opening Balance, Purchases, Payments/Returns, and Closing Balance, plus a supporting bill-by-bill schedule with a running balance, matching how Nepal IRD Annexure 13 (अनुसूची १३) reconciliation and NSA 17 external-confirmation audit evidence are actually done: sign, stamp, and return within 7 days. Reachable from the new page's own vendor/fiscal-year pickers, or via a "Confirm Balance" link added to each row on the Vendors page (`?vendor=<id>` preselects it, same `useSearchParams` pattern Stock Movements already uses for its own preselect link).

Opening Balance — the balance as of the fiscal year's start date (Shrawan 1) — is new logic with no prior analog in the codebase: every other balance figure in the app (Outstanding Payables' `remaining`) is a live "today" snapshot, not a historical as-of-a-past-date one, so it nets only payments/returns dated before the cutoff rather than all of them. The bill-total math itself reuses `calcBillTotals()` (VAT-correct, same function the Purchases voucher and Outstanding Payables use) — deliberately *not* Vendor Report's own `allBills` builder, which skips that function and reads ~13% low on VAT-inclusive bills. `billKeyOf`/`aging`, previously duplicated across Vendor Report and Outstanding Payables with two genuinely different shapes, were centralized into `purchasesHelpers.js` in the cross-period-safe (`purchase_group_id`-first) form and Outstanding Payables now imports them; Vendor Report's single-period-scoped version was left alone since reusing it here would silently misgroup bills across period boundaries.

**Files:** `src/modules/ims/reports/VendorBalanceConfirmation.js`, `src/modules/ims/reports/VendorBalanceConfirmationPrint.jsx`, `src/modules/ims/reports/vendorBalanceHelpers.js` (new); `src/modules/ims/purchases/purchasesHelpers.js`, `src/modules/ims/reports/OutstandingPayables.js`, `src/modules/ims/vendors/Vendors.js`, `src/utils/bsCalendar.js`, `src/App.js`, `src/components/Layout.js`, `src/context/AuthContext.js`, `src/context/SettingsContext.js`, `src/pages/adminClients/FeatureAccessModal.js`, `src/pages/Help.js`

### S500 — 2026-08-03 — Purchases table: single-item bills collapse into one row instead of a redundant header + item-row pair

Every bill rendered as a bill-header row (Day/Vendor/Total/Payment/Actions) followed by one row per line item — correct for a multi-item bill, but for a single-item bill (the majority of this client's data: Mega Mart, Horizon Mart, Three Star Foods, etc. are mostly "1 item") it meant two rows to show one fact, with the header row's Item cell and the item row's Day/Vendor cells always blank. Added a branch in the day/group render loop: when `groupEntries.length === 1`, the bill-level fields and the single line item's fields render together in one row (Day · Item+Category · Vendor+invoice · Qty · UOM · Rate · Total-with-VAT/discount · Expiry · Payment+Actions) with no blank cells; multi-item bills keep the existing header-row + item-sub-rows layout unchanged, since collapsing those would lose the per-item Qty/Rate breakdown. The day-number cell and the payment/actions cell (identical in both row shapes) were pulled into local `dayCell`/`actionsCell` variables built once per group so the two branches don't duplicate that JSX.

**Files:** `src/modules/ims/purchases/Purchases.js`

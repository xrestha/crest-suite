# Session log — S675–S691

17 entries, 2026-09-04 to 2026-09-07. Newest first, as in the original `README.md`.
Full range list: [CHANGELOG/README.md](README.md).

---

### S691 — 2026-09-07 — Navigation moved to a two-band top bar, and the bar would not have stuck

**No migration, no Edge Function deploy.** `CACHE_NAME` and `APP_VERSION` both bumped to
`crest-v220`.

The 240px fixed sidebar is now a **two-band top bar**, ported from the sister repo hss-suite, which
made the same move on 2026-09-07 and whose `Layout.js` carries the reasoning inline. Band 1 is
SESSION — brand, the IMS/HR/POS/Admin switcher, which property and BS period you are on, plan,
search, calculator, account. Band 2 is NAVIGATION within the selected module: Dashboard as a pill,
then one dropdown per existing nav group. Two bands rather than the one in the reference screenshot
because those are independent axes and a single strip makes them read as one list; the shell
reclaims 240px of width on every page in the product.

**Nothing about the nav MODEL changed, and that was the point.** `IMS_GROUPS` / `HR_GROUPS` /
`POS_GROUPS` were already `{ key, label, items }` — a dropdown in everything but name — so
`renderBarGroup` is `renderGroup`'s horizontal twin and the rows inside every panel are the same
`renderNavItem`, with the same `isItemVisible` gates, the same per-route count badges and the same
pin star. The command palette, the four gate types and every route guard are untouched. Below 768px
the whole thing is still the phone drawer, with its class names deliberately unchanged so the
drawer rules, the coarse-pointer bumps and the print hide-rule kept working.

**The bar would not have stuck, and only measuring caught it.** `src/index.css` opens with
`html, body { overflow-x: hidden }`, so `body` becomes its own scroll container sized to its
content and a `position: sticky` child of the body flow has a scrollport that never scrolls. It
renders, it computes to `sticky`, and it moves 1:1 with the content forever. Measured in a headless
browser against the real stylesheet: the bar's viewport `top` went 0 → −600 after a 600px scroll.
This is S604 — the rule this repo already wrote for the guest menu, `/login` and `/pricing` —
arriving in the authenticated shell for the first time. `.layout-root` now owns the scrollport
(`height: 100dvh; overflow-y: auto; overscroll-behavior: contain`) and carries **no `min-height`**:
100vh ≥ 100dvh, so a min-height would push the root past its own scrollport and hand the scroll
straight back to the body, which is the original bug. One consequence swept: `window.scrollTo` no
longer describes the page, and `FinalSettlement.jsx` held the only call site inside the shell.

**Every dropdown panel is `position: fixed`, anchored to its trigger's measured rect.** Band 2 sets
`overflow-x: auto` so the bar can never grow a third band — and a scroll container clips BOTH axes,
there is no one-axis overflow in CSS, so an absolutely positioned panel is cut off at the bar's own
height and never appears at all. Verified rather than assumed: `elementFromPoint` inside the open
panel returns a `.sidebar-link` at 900 / 1024 / 1280 / 1440, and the right-most menu's right edge
stays inside the viewport at every one.

**`.topbar-pill` is a complete class, not a hover rule over an inline style object.** The first
screenshot of the harness showed why: a pill carrying no background of its own falls through to the
UA — `#f0f0f0` on a `<button>`, underlined `#0000EE` on an `<a>` — which is the S678
`.btn`-with-no-variant failure on a second class. Everything the pill IS now lives in `Layout.css`;
only which state it is in (`--current`) comes from JSX.

**What lost its home, and where it went.** The collapse toggle is gone (there is nothing to
collapse to). The upgrade teaser and the footer chip both lived in a 240px column: the full teaser
stays in the phone drawer, and on desktop the same two facts — next tier, locked count — are one
row in the account menu, computed from a single `upgrade` object so they cannot drift. Pinned
favourites became the first menu in band 2. Help, Support and Sign out moved under your name.
`.context-bar` is now phone-only: the bar states tenant and period on desktop, and saying it twice
on one screen is worse than saying it once — but below 768px the bar is gone and the drawer is
off-canvas, so it stays as the only place a phone user can read it.

**Also swept, because a location claim in copy is a bug too.** The Help FAQ "How does the sidebar
work?" described the icon rail and the flyout panel sentence by sentence; it and 23 other location
claims across `Help.js`, `imsGuideData`, `posGuideData` and `suiteGuideData` now name where the
control actually is. `DESIGN.md`'s Layout and Navigation sections were rewritten, including the
paragraph that existed solely to justify the `width`/`margin-left` collapse animation — the
argument was sound and is now moot, so the two matching `layout-transition` entries in
`.impeccable/config.json`'s `ignoreValues` went with it: an exemption that no longer exempts
anything is a claim the next reader has to go and disprove.

`node scripts/check-design-layers.mjs` passes; 703/703 tests pass, and `appVersion.test.js` caught
the `APP_VERSION` half of the cache bump that the `CACHE_NAME` half alone would have left behind.

**Same session, separate sweep: nine literals from a palette retired in S689.** The design hook
flagged one `rgba(138,146,163,0.10)` in `Layout.css`; the value is `#8a92a3`, which was
`--theme-text2` on the **pre-Modernist** palette. S689 moved that token to `#bab6b6` / `#605d5d`
and left nine frozen copies behind across six files (`.badge-gray`, the subscription
"No end date" chip, the Starter plan pill in two places, the audit-log fallback and an HR note
band) — each paired with `color: var(--theme-text2)` text that re-toned correctly while the fill
did not. All nine now derive from the token per the S683 rule. The `#6b7280` still in
`BestSellers` / `CoversReport` / `SalesReport` is deliberately NOT part of this: it is `chart-tick`
on Recharts SVG props, where `var()` cannot resolve.

**The rule itself is what made them read as sanctioned**, and that is the lesson worth keeping:
`design-system.md` had stated the canonical neutral AS the literal since S540, so the hook's own
guidance endorsed a value the palette had moved out from under. A rule must never freeze a value
that moves.

**A measurement scare, and the harness was wrong, not the code.** A first pass also changed the
neutral chips' ink to `--theme-text1` on the strength of a reading that `.badge-gray` measured
4.31:1 on Light, under AA. That number was an artifact: `getComputedStyle` returns a `color-mix` as
`color(srgb r g b / a)` with **0→1** components, and a parser written for `rgb(0→255)` silently drops
the tint and reports the text on the bare ground. Re-measured from painted pixels with the browser
doing the compositing, the real figures are **4.68:1** (new) against **4.95:1** (the retired
literal) on Light and 6.44 / 6.83 on Night — AA-legal throughout, so the ink change was reverted
and this landed as a pure token swap. `.badge-gray` is still the thinnest margin of the six badge
classes and the only one whose fill and ink share a token, which is now recorded where the next
person will look.

**A second doc pass, because the first one only swept the sections that were obviously about the
shell.** `DESIGN.md`'s Layout and Navigation were rewritten in the first pass; Colors, Type, Brand,
Print and the skip-link bullet still placed the wordmark, the lockup and the print hide-rule in a
sidebar. The `--theme-sidebar` token keeps its name and now grounds the bar as well as the drawer
(it is the shell colour, not a position); the print rule names **both** `.app-topnav` and
`.sidebar-wrap`, since a class missing from it prints the whole navigation at the head of every
document; and the skip link's justification was a count — 41 sidebar controls — that is now about
**20** (9 in band one, 11 in band two on the IMS panel). Half as many, and the link matters exactly
as much, because they still all precede the content in DOM order on every route.

**And the Admin Dashboard's four adoption pills were three different boxes.** Reported from a
screenshot. The module pills were `1px 4px` with no border; the Suite pill was `1px 5px` WITH a 1px
border — and a border adds 2px of height, so the pill meant to read as a peer of IMS/HR/POS was
the odd one out. Measured, the old heights were `13/13/13/18` and only looked equal while the row
fitted on one line, because flex `stretch` was painting the fills to the tallest; the moment it
wrapped, the difference showed. `adoptionPill()` now states the box once and only COLOUR varies,
with a TRANSPARENT border on the three so they match height without taking Suite's ring. All four
measure 19px at every width. Weight was deliberately NOT unified — a heavier weight is one of the
two things marking Suite out, and weight is not size, so flattening it would have removed a
distinction this file draws on purpose while fixing one it never meant to. The row went 133.4 —
137.4px, so it wraps in exactly the same places it always did. The block's comment also cited a
190px grid floor; S642 lowered it to 158 and the comment was never updated.

**Then the next screenshot asked where the change was, and both answers were mine.** First, the
pill commit bumped **neither** `CACHE_NAME` nor `APP_VERSION`, so the service worker went on
serving the old chunk — the exact S452 trap `CLAUDE.md` documents, and the reason a shipped fix can
be invisible with nothing broken anywhere. Both are `crest-v221` now. Second, and worse: even
served fresh there was almost nothing to see. `align-items: stretch` had already made the four
heights agree on one line, so the heights were never what looked wrong — the Suite pill's
**border** was, and the border is precisely what the first pass kept. Equalising the invisible half
while preserving the visible one is a ~1px change nobody can point at. The ring is gone; all four
pills now share the box completely and Suite is marked out only by a stronger fill, a heavier
weight and the ★, which keeps S552's rule that a billed axis stays visible on the screens that
bill it. The lesson is in `design-system.md`: when a difference is reported by eye, name the cue
carrying it before choosing what to equalise — a measurement saying two things now agree is not
evidence that anyone can tell.

**It was still wrong, and it was never the pill.** Unregistering the service worker did not change
it either, which is what finally ruled out cache and forced a look at the real markup. The Suite
pill is wrapped in **`Tip`**, whose wrapper span is `display: inline` with a dashed `border-bottom`
and `cursor: help` — right for a tipped word in a sentence. As a **flex item** in the pill row it
stretches to the row height while the pill inside stays at its natural height at the top, and the
dashed rule draws at the bottom of the stretched box, below the pill. That is the whole defect: the
pill's own box had been correct for two commits. Fixed with `style={{ display: 'inline-flex',
borderBottom: 'none', cursor: 'default' }}` — the override `UsageChip.jsx` has always carried.
Three `.badge` chips in `EmployeeList.jsx` had the same shape and are fixed too; two of them sit
beside a Tip-wrapped `<button>`, which Tip correctly leaves undecorated, so one chip in that row
wore a dashed rule and its neighbour did not.

**The method failure is the part worth keeping.** Three rounds of measurement reported
`[19,19,19,19] equal` because the harness rendered a bare `<span>` while the live pill is inside a
`Tip`. It was not measuring the thing on screen, and it agreed with itself every time. When a
harness disagrees with a screenshot, the harness is the suspect.

**Fourth screenshot, deploy confirmed landed, still the same — so this time the check went to the
CDN, not the browser.** Fetching `crest-inventory.vercel.app`'s own `main.*.js`, walking its chunk
map and grepping the served dashboard chunk showed BOTH fixes live under `crest-v222`: the
borderless helper and the Tip override. What Vercel serves is right; the tab was running JavaScript
from two versions earlier, after an unregister. Rendering the three versions side by side (ring +
dashed rule / dashed rule only / clean) is what let the screenshot be identified by eye rather than
argued about. But even the correct version still had the Suite pill LOUDER than its siblings — 20%
fill against 10%, and bold — kept through two passes on the reasoning that S552 wants a billed axis
visible. The star alone does that, and the reader had asked three times for one set. Now all four
share fill intensity and weight too; Suite is the ★ and its Tip, and nothing else. Emphasis the
reader keeps asking to remove is not a design distinction, it is noise wearing one's clothes.
`crest-v223`.

---

### S690 — 2026-09-07 — Menu Pricing sorts by any column and searches, and its failed read stops reading as an empty menu

**No migration, no Edge Function deploy.** `CACHE_NAME` and `APP_VERSION` both bumped to
`crest-v219`.

96 items on one page with no way to ask "which of these is eating my margin". Every column heading
in the IMS branch is now a sort control — Item, On POS, Food Cost, Current Price, FC %, New FC %,
Change — and a Search box beside the category tabs filters on item name and category. A figure
column opens **highest-first** on the first click, because the reason to sort by FC % is to find
the bad ones, not to confirm the good ones. Both branches get the search; the sortable headers are
the IMS table's, which is the one with figures to sort.

**Sorting on a value the reader is halfway through typing needed a rule.** New FC % and Change are
computed from the unsaved draft in the New Price box, so a naive sort re-orders on every keystroke —
"7", "75", "750" are three different food costs and three different positions — and the row being
typed into slides away under the cursor. Two decisions fall out: a row with nothing typed sorts
**last in both directions** (no new price is not a small number), and the row order **freezes the
moment a price box takes focus** and is released by re-sorting, searching or switching tab. It is
deliberately NOT released on blur: the Save button sits in the same row, and a row that re-sorts on
blur moves out from under the pointer between mousedown and mouseup, so the click never lands. A
line beside the search box says the order is being held and how to release it.

**The header sort button would have printed as nothing.** `@media print` carries a blanket
`button { display: none !important }`, so turning a heading into a button silently removes the
column's LABEL from every printed price list. The new `.th-sort` rules in `Layout.css` reprint it
as plain text and drop only the arrow. Worth stating as a general rule: in this codebase, promoting
a static label to a button removes it from print unless the print block is told otherwise.

**A failed read in the IMS branch had no case at all** — it fell through to
`display.length === 0` and rendered "No menu items found. Use **+ Add Item** above to add your
first item", which is an invitation to re-key a menu that is already there. That is the exact S594
shape the POS-only branch below it already handled with `ReportLoadError`; only the branch with the
money in it was missing it. Both empty states now also distinguish "nothing matches this search" from
"nothing here yet".

Print and Excel already exported "as filtered on screen", so the search had to reach both: the
print title and the `.xlsx` filename now name the search as well as the category tab, or a
three-row export called plain `menu-pricing.xlsx` reads as the whole menu.

Docs in the same pass: Help page, the IMS module guide and the column tooltips; `DESIGN.md` gains
`.th-sort` under Data Tables and the print consequence under Print; `design-system.md` gains the
class plus two new sections (the button/print trap, and the three rules for sorting on a column the
reader is still typing into); `report-pages.md` and `component-library.md` both record the
half-applied-fix instance, which is the more useful half of this session — S683's fix was real,
present in the file, and reaching one of two branches. 65 suites / 703 tests.

---

### S689 — 2026-09-07 — Modernist: two presets, a zero-radius product, and the signup form gets its own page

**No migration, no Edge Function deploy.** `CACHE_NAME` and `APP_VERSION` both bumped to
`crest-v218` — the service worker is cache-first, so without it none of this reaches an existing
user. Those two had also **drifted apart on master** (`crest-v208` against `crest-v217`), so
`appVersion.test.js` was failing before this session started; nine cache bumps had landed without
the mirror. Fixed in the same pass.

A design handoff in `Claude Design/design_handoff_modernist_theme/` specified a whole-app re-theme:
Archivo, zero corner radius, 2px section rules over 1px row rules, flush-left button labels, KPI
cells sharing edges as one drawn grid, and a single signal red held for the primary action. Two
schemes — **Modernist Night** (`#191817`, accent `#ff563c`) and **Modernist Light** (`#f3f2f2`,
accent `#ec3013`) — built as the existing `dark`/`light` pair, so `system` mode kept working with
no new key. One structural change rode along: the trial form moved off `/login` onto `/signup`.

**Three corrections to the handoff, each measured rather than argued.**

The handoff specified loading Archivo at 400/600/800. The codebase uses weight **700** at 625
inline sites plus 43 CSS declarations, and **500** at 35 more; with only three weights available
CSS font matching rounds 700 to 800 and 500 to 400, silently re-rendering ~660 sites. Archivo
ships 100–900, so all five weights are loaded and nothing moved.

Both presets' `accentInk` had to move a ramp step further out than specified, and this is the
finding worth keeping. **A red accent lands next to the danger and warning slots in a way a brass
accent never did.** On Light, the specified `#ae1800` (accent-700) sat at ΔE **0.5** from
`amberText` under deuteranopia and 6.5 under protanopia — under the floor of 8, i.e. the
categorical slot ("decided, unpaid"; a rank; a close type) and the open-and-waiting slot reading as
one colour for ~8% of men, on exactly the HR approval queues a manager reads to tell them apart.
That is the S608 failure recurring in a second guise. `#7c1405` (accent-800) clears at 16.1/21.3
and holds 8.85:1 on the card, 9.59:1 on the page and 8.11:1 on its own badge tint over the page
ground. On Night the same collision ran the other way — `#ff9783` (accent-400) at ΔE 7.4 from
`--theme-red` under deuteranopia — and `#ffc4b8` (accent-300) clears at 10.4 worst-pair. Both
replacements are documented ramp steps, not invented colours.

The mock renders a positive KPI delta in accent ink. The README then says not to introduce new
signal colours. Existing semantics won: a positive delta stays `greenText`.

**The signal colours and the light preset's four `*Text` variants are carried over byte-identical.**
They were tuned across S551/S608/S683 and none of that work is about the accent. `accentInk` is the
one thing a new accent genuinely forces.

**`::selection` is the one accent fill that cannot be given a size floor.** White on `#ec3013`
measures 4.20:1 — legal as large text, not as normal text — so `.btn` went to **15px/600**, which
is where WCAG's large-text threshold begins, and that size is now a contrast floor rather than a
density choice. Selection inherits whatever size it lands on, including 11px badges and 10px
micro-captions, so it takes `--theme-accent-hover` instead: `#dd2b0f` on Light where white clears
4.74:1, and on Night the *lighter* `#ff9783`, which raises contrast against that preset's ink
foreground rather than lowering it. One declaration, correct on both grounds, still a token.

**Radius: 422 tokenised sites came free, 148 inline literals did not.** The six `--radius-*` steps
all resolve to 0 and were kept as names rather than deleted, because a scale with a home can be
re-tuned in one edit where 420 hardcoded zeroes cannot. The 32 `'50%'` circles squared too — status
dots, toggle knobs, avatars, step markers, PIN dots — which withdraws DESIGN.md's standing
exemption for "the circular status dot and its pulse ring". Two exemptions stand and are named in
DESIGN.md's Shapes section: print templates, and the scoped guest surfaces (`.guest-menu`,
`/pos/book/:clientId`), which keep their own shape language for the same reason they keep their own
bone-and-pine palette. Verified by walking the live DOM for a computed `border-radius` that is not
`0px`, not by grepping.

**The login page's lighting rig is gone, and it is the clearest illustration of why a re-theme is
not a palette swap.** `/login` was lit by three radial layers on its scrollport plus accent-tinted
faces on the header, the sign-in card and `/reset-password`'s panel — every one a percentage mix of
`--theme-accent`. Under brass, 34% of a muted gold reads as light falling on a dark room. Under a
saturated red it reads as a brown cast over the whole page, which is what it rendered as the moment
the preset changed. **A rig built as percentages of the accent is only ever as neutral as the
accent is.** Two neutral rule tokens (`--lp-row-rule`, `--lp-section-rule`) replace it: with the
corner gone, rule weight carries the structure, and structure is not something the brand colour
should be spent on.

**`/signup`.** All twelve `t*` state variables, `handleTrialSignup`, the field helpers and
`EMAIL_RE`/`edgeOp` moved; `acceptancePayload()` came with them untouched, since it is the evidence
a contract was formed and is kept seven years. Six things a naive move would have dropped, all
handled: the trial-success path used to pre-fill the sign-in card 300px below and now redirects to
`/login?email=…` (the two copy lines that said "sign in above" were false the moment the form
moved); the `ready && session && profile` redirect is reproduced, or a signed-in tab meets an
account-creation form; `/login?trial=1` became a redirect rather than an autofocus, keeping
Pricing's two CTAs and any bookmark working; the header's `<a href="#start-trial">` became a router
navigation and its band's `scroll-margin-top` went with the anchor; `Login.trialConsent.test.jsx`
**moved** to `Signup.trialConsent.test.jsx` rather than being rewritten, because it follows the
FORM — a clickwrap test rendering a page with no clickwrap would have passed vacuously on the
assertions that merely count anchors; and `Signup.js`/`Signup.css` were added to the `paths:` globs
of `auth-and-pins`, `accounts-and-logins` and `support-contact`, none of which a new file inherits.

**Two shared rules were dropped as collateral and caught by looking at the page, not by any check.**
`.login-form` and `.login-footer` sat inside the span the slim signup band replaced. The build
compiled fine without them; the footer's copyright line simply sat hard against the viewport's left
edge, and `.login-form` is one of the primitives shared three ways (Login, Signup, ResetPassword),
so its gap was gone from every signed-out form. **A CSS rule's absence is not a build error.**

**The chart palettes were re-measured and deliberately left alone.** The Chart Palette Rule says to
re-measure when they are touched, and the card grounds moved under them. `CHART_COLORS` holds
worst-pair ΔE 37.9 normal / 14.7 deuteranopia / 17.5 protanopia and `COST_BREAKDOWN_COLORS` 56.3 /
12.4 / 30.6 — all clear of the 15 and 8 floors — and the new accent's nearest chart slot is
`#ea580c` at ΔE 16–17, so chart and chrome do not collide. The gold slot stays gold: it is Food
Cost's identity across three surfaces and it used to double as "the accent", which it no longer is.
**That second meaning is what the re-theme removed, not the colour.** Two real defects were fixed
while passing: `PeriodComparison.js`'s `FALLBACK_HEX` claimed in a comment to mirror `CHART_COLORS`
and did not — slots 2–8 were a different set, so a category drew one colour on the Dashboard and
another here, and it had introduced `#818cf8`, one of the two indigos the One Accent Rule names as
a seven-time violator; and `SalesReport.jsx` carried a silent copy of `CoversReport.jsx`'s hex pair
without the measurement that justifies it.

**The impeccable hook caught a real Closed-Scale violation on the first write of `Signup.css`** — a
`clamp(28px, 4vw, 36px)` title, both ends off the type ramp. Fixed to the documented `display`
role's own `clamp(24px, 3.4vw, 32px)` rather than suppressed. That is the rule working, and it only
works because `DESIGN.md`'s frontmatter was updated **first**: the detector reads it as its source
of truth, so doing the docs last would have meant every edit in between firing advisories against a
palette and a radius scale that no longer existed.

**Documentation.** `DESIGN.md` frontmatter (colours, the zeroed `rounded` scale, Archivo, a new
`button-label` role) plus the prose the code was about to contradict: the "softness comes from the
radius scale" thesis, the whole Shapes section, the Proportional Corner Rule (replaced by the
**Zero Corner Rule**), and the Colors palette. The signal-vocabulary word **"brass" became
"accent"** — under Modernist the categorical slot and the danger slot share a hue, which makes the
rest of the One Signal Meaning Rule load-bearing rather than tidy: they still separate (ΔE 33 under
deuteranopia) but no longer separate by hue *name*, so the label and the shape mark carry more than
they used to. Do not add a sixth hue to recover the old separation; say the word.

**Verified:** 703 tests green including the 22-assertion `themeTokenParity` suite (which now
asserts the text ladder as a luminance ORDER rather than two hardcoded hexes, so the next palette
change cannot silently re-invert it) and the relocated 9-assertion consent suite; `build:verify`
clean; `check:docs` clean at 161 globs; `/login`, `/signup`, `/signup?trial=1`, `/pricing` and
`/login?trial=1` walked in both schemes in a real browser.

---

### S688 — 2026-09-07 — two Supabase advisor exports, three findings, and a decision that had gone stale

**Two migrations, `20260907120000` and `20260907130000`, both applied 2026-09-07.** No service
worker bump, no Edge Function deploy, no frontend change. Each ends in a `DO` block that raises
unless its own change took, so a clean run in the SQL Editor is the verification; the advisor
exports were not re-pulled afterwards.

The owner pasted the Performance and Security lint exports with no more comment than "info from
supabase". Most of both is expected and already reasoned about in `.claude/rules/supabase-sql.md`;
the work was separating that from what genuinely moved.

**The Performance export was one finding printed six times.** `multiple_permissive_policies` on
`client_groups`, once per role — `client_groups_select` (`FOR SELECT`) and `client_groups_write`
(`FOR ALL`) both match SELECT, so every read evaluates both and ORs the results, and the second is
a strict subset of the first because an admin already passes `client_groups_select`'s `is_admin()`
arm. Neither policy carries a `TO` clause, so both are `TO public` and the advisor counts the
overlap once per inheriting role.

**The fix is per-command, against the house rule, for a reason worth keeping.** `supabase-sql.md`
says reach for a single `<x>_all` rather than a per-command set. That is right where one audience
does everything, and wrong here: members READ their own group row, only admin WRITES. A single
`FOR ALL` has to carry the member's read clause in `USING` — and **DELETE is checked against
`USING` with no `WITH CHECK` behind it**, so the obvious collapse would let any member of a group
delete the group row. Three write policies keep the read clause out of the delete path.
`20260907120000`.

**Behind the lint, a write policy that may never have been reachable.** `20260812170000` granted
`authenticated` **SELECT only** on `client_groups`, while `ClientDrawer.js`'s "Create group" button
inserts into it from an admin's browser — which is the `authenticated` role, not `service_role`.
Whether that INSERT works today turns on whether Supabase's stock default privileges got there
first, which cannot be read out of the migration history and could not be checked from here (no DB
password on this machine). The migration grants INSERT/UPDATE/DELETE explicitly either way and
asserts it; the grant is not the gate, `client_groups_insert` still requires `is_admin()`.

**The Security export's 0028/0029 lists are mostly by design** — the guest/booking/PIN-picker
functions are deliberately anon-callable pre-auth entry points with their own internal
authorization, and `is_admin()`/`my_client_id()` are the two permanent anon exemptions
(`20260812100000` re-tested and asserted that revoking either blanks the app name for every
signed-out visitor). Two things in them were not.

**Two stale function overloads, the exact fault S630 fixed for `submit_guest_order` and did not
sweep for siblings.** `CREATE OR REPLACE` keys on the full argument-type signature, so appending a
defaulted parameter creates a *second* function rather than replacing the first.
`submit_my_leave_request` did it with `p_day_type` (`20260707280000`) and `submit_my_tada_claim`
with `p_start_point` (`20260709120000`). `20260810190000` then granted **both** signatures of each
— it saw two and treated that as the shape rather than as the bug.

Neither stale body is a hole: both carry the same `hr_self_service = true` check as their live
counterpart, and the columns they omit default to what the old call meant anyway. What makes them
worth dropping is drift — every later fix lands on one body only, and **PostgREST binds by the
argument keys in the payload**, so a short call silently reaches the older code. After the drop a
short payload resolves to the live body through its own parameter default instead, which is
strictly better than what it hits today. Both `SelfServiceHome.jsx` call sites send the full key
set, so nothing changes for the app.

**Seven trigger functions holding an EXECUTE grant that does nothing.** `assign_asset_code`,
`assign_ims_gate_pass_no`, `assign_pos_credit_note_no`, `assign_pos_invoice_no`,
`assign_pos_order_no`, `assign_pos_parking_slip_no`, `enforce_asset_schedule_immutable` — all
`RETURNS trigger`, and EXECUTE on a trigger function is checked at `CREATE TRIGGER` time, never at
fire time. These are precisely the seven `supabase-sql.md` predicted would keep showing under 0029
"purely because of it", the older `assign_*` pattern of REVOKE-from-PUBLIC-then-GRANT-back. Now
revoked. `20260907130000`, wrapped in `BEGIN/COMMIT` so its assertions can roll the whole thing
back, and asserting on `pronargs`/`pg_trigger` rather than a formatted signature string — the S630
lesson about `pg_get_function_identity_arguments` rendering parameter names.

**The third finding is not SQL: the leaked-password decision had gone stale under its own fix.**
S538 declined the dashboard toggle for two reasons. The first still stands — `adminUserCreate`
never calls `checkPasswordStrength()`, so signup is unaffected either way and the per-path
`isPasswordPwned()` calls are what cover it. The second was that the toggle "would have broken
`reset_pos_pin` outright", because that path fed a 4-digit PIN to `updateUserById` and every
4-digit string is in the Pwned corpus. **That stopped being true when PINs stopped being auth
passwords.** `admin-user-ops` now sends `derivePinPassword(...)` — a base64url HMAC — on every PIN
write, and S569 deleted the raw-PIN fallback. So the blocker is gone and enabling the toggle would
add real coverage to `/reset-password` and `reset_ims_password`/`reset_hr_password` at no cost.
Left as the owner's call rather than assumed, and recorded in `auth-and-pins.md` so the stale half
of S538 stops being read as current.

**The shape worth carrying forward: a fix that removes a constraint does not go back and update
the decisions that were made because of it.** S538's reasoning was correct when written and half of
it was obsolete within a session of S569, with nothing anywhere to say so. The advisor re-raising
the same warning is what surfaced it — which is an argument for re-reading the *rationale* when a
lint you have already dismissed comes back, rather than re-dismissing it from the note.

---

### S687 — 2026-09-06 — the Reservations page opens on every future booking, and says what changed since you last looked

**Service worker `crest-v216` → `crest-v217`. No migration, no Edge Function deploy.**

The owner, after S686: "how is a user / manager / owner going to know about reservations confirmed
by a staff? When I scroll to the date a staff has booked then only I can see the reservation." Right.
The page opened on **today**, "Next 7 days" was a tab rather than the default and stopped at seven,
and nothing anywhere said *what changed since I was last here* or *who did it*. A booking user X
took for the 12th was invisible to user Y until Y happened to pick the 12th. Four decisions were
put to the owner and taken: build all three of the proposed pieces; the Activity view is for every
POS rank (a waiter is the one asked "do we have a booking tonight?"); Upcoming lists everything in
the future, not a window; and "since you last looked" is kept per device in browser storage, not
as a column on the profile.

**Upcoming is the default, and it is the whole future book.** `PosReservations.jsx` opens on
`filter = 'upcoming'`, which now reads `reserved_for >= today` with no upper bound (an outlet's
future bookings cannot approach the 1000-row cap; the query is deliberately unpaged) and renders
under **day header rows** — `1st Ashwin 2083 · Today · 17 September 2026 · 3 bookings` — via
`groupByDay()`. Unconfirmed reads the same range, so it is "every future booking still waiting on
the guest" rather than today's. Day keeps the hour strip; the day picker and ‹ › are live only on
Day and switch to it when used. "Next 7 days" is gone as a label; "Hours over seats" now says
*upcoming*.

**Activity: the newest hundred changes, newest first, with who took each booking.** A fourth tab,
ordered by `updated_at DESC` (the S677 trigger maintains it on every write, the one column that
does), `LIMIT 100`. Each row: how long ago (`agoLabel` — "12 min ago", "yesterday"), what the
latest change was (`activityEvent` — Booked / Edited / Confirmed or Accepted / Arrived / Seated /
Completed / No-show / Cancelled or Declined, the online-request decisions named differently from
the phone-book ones), **by whom for a new booking** (`created_by` through
`get_client_profile_names`, or "guest, online"), the guest, what it is for, and the same one-step
actions as everywhere else. **Who confirmed, seated or cancelled is NOT stored** — only
`created_by` is — so the label carries no actor for those, the column header's tooltip says so,
and Help says so. Storing it would be a migration (`updated_by` set from `auth.uid()` in the
trigger, never a parameter — the S576 attribution rule) and is the obvious next step if the owner
wants it.

**"New since you last looked", per device.** `src/shared/reservationSeen.js` keeps one stamp per
client in `localStorage`, read when the page mounts and written when it is left or the tab hidden
— written on leaving rather than on every poll so that what is new stays marked for the whole
visit. The page head-counts `updated_at > stamp` on every load, shows it on the Activity tab
("3 new") and tints those rows; `useNavBadgeCounts` runs the **same count from the same stamp**
so the sidebar's Reservations chip (S686) now reads requests + changes and never disagrees with
the page. The chip is amber while a request needs a decision and grey when it is only news; the
rail dot is unchanged — it pulses for things that need a hand, and news does not. No stamp (first
visit on a device, cleared site data) counts nothing, never everything since the outlet opened.

`reservationActivity.js` and `reservationSeen.js` are pure and tested (14 new tests: the Edited
threshold, Accepted/Declined vs Confirmed/Cancelled including an accepted-then-cancelled online
booking, the ago ladder, a clock ahead of the server, the stamp round-trip and a corrupted value).
Help's Reservations tips and the POS guide describe the four views. Verified by lint, 31 tests in
the reservations suites and `npm run build:verify`; the page itself was not clicked — the shell
needs a signed-in session and this session has none.

---

### S686 — 2026-09-06 — a booking request now shows a count in the sidebar, not only on the page it lands on

**Service worker `crest-v215` → `crest-v216`. No migration, no Edge Function deploy.**

The S685 fix was retried on the live BHATTI CHOILA page and the request went through (status card
"Request sent", 1st Ashwin 2083 at 11:30 AM). The owner's next message, from the IMS dashboard:
"don't see any notifications for the same in the app." The request WAS on screen — the dashboard's
Bookings Tonight tile read `1 request to accept →` — and it was also in the Reservations page's
amber band with the 15-second chime, and counted on the Orders floor. None of those is a
notification: two are on pages the owner was not on, and a tile's second line is not where anyone
looks for an alert. **The shell already has the alert shape** — the HR and POS module icons in the
rail carry an amber pulsing dot with a "N pending" tip, fed by `useNavBadgeCounts` on a 60-second
poll — and POS's dot counted only unsent KOT lines, so a request that needs a human Accept lit
nothing.

`useNavBadgeCounts.js` — the POS poll now also counts `pos_reservations` where
`status = 'requested'` (head-only count, alongside the existing open-orders read) and returns it
as `posRequests`, kept separate from `posPending` so the number can sit on its own nav row. A
failed count keeps the last value: a badge that vanishes on a dropped poll reads as "handled".
`Layout.js` — the POS rail dot lights on either count and its tip composes them ("Crest POS —
2 pending · 1 booking request"); `renderNavItem` renders a per-route count from a `navCounts` map
(`NAV` is a module-level constant, so the lookup is by route) as a `badge-amber badge-sentence`
chip with an `aria-label`, and Reservations is its first entry. Help's Reservations tips and the
POS guide say where the count shows.

Verified by lint and `npm run build:verify` only. The shell needs a signed-in session to render
and this session has none; the owner is the next check — the Reservations row and the POS rail
icon should both show the waiting request from the IMS dashboard after deploy.

---

### S685 — 2026-09-06 — "Request this table" did nothing: a refused submit said so two screens above the button

**Service worker `crest-v214` → `crest-v215`. No migration, no Edge Function deploy.**

Reported from a phone on the live guest booking page: name and number filled in, occasion picked, a
note typed, tap **Request this table** — nothing. The three RPCs behind the page were probed on the
live project first and all answer (`submit_reservation_request` returns the S677 availability
body's refusal text, so both migrations are applied); the server was never the problem. Reproduced
at 390px against the dev server with the RPCs mocked: the tap called nothing, the page did not
move, focus stayed on the button, and the only feedback was `Pick a day.` rendered at 85px above
the viewport. **A validation error printed beside a card the guest cannot see, with nothing scrolled
or focused and nothing beside the button, is indistinguishable from a button that is broken** —
and on a phone the Day and Time cards are two screens up from where the thumb is. The guest in the
screenshot had almost certainly not picked a day, or had re-picked one (which deliberately clears
the time) and lost the time silently.

`GuestBooking.jsx` — `validate()` now does three things on a refusal instead of one: the inline
errors as before; a summary in the alert beside the button (`Pick a day and a time above, then
send the request.` / `Pick a time above, …` — name and phone keep only their inline error, since
those fields sit right above the button); and `scrollIntoView` + focus on the first card or field
that still needs the guest (the two cards take `tabIndex={-1}` for that, with `.gb-card:focus`
un-ringed since they are never in the tab order). Picking a day or a time clears the summary. The
submit RPC is also wrapped in `withTimeout(…, 20000)`, because a supabase call can stall before it
reaches `fetch` (S453/S454) and on a public page that leaves the button on "Sending…" with no way
back but a reload.

Verified with the same mocked page after the patch: the refused tap brings both inline errors into
view with the Day card focused and the summary beside the button; picking the first open day and
time clears the summary and the next tap calls `submit_reservation_request` once with the expected
body and lands on the "Request sent" card. Not verified on the real BHATTI CHOILA URL — none of the
client ids on record has online booking switched on, and a live submit would have put a test
booking in a real outlet's queue.

---

### S684 — 2026-09-06 — Settings → Support critiqued at 22/40, then fixed in six pushes

**Service worker `crest-v208` → `crest-v214` across the six stages. No migration, no Edge Function deploy.**

`/impeccable critique admin-settings-support tab` — dual-agent, with the browser signed in as the
admin for the first time on an authenticated surface (A reviewed from source only; B ran the
detector and measured the live tab in its own Playwright tab, Dark live and Light by writing the
preset's tokens inline, at 1280 and 390). The tab is the one S683 built. Detector: 0 findings.
Score 22/40 — the best-written settings form in the product on a mechanism that punished the most
natural click. Three P1s, and only one of them was reachable from source:

- **The header "Save Changes" reverted the top card's unsaved edits and said "✓ Saved".**
  `saveSettings()` ends in `loadSettings()`, which sets `platformSupport` to a fresh object from the
  row, and the form's seeding effect was keyed on that reference — so a page-level save, and a
  client switch (which the lower card's empty state tells the admin to do), both re-seeded the form
  from the row over whatever had been typed. Measured: the nearest Save to the consultant fields was
  the *other* card's, 243px away, while the one that saved them sat 1,345px up.
- **The emergency checkbox rendered as a 454×13 strip with its label one word per line.**
  `.form-field input { width: 100% }` matched `type="checkbox"`; no rule anywhere had ever said
  what a checkbox in a field looks like. Both presets. Invisible to a source read.
- **No phone layout.** `.form-grid-2` had no media rule (DESIGN.md had claimed the collapse for a
  month), and the inline nowrap tab strip measured 639px inside 358px, with Data, Theme and Guides
  outside the viewport and unreachable.

**Stage 1 — each card commits its own row** (`46639d9`). The platform form's seed is keyed on the
serialised stored value, so the same row arriving again is a no-op and typed edits survive a reload
or a client switch. The Support tab hides the page-level button; the consultant card gets its own
"Save Consultant" beside the fields it commits. The busy Save stays enabled behind a handler guard
(DESIGN.md's `aria-busy` rule), the emergency select keeps its specific `aria-label` instead of
losing to `aria-labelledby` (measured name was the caption), and the checkbox is described by its
caption.

**Stage 2 — "no phone line" is a state** (`d460019`). Decided with the founder: a blank Mobile fell
through to the compiled floor (their own number), so the only way off the eight support surfaces was
another number or a deploy. `phone_enabled` (default on, so every row saved before it keeps
publishing) drops the whole phone family and the any-time promise with it; a WhatsApp or Viber
number survives only where given explicitly; a consultant still wins per client. The built-in
number is named once (the hint) instead of four times; Mobile and Landline disable while the line
is off. Test covers the off state, the explicit chat channel, the consultant and the default.

**Stage 3 — a checkbox in a `.form-field` is not a text field** (`fff4b01`). One rule exempts
checkboxes and radios; `.form-check` is the row a checkbox sits in when the caption above it is the
field's name. ClientDrawer's VAT Registered box had carried a nine-property inline workaround for
the same reason; it takes the class now.

**Stage 4 — phone layout** (`4eeb605`). `.form-grid-2`/`-3` collapse under 768px, product-wide. The
Settings tab strip is `.panel-tab-bar` + `.panel-tab` with tablist semantics and a roving
`tabIndex` (arrows, Home, End), the shape Help.js already had; the content sits in a labelled
tabpanel.

**Stage 5 — copy that is true** (`1421405`). The subtitle had named Branding and Property, which a
client cannot open, and Thresholds, which an admin cannot; it names the reader's own tabs now.
"Blank keeps the legal support address" meant the Terms page and says so. The preview renders the
FORM, not the row, so it carries an amber "Unsaved — clients still see the saved version" chip while
the two differ. "— pick a client first" left the heading; the paragraph already said it.

**Stage 6 — the preview is a specimen** (`1d02c84`). On Dark `--theme-bg` is also the input well;
the read-only box now sits on card colour with a dashed edge. Full production build verified.

**Left as decisions.** The two intro paragraphs run ~141 characters per line and stay that way: the
density rule says no reading measure inside the app. The 14px uppercase `<h3>` card title and the
h1→h3 heading skip are the page-wide convention on every Settings tab, not this tab's to fix alone.
The `.impeccable/design.json` sidecar was not regenerated; `.form-check` is documented in DESIGN.md.

---

### S683 — 2026-09-06 — the fifth product-wide critique, and the close that kept one of its three promises

**Service worker `crest-v198` → `crest-v205` across steps 1–6 and the polish pass. No migration, no Edge Function deploy.**

`/impeccable critique all modules, features, pages & branches` — dual-agent, all 97 routes, with
"branches" read as the conditional variants inside surfaces (plan, role, module flag, outlet,
page-internal, state) since `master` is the only git branch — scored **29/40**, 1 P0 / 5 P1.
Snapshot at `.impeccable/critique/2026-09-06T05-59-39Z__crest-suite-all-modules.md`. Not a
regression from 34: the prior pass's two P1s are verified closed (inline `stat-grid` overrides 0,
period close preflighted), and the points came off four heuristics previously scored 4 on
**claims that measurement did not support** — most consequentially "NPR/en-NP throughout":
`new Intl.NumberFormat('en-NP').resolvedOptions().locale === 'en'`, so 261 sites format
`1,248,650` believing they format `12,48,650`, and the only Nepali-grouped figure in the product is
the one on the subscription agreement. The detector returned 7 findings, all false-positive, and
has a bug: `--no-advisory` neither filters advisory findings nor changes the exit code. B measured
the public routes flawless (0 contrast failures over 728 text nodes, both presets, both viewports)
and the shared classes failing in Light — 23 accent-as-text sites at 3.27–3.61:1, 323 `rgba()`
tints frozen to Dark-preset RGBs — because Dark declares no `*Text` keys, so base and variant are
byte-identical in the preset everyone develops in.

Decisions taken at the close: P0 first; **money moves to Indian grouping**; everything including
the P2s. Fixed in file-partitioned steps, each with its own build + full test run.

**Step 1 — the P0, which turned out to be bigger than its sentence.** The close dialog promised
*"entry pages become read-only"* and HR has zero closed-period checks against 8 IMS files that do.
The obvious fix — an IMS-style lock on HR — would have been wrong: HR does its month's work
*after* the stock month closes (count on the 1st, payroll finalized by the 5th), so a lock would
block every HR client's payroll run. HR's lock is Payroll Run's own finalize; the sentence was the
defect. Reading the close to fix the sentence found the real one: **the Dashboard's "End Bhadra &
Start Ashwin →" — the button an Owner actually presses — updated the status and inserted the next
row and did nothing else.** No opening-stock carry-forward, no frozen report, no closing-count
preflight (S613's gate had landed on `Periods.js`'s three paths only). The IMS module guide had
noticed and taught *"always close from Periods"* as advice. Every month closed from the Dashboard
before today opened with no opening stock and no snapshot until first view.

Now one routine: `src/pages/periods/closePeriod.js` holds both preflights (closing count, and a
new **payroll-finalized** note that is amber when payroll is open, because the frozen report then
carries an estimated labour cost until Regenerate), the carry-forward, `performPeriodClose()` —
never throws, records each stage's failure, later stages still run, the close itself the one
exception — and `closeFailureText()`, whose `close` branch says *"may not have closed"* because a
dead fetch proves nothing. `CloseConfirmBody` renders every close dialog's notes. Four asks, one
commit; the Dashboard's dialog now names what locks (*Purchases, Sales, Stock Count, Overheads … HR
pages stay open*) and its error is a consequence sentence instead of `err.message`. `Periods.js`'s
"Report is ready" banner renders only when the report was actually saved, and its admin close now
surfaces a failed carry-forward instead of discarding it. 15 new tests; the `appVersion` guard
caught the half-bumped cache version. 59 suites / 666 tests. Rules: `closed-periods.md` (the
HR-not-locked decision and the one-routine rule, with the guide-as-tell lesson), `owner-report.md`.

**Step 2 — money, in lakh and crore.** `src/shared/nepalMoney.js` is the one place a number
becomes a rupee figure: `npr` / `nprInt` / `npr2` / `nprExact` / `nprOrDash` / `nprWords`, on
`en-IN` — the locale whose Latin-digit grouping is Nepal's (`ne-NP` prints Devanagari digits).
The sweep was mechanical and asserted: **48 local formatter definitions across 46 files replaced
by aliases** (`const fmt = nprInt`), so not one call site changed; **243 inline `en-NP` literals
locale-swapped** with their options untouched; **129 bare `toLocaleString()` calls given the
locale**, with the three on a `new Date(` construct skipped by rule; 95 files. Shapes with a
different rounding mode (`Number(v)` under `maximumFractionDigits: 0` rounds half away from zero,
`Math.round` rounds half up) were locale-swapped and left local rather than consolidated — a
paisa on a negative variance is not worth a tidier file. The POS bill and the shift slip, which
formatted every figure with `.toFixed(2)`, now group too: an IRD tax invoice no longer reads
`342500.00` under "Three Lakh Forty-Two Thousand Five Hundred only". `nepalMoney.test.js` reads
the source and fails on any `en-NP`/`en-US` money format or any bare `toLocaleString()` off a
Date, so the class cannot return one call site at a time. Every figure in the product now looks
different, which is why this was a decision before it was a sweep. 60 suites / 673 tests. Rule:
`design-system.md`, beside the `nepalTime` one it mirrors.

**Step 3 — the guards that failed open.** Four, each a rule the repo already had, live in a place
the rule's own sweep never reached. (1) `depleteManualSales()` deleted the day's manual movements
and *then* read the POS-supersedes guard with its error dropped — a dead connection gave an empty
index, every manual row counted as not superseded, and a recipe POS had already depleted was
depleted again. It reads first now, stops on a failed read or a refused delete, and leaves the
previous depletion in place; four branches pinned in `depleteManualSales.test.js`. (2)
`runForecast()` dropped the error on all four of its reads and spelled every order id and every
period id into one `.in()` URL; each read is chunked, paged and throws into the run log it already
had. (3) `KITCHEN_TEAM_ALLOWED_PATHS` — "fail-closed, so a future POS page is hidden from
kitchen/bar by default" — was fail-closed for the sidebar and nothing else, because `posTeam` is
not a `min*Role` tag and the audit greps for those; a kitchen PIN account typing `/pos/shifts` got
the cash drawer. One predicate now, `src/shared/posTeamAccess.js`, read by the nav, the palette and
**`ModuleGate` on every POS route**, so the sentence is finally true. (4) `/menu-pricing` had no
`ModuleGate` at all (`anyOf={['ims','pos']}` now, for a route two modules share) and `App.js` had
no `path="*"` — `NotFound.jsx`, nested inside the Layout group where a signed-in reader keeps the
sidebar and a signed-out one is already on `/login`. 62 suites / 682 tests. Rules:
`access-control.md` (the sixth recurrence, with why the audit missed it), root `CLAUDE.md`
pointer, `supabase-sql.md` beside `save_sales_day`.

**Step 4 — four gates, one grammar.** `ModuleGate` was three silent redirects; `SuiteGate`'s
module-missing card said "Contact your consultant" with no contact; `PremiumGate` headlined the
plan and buried the feature; every upsell sent a signed-in reader to a `/pricing` that read no
session and showed them "Login →". Now: `ModuleMissingCard` behind both `ModuleGate` and
`SuiteGate` — names the module, says the data is untouched, gives the owner the support buttons
and tells a staff login who can act instead. `PremiumGate` says *"Variance Report is on the Growth
plan"*, with the "also adds" sentence derived from `src/shared/featureCatalog.js` — the tier grid
moved out of the admin modal into a data-only file (the modal shrank 436 → 359 lines and imports
it), and `featureCatalog.test.js` reads `App.js` and `Layout.js` so a routed key with no label
fails the suite. The sidebar teaser and footer chip render for the owner only. `/pricing` reads
the session: "Back to Crest →", "Ask about Growth" (a `mailto:` with the plan in the subject), and
a closing banner that asks what to switch on rather than selling a trial to a paying customer.
**Left open on purpose:** `SUPPORT_PHONE_RAW` is still `[[NEEDS VALUE]]` — the number is not
mine to invent; one constant, six surfaces. Rules: `access-control.md` ("Four gates, one
grammar"), `support-contact.md`.

**Step 5 — the Light preset.** Two classes, one cause: Dark declares no `*Text` keys, so base and
`-text` are byte-identical in the preset everyone develops in and every misuse renders correctly
there. (a) A scanner that reads `color:` VALUE expressions — ternaries and wrapped lines included,
which is what a property-level grep kept missing — found 41 base-token sites; 28 of them are the
`color` key of a fill map with its own `textColor` (Overheads, the attendance statuses, the tier
grid, `STATUS_TINT`, whose consumers already override `color` with the variant) and four are the
brand-mark icons, graphics at 3.3–3.6:1 against a 3:1 floor. The 13 that are type — QtyInput's
result badge, SearchableSelect's selected option, PayslipBody's figures, HrReports' "⚠ missing",
ClientDrawer's plan badge and status lines, Settings' cycle tabs, AuditLog's area glyph — moved to
`-ink`/`-text`. (b) **334 `rgba()` tints frozen to the Dark preset's RGBs**, in 50 files, converted
by a scripted sweep to `color-mix(in srgb, var(--theme-x) N%, transparent)` so they re-tone with
the preset; skipped by rule: `:root`'s own `--theme-focus-ring`, `@media print`, the `var(--x,
rgba())` fallbacks in the boundary/loading screens, print HTML builders, the guest menu. Verified
after the write: no `color-mix` within two lines of a Recharts series or SVG attribute, residue is
the three deliberate literals, and the bundled detector still reports the same 7 known false
positives. The design-system rule that had *prescribed* the frozen rgba convention is rewritten in
place, with why the hook could never have caught this (a Dark hex IS a documented colour). 63
suites / 685 tests.

**Step 6 — the P2s, three of which dropped on re-measurement.** Re-measured before fixing, per
the campaign rule: **"18 mouse-only `<tr onClick>`, 0 with tabIndex"** was the wrong test — the
codebase's own pattern puts the keyboard control in a *cell* (`RowDisclosure`, `.btn-linklike`, a
button), and all 16 real rows already carry one (`PaySetup`'s Edit button was simply beyond the
grep window); **the two 15px checkboxes** are already under S682's `!important` coarse floor
(`Layout.css:1404`); and **the three "unlabelled selects"** all carry `aria-label` after their
`onChange` — the scanner had stopped at the `>` inside `e => …`. That last one was my scanner too,
and it was flagging 94 controls until it learned to walk balanced braces; the real count was 56,
of which 6 are component internals, a wrapped checkbox and two doc comments. **50 controls across
26 files now have an accessible name** (search boxes, per-row quantity/price/par inputs with the
row's name in the label where the row is in scope, select-all checkboxes, settings fields),
applied by a script that asserts each site once and re-scanned to a residue of exactly the six.
Fixed as found: `GroupDashboard` no longer renders *"No outlets in this group"* under *"nothing
here is a real figure"* (table and both admin panels gated on the read); `ConsolidatedPnl`'s
Labour line says *no labour entered — Crest HR is off and Overheads has no Labour line* instead
of an empty annotation, with a banner above the figures; the Owner Report names the modules that
were off when it was generated; `MenuPricing` gets a real `loadError` → `ReportLoadError`
(three reads and the ingredients read all rendered a failure as *"No menu items yet — use + Add
Item"*), with the ingredients `.in()` chunked; `TaxPoolTab`'s repair-expense read stops
understating the Section 16 cap on error; the KDS gets an empty-board sentence and stops printing
`error.message` to a cook; Festival Allowance and Incentive Run announce a failure as `alert`,
not `status`; the legal §-chips get the 24px floor; and the Suite comment says six features, which
is what it lists. **Deliberately left, and recorded here so they are decisions rather than gaps:**
Holiday Calendar lets a Staff-rank HR account delete a gazetted holiday (a rank decision for the
owner, not a sweep); the bill line-total box stays a plain `<input>` rather than `QtyInput`
(swapping it changes what a receiver can type into the one box that must match the supplier's
paper); Crest Staff carries no `<Tip>` (content, not code); `SUPPORT_PHONE_RAW` still needs the
number. 63 suites / 685 tests. Rules: `dashboards.md`, `report-pages.md`.

**Polish (`/impeccable polish`) — the pass that rendered what the six steps had only built.**
Driven against the dev server on the Owner session, read-only, Dark and Light at 1280. Everything
new rendered as intended — `/pricing` signed in ("Back to Crest →", "Ask about Growth →", the
switch-on banner), the NotFound page inside the shell, the KDS empty board — with three finds:
the pricing hero still wore its "7-day free trial" chip for a paying owner (hidden signed in); the
KDS kept three "—" column placeholders under the new sentence (dropped when the board is empty);
and the one the six steps could not have caught without a browser — **on Light, text on a tint
over the PAGE ground**. A probe of the real `.badge-*` classes measured amber **4.19** and purple
**4.33** on the page (both pass on a card), and the sidebar's subscription chip 4.34 on the darker
sidebar ground. S608 had tuned Light's `redText`/`amberText` for colour-blindness and for the card;
`purpleText` had never been tuned at all (it was the bare base). The three `*-text` variants moved
one step darker (`#116b33` / `#964900` / `#6d28d9`), chosen by measuring four candidates each
against every ground in the product and re-checking S608's constraint — amber vs red stays ΔE
13.5 deuteranopia / 17.6 protanopia, floor 8. The subscription chips mix with `--theme-card`
instead of `transparent`, so they are opaque pills that read the same on the sidebar as on a
card. Confirm round on the built page: five badges 4.99–6.55 on the page ground, 28 tinted text
nodes with no failure, chip 6.11. Diff scanned for churn (none), theme restored, screenshots
deleted. 63 suites / 685 tests.

**Settings → Support — the support contact becomes data the admin edits, not a constant a
commit sets.** `SUPPORT_PHONE_RAW` — the P1 the critique ranked sixth — turned out to be a
business fact (Bloom Hospitality has no landline; the founder offered their own mobile "for now"),
and a fact that will change again when an office line or an IT desk exists. So instead of filling
the constant and waiting for the next commit, the contact moved to `settings.support_contact`, a
jsonb column on the **platform row** (`client_id IS NULL`) — the one row `settings_select` already
lets every reader including `anon` on `/login` SELECT and only `is_admin()` UPDATE, so no policy
changed; migration `20260906120000` adds the column and asserts that arm of the policy still
stands. Four decisions taken with the founder: **fixed channel slots, not a list** (mobile,
landline, WhatsApp, Viber, email, website); **no social handles at all** — a crashed till does not
need a TikTok link; **hours text plus an emergency switch** naming which line is answered outside
them, so "outlet-down issues any time" is a promise the business toggles rather than a sentence
the code asserts (it defaults on, mirroring the S673 string, so filling the row never withdraws
it); and **one Support tab with two sections** — Crest's line for everyone, then the viewed
client's consultant override — replacing the per-client Contact tab. Viber sits beside WhatsApp
because it is Nepal's household default for free calls (10M+ users; deep link
`viber://chat?number=977…`). `resolveSupportContact()` is the one merge — consultant → platform →
constant floor, a consultant phone replacing the whole phone family — and it is pure, so
`supportContact.test.js` asserts the precedence rather than each surface re-deriving it.
`SupportContactLine` gained "Call office", Viber and the emergency sentence; the Help page's
hardcoded hours `Tip` went, since it would now drift from the setting. A seventh slot, **AnyDesk**,
was added on the founder's ask and deliberately shaped the opposite way from the others: it is
Crest's own ID/alias, shown on Help → Support as "install AnyDesk, send us the 9-digit address it
shows, and accept only a request from *crest@ad*" — never an `anydesk:` deep link, which would
open a session onto Crest's machine. AnyDesk shows the requester's alias in the client's accept
dialog, so publishing it is how a client tells Crest from an impostor. The founder's mobile is the
floor under the row. **Migration applied and verified live 2026-09-06** (`support_contact jsonb`
present; the `settings_select` assertion passed). Service worker `crest-v206`.

**Then a screenshot of `/login` caught the merge lying.** The footer read `9803727572 ·
xrestha@gmail.com` — neither the Support tab's values nor the floor. They were the platform row's
old `contact_phone`/`contact_email`, written by the per-client Contact tab whenever an admin used
it with no client selected, and signed-out `SettingsProvider` loads that very row as `settings`,
so the hook read them as a *consultant override* and let them outrank the Support tab. Fix in two
halves: `useSupportContact()` passes a row as `client` only when it has a `client_id`; and
`platformSupportFromRow()` treats those columns as what they always were — legacy platform values
— seeding the Support form from them when `support_contact` has never been saved, and ignoring
them once it has. So the tab opens showing exactly what clients currently see, and the first Save
retires the pair. Pinned in `supportContact.test.js` with the real numbers from the screenshot.
Service worker `crest-v207`.

**A second screenshot asked the two questions the tab had not answered.** "Why is this number
not editable?" — the emergency dropdown showed `Mobile — +977 971 459 8771` while the Mobile box
above sat empty with an example placeholder, so the number a reader could see lived in no field
they could type in: it was the built-in floor. The Mobile field now says so in its own placeholder
and hint (*Blank — clients see the built-in number …; type here to replace it*), and the checkbox
row stops inheriting `.form-field label`'s caption weight. "Where do clients find support?" — six
tabs into Help, with no URL that could name it. Help now reads `?section=` (`HELP_SECTIONS`, else
the guide) and follows it on change, and the sidebar's bottom rail has a **Support** button
(`LifeBuoy`) beside Help that lands on `/help?section=support`. Service worker `crest-v208`.

---

### S682 — 2026-09-06 — the product-wide audit, and the ~230 dropped errors behind its score

**Service worker `crest-v194` → `crest-v198` across the session's twelve pushes. No migration, no
Edge Function deploy.**

`/impeccable audit` over every route — 95 reachable as an Owner plus the four signed-out and guest
surfaces, all four module trees, the shell and shared components — scored **13/20** (Acceptable):
Accessibility 2, Performance 3, Responsive 3, Theming 3, Implementation Integrity 2. **P0 0 · P1 19
· P2 66 · P3 34**, 119 verified with ~45 leads ruled out. Evidence was the bundled detector (0
failures), four isolated source-audit agents checkpointing to disk, and a live Playwright sweep of
all 95 routes in three passes (Light and Dark at 1280, Dark at 390 with a coarse pointer) measuring
overflow, alpha-composited contrast, accessible names, landmarks and touch targets across ~24,000
text nodes. Zero console errors on 285 route loads and zero horizontal overflow at 390 px. The report
is the first file in a new tracked directory, `.impeccable/audit/`, beside `critique/`.

**What held the score at 2 was one shape repeated ~230 times**: `const { data } = await …` with no
`error` (109 sites) and bare `await scopedUpdate/Insert/Delete(…)` (122 sites). CLAUDE.md already
names this class three ways; the S654 sweep fixed `PosOrders.jsx` and stopped, the S613 sweep fixed
report renders and stopped at write and guard paths, and each of the four groups found it again
inside a money or safety path. The fix ran as the audit recommended — file-partitioned, one build
and one test run per step — across ten pushes.

**Step 1 — the money paths and the keyboard blockers (`1c41839`).** Payroll finalize and reopen,
leave approve and decide, Stock Count autosave, purchase deletes, the sub-recipe delete guard, the
period-close carry-forward, the KDS poll, Table Management's settings loaders and HSC save, and
delivery settlement all destructure `error` now and say which ledger did not move. Finalize used to
run five ledger writes bare and report "Finalized"; Stock Count's autosave was delete-then-insert
with a `.catch(() => {})`, so a refused insert after a successful delete erased the prior wastage
figure while the cell read as saved; the KDS poll blanked the kitchen board on one failed read.
PaySetup gets an Edit button (the row click was the only way to set pay, so a keyboard user could
not), Help's Getting-Started headers are real disclosure buttons, PayrollRun's TDS and TADA inputs
are named, the employee status chip takes the `*-text` variant (3.99:1 on Light before), and
`hr_leave_requests`, the roster's leave guard and the TADA list are paged.

**Step 2 — the Light-preset text sites, the unnamed till inputs, and the report error card
(`8d882c4`).** Twelve measured AA failures on Light were one pattern: the base accent or a chart
swatch used as text — Pricing 3.1–3.35:1, Settings tabs 3.27, HR Dashboard KPI 3.61, the Owner
Report 3.61, the Owner Dashboard legend 1.9–2.5 at 10 px. Each takes `accent-ink` or a swatch now.
Sales stopped dimming unsold rows with opacity (2.5:1); the VAT toggle's "No VAT" label stopped
wearing the border token (1.44:1); Covers' Recharts tooltips use `TOOLTIP_CHROME`; both hand-rolled
switches get a slate off-track and a card-coloured knob, a rule now in `DESIGN.md`. The Tax Invoice
buyer block, the POS menu search, line notes, the discount field, the shift denominations and the
void checkbox carry names. The sidebar search meets the 44 px floor. And **`ReportLoadError` now
converts at render**: every one of its ~70 callers handed it either the error object or the raw
`error.message` string, so a report body read "TypeError: Failed to fetch"; a report is only ever
read by the operator, so the audience decision the call-site rule protects is the same at every
site, and converting once in the component closed all of them. `error-messages.md`,
`report-pages.md`, `DESIGN.md` and the sidecar carry that rule and the new `readout` type step (the
48 px till keypad readout, named so it stops living as a literal; the 34 px estimate beside it was
drift and is 32).

**Step 3 (`597a88b`).** `OutstandingPayables.js`: the second write of each two-write settlement
(stamping or clearing `purchase_entries.paid_at` after the payment rows have committed) can fail on
its own, and the payment is real at that point, so a page-level warning names the state the bill is
in ("recorded, but could not be marked as settled — it still shows under Outstanding with nothing
remaining") rather than a refusal; payment deletes ask through `ConfirmModal` with the amount and
the bill; tab loads go through `useLatestRequest`; every inline red `<p>` is an `ActionError`; the
read errors reach `ReportLoadError` as objects. `viewPosBill.js`: a failed items, payments or recipes
read wrote a blank "VIEW ONLY" invoice with zero lines and NPR 0, indistinguishable from an empty
bill — it writes one failure line in the popup now.

**Step 4 — the two half-edited files, finished (`320f652`).** They were left uncommitted at the end
of step 3: `ShiftSettingsPanel.jsx` had its `INP` style constant removed while eight inputs still
referenced it, and `Periods.js` had only the imports for its conversions. Both were inspected
before being trusted rather than resumed on faith. The shift panel's inputs wear `.form-input`, its
Delete goes through `ConfirmModal` naming what a deleted shift type does to the roster (every cell
using it goes blank, and the hours drop out of the labour forecast), and every write carries its
error with the editor staying open on failure. `Periods.js`'s **ten `window.alert`s became one
page-level notice** — `ActionError` for a failure, a `role="status"` card with a Dismiss button for
a result — and its two list loads render `ReportLoadError` instead of the empty state, on the page
every empty IMS page links to. **The audit's own recommendation, build after each file, is the rule
that would have caught the half-edit before a session ended; every step below follows it.**

**Steps 5 and 6 — HR (`4b7f208`, `50b8fdb`).** Two new shared pieces came out of it.
**`useConfirm()`** (`src/shared/hooks/useConfirm.js`) is the consequence dialog as one line per
page: `ConfirmModal` had existed since S575 and been adopted only where a critique named the page,
because every adopter hand-rolled the same pending/busy/run state — that boilerplate was the
adoption tax, and 24 `window.confirm`s stood in HR alone. **`errorLine(err)`** is the one-string
form that keeps the detail, for the pages on the `setMsg('error:' + …)` convention that have no
`ActionError` slot. Across all eighteen HR pages: every read keeps its error and says the list is
from the last successful load rather than painting an empty queue; every write and inline edit
names the figure that did not land and reloads the stored one; deletes of an OT entry (approved OT
is pay), a holiday (it drives the 2× rate), a loan ledger row, a shift type, an employee, a login
and a TADA claim ask through `ConfirmModal`, as do Recompute (wipes manual edits) and
Finalize/Reopen. `EmployeeForm`'s doubled "Delete? / Are you sure?" became one dialog whose
pre-check **refuses on a failed count** rather than reading null as zero. Payroll Calculation and
TADA rows carry `RowDisclosure`, so the drill-down is reachable by keyboard.

**Step 7 — IMS (`e366293`).** Eleven irreversible actions moved off `window.confirm`: FORCE DELETE
(which removes an item *and* every purchase, count, wastage and recipe line referencing it),
deleting a period's whole `stock_movements` ledger, resetting all par levels, a permanent vendor
delete, a non-draft PO, a bill, a recipe, a staff login. The assets tabs **refuse to compute on a
failed read** — the depreciation and tax-pool previews were deriving every opening NBV from cost
and every opening WDV from zero when their prior-run read failed, which reads as a first-ever run
and could be *posted* as one. Food cost stopped having three definitions: Recipes' live form and
Monthly Summary's headline were local `30/38` and `35/45` ternaries beside an imported `fcBand`, so
one month read green on one page and amber on another; both go through `fcFigure(settings)` and
carry the ✓/△/▲. The seven hand-rolled overlays in `MenuPricing` and `ImsStaff` are on `Modal`.

**Step 8 — POS and the last report guards (`837497a`).** The twelve period-driven IMS reports that
still had no overlapping-load guard now claim the page before their first await — including VAT and
Non-VAT, which are statutory filings whose label is also the print title and the workbook scope
line. In POS, deleting a table (its reservations cascade away with it — the two FKs behave
oppositely and neither is visible from the button), a loyalty scheme (naming how many customers
stop earning), a POS login and Clear-all-occupied-tables ask properly; `PosStaff`'s settings guard
uses `maybeSingle` so a failed read no longer writes a second settings row. **The floor view's six
`window.alert`s were deliberately left**: `setMsg` renders only in the order-screen tree, so the
floor has no banner, and each is a refusal a waiter must not walk past mid-service. S616 chose that
on purpose; the audit called them unstyled, and being un-missable still wins. The file now says so.

**Steps 9 and 10 — the shell, admin and dashboard (`556b0b4`, `bd1d16b`).** `SettingsContext`'s
three write paths carry their guard-read error, so a failed read can no longer fall into INSERT and
write a **second settings row** for the client — the S613 trap, in the context every settings screen
goes through. Monthly Owner Report claims the page before its await. Admin Clients and Audit Log
render `ReportLoadError` instead of "No clients yet" and "No entries found", and the client-list
failure also stops the deactivation sweep running against an empty list. Both dashboard sales
loaders now throw rather than returning an empty list, so the Sales by Category card and the Sales
Mix pie say they could not be built instead of "No sales recorded yet this period". Help's locked
features are labelled **"Not on your plan"** rather than dimmed to `opacity: 0.45`, which had put
the names of the features an owner would upgrade *for* below AA. The "wants to subscribe" dot moved
to a class — and took `.module-tab-dot` with it, which had never been in the reduced-motion block
either, so the sidebar badge had been pulsing indefinitely for anyone who asked their OS for less
motion. The dead CRA `src/App.css` is deleted. The coarse-pointer checkbox rule gained `!important`: it had been shipping at 20px for
every checkbox **except** the eleven that set an inline 15px, which is exactly the set that needed
it.

**Step 11 — the last raw messages and the three irreversible bulk confirms (`7080c6e`).**
`PosTableManagement`'s seven remaining `error.message` sites became `errorLine()` with a consequence
sentence apiece — a failed delivery-partner save now says settling a platform bill still uses the
previous commission rates, rather than showing a PostgREST code. Settings' three code-renumber
buttons ask through `useConfirm`: each regenerates *every* item, vendor or sub-recipe code
sequentially, so codes already printed on past bills, stock sheets and reports stop matching what
the list shows — the fourth Settings confirm, assigning codes to items that have none, keeps
`window.confirm` because it is additive. Audit Log's Clear keeps its load-bearing warning as body
copy in the dialog: the User filter and the search box do **not** narrow what is deleted, so
everything in the chosen range goes, not just the rows on screen.

**Step 12 — two things step 7 walked past in the same file (`deedbad`).** `cancelPo` was the last
irreversible IMS action on `window.confirm`: its own message says *this cannot be undone*, there is
no path from `cancelled` back to draft, and `deletePo` two functions below already asked through the
product's dialog. The comment above it worked **around** the native dialog rather than fixing it —
"Cancel PO 1234?" in a box whose Cancel button does the opposite is a coin toss — and since
`ConfirmModal` forwards `cancelLabel`, the buttons now read **Keep PO open** and **Mark cancelled**,
answering the ambiguity instead of dodging it. Separately the cancelled row rendered at
`opacity: 0.45`, the same defect fixed on Sales in step 2 and Help in step 9: it dims every cell
including the NPR total below AA, and the row already carries a red Cancelled badge, so the dim was
redundant signalling. **No opacity dimming of live content remains anywhere in `src/`.**

**What was deliberately not changed**, so it is not re-found as a defect: the till's six floor-view
alerts (above); `ClientDrawer`'s two-tier escalation, where module-level clears keep
`window.confirm` and whole-client blast radius requires typing the client's name; and the P3
typeset tail — Georgia on Help, the off-ramp sizes on Pricing and Help, and HR's thirteen
hand-rolled `inp` input copies — which is drift, not a defect, and is the cheapest thing in the
report to leave standing.

Twelve commits, each built and tested on its own. 58 suites / 651 tests throughout. The 22
`window.confirm`s left standing were each read once more at the end and are the reversible class
the rule keeps there — routine single-row deletes, additive fills that are editable afterwards,
proceed-anyway warnings, and `ClientDrawer`'s deliberate two-tier escalation.

**Sales Entry's search box now works on all four tabs**, and its text carries across them. It was
on Daily Entry and Daily Breakdown only. Bulk Entry filters by the search alone — that tab has no
category control, and applying a category picked on another tab would hide Bulk rows with nothing
on screen to explain it; a Bulk Save still writes every recipe, because `buildBulkRows` reads
`recipes` rather than the drawn rows. Period Summary filters its rows by the search but keeps
**`% of Revenue` on its category-scoped denominator**, so searching "momo" does not make each
variant read as a share of momo revenue only; its footer sums the visible rows and relabels
itself "Total (N of M items shown)" whenever a search has hidden some. Both tabs gained the "No
menu items match this filter" row the other two already had; the input and its clear button have
accessible names; and the Bulk tab's Category tooltip stopped claiming a category filter that tab
never had. Help and the IMS module guide describe it. 58 suites / 651 tests.

---

### S681 — 2026-09-05 — the Reservations critique: a no-show can be forgiven, and a row shows one next step

**Service worker `crest-v193` → `crest-v194`. No migration, no Edge Function deploy.**

`/impeccable critique` on the Reservations page, dual-agent, scored **29/40** — best dimension
Error Recovery at 4, worst User Control and Aesthetic at 2 — with one P0, three P1s, one P2 and
seven minor observations. The user chose the recommended answers on all three questions (same-day
reversals, one-next-step row with an overflow menu, everything in the report). All of it is in.

**The P0: three terminal states were dead ends, and one was a single unconfirmed tap away.**
`TRANSITIONS` had `no_show: []`, `cancelled: []`, `completed: []`, and "Done" called
`transition(r, 'completed')` straight from a six-button cluster. A no-show is recorded against a
phone number and shown on every future booking form from it; a guest marked at 8:20 who walked in
at 8:25 had no path back. Now `no_show → arrived` and `cancelled → booked` exist, and only those two
(`completed` stays terminal: the bill that closed it is its own record). `stampFor(to, now, from)`
clears the mark the row leaves — `no_show_at`, or `cancelled_at` + `cancel_reason` — so the
customer's no-show count and the Covers Report forget it in the same write; the two-argument form
PosOrders uses is unchanged. **The gate is the booking's own Nepal civil day** (`canRevive`), on the
page rather than in the table: a no-show undone a week later is a record being edited, not a guest
at the door. Both reversals and Mark done go through `ConfirmModal` with consequence copy.

**The row shows ONE next step.** `rowActions()` used to render every legal move as its own
`btn-ghost btn-sm` — a booked row carried six, the irreversible ones interleaved with the routine
ones, and twenty rows on a Saturday was a wall of identical 12px pills. Now: 💬, the most likely
legal move (Confirm → Arrived → Seat), and **`RowMenu`** (`src/components/RowMenu.jsx`, new) — a
real `aria-haspopup="menu"` control with a portalled `role="menu"` panel, arrow/Home/End/Escape
handling, focus return, and outside-click close — holding Arrived/Seat as alternatives, Mark done,
and the two danger items under a separator. Requests keep Accept + Decline inline (three controls,
fine). Decline is `btn-danger` alone, not `btn-ghost btn-danger` stacked.

**Words written for one moment were being shown at another.** Declining a request reused
`CANCEL_REASONS`, and that reason is rendered on the guest's phone — "Guest cancelled" shown to a
guest who asked and was refused is false. `DECLINE_REASONS` (No table at that time · Closed that
day · Party too large for us · Other) is picked when the status is `requested`. And 💬 was returned
for every status with no actions, so a completed or no-show guest could be sent "your table is
booked for…"; it now appears on live rows only.

**Every move now says what it did.** `transition()` ends with a `role="status"` line —
"Sharma ×4 confirmed." — and when the row belongs to a day other than the one on screen (an
accepted request usually does, since requests are unfiltered by day while the list is bounded to
`dayIso`) the line says where it went and offers **Show that day**. Before this the accepted row
simply vanished, and the host could not tell Accept from Decline from a failed write.

**The day list polls too**, every 30s, paused while any dialog is open — a colleague's "Arrived" on
the door phone reaches the till without anyone acting here. `load()` now uses `setIfChanged` on
every setter (rows, tables, open-table ids, settings, hours), with a row signature covering every
field the row draws including the nested table assignment, so the usual answer of "nothing moved"
costs one string compare rather than a table re-render; a failed quiet refresh keeps the last good
page instead of swapping in the error card.

**Words and the hidden note.** "Guests" → "Party" (both tables) and "Party size" in the form;
"Covers booked" → "Guests expected", "Via" → "Booked by", "Booked covers by hour" → "Guests
expected by hour". Occasion is a `badge-yellow` chip beside the name and the first 60 characters of
notes ride the `.cell-sub` line with the phone — "cake at 8" no longer needs a click on the night —
with the disclosure kept only when there is more than the row shows. The Time cell's three inline
sizes became one `.cell-sub`. Phones render through `normalizePhone`, so one column stops showing
three shapes of the same number. The subtitle and the 39-word title tooltip came down to PRODUCT.md's
two-sentence constraint. An arrived party waiting past the grace period wears `△ waiting N min`
(`waitingMinutes`) — `isLate` stops the moment a party is marked arrived, and nothing had picked the
clock up after it.

**The shell caught two more.** `ConfirmModal`'s Cancel was a bare `className="btn"` — S678's
UA-chrome bug in a shared component, reachable from every confirm in the product including this
page's No-show dialog; it is `btn btn-ghost` now. The empty state is on `.empty-state` and says what
to do: **+ New booking**, and either "requests from your booking link appear at the top" or a link
to Tables → Reservations to switch the link on. "set table capacity in Tables" is a link. The
request band lost its inline amber border (the badge carries the amber; a queue is not a notice).
"Next 7 days" — the owner's natural view — gets **Hours over seats this week**, one amber chip per
day with an hour over the room's seats, each opening that day; the hour strip cannot sum across
days, so it answers the one question it is for.

Help and the POS module guide name the new row shape, the reversals and the decline list.
`RowMenu` is in `component-library.md`; the ladder rules are in `pos-billing.md`. Code-level only
again — no dev server was up.

**The design sidecar caught up in the same session (`/impeccable document`, sidecar only).** The
S668 assertion pass ran first and came back clean — 51/51 colours with a `colorMeta` whose canonical
equals the frontmatter, 17/17 type roles, no `PRESETS.dark` mismatch, 27/27 do's and don'ts — so the
narrow path was sufficient: two components (Small Ghost Button with its `aria-busy` state, Row Menu
shown open and static) and three `components`-section rules, appended rather than re-extracted, with
every other count asserted unchanged across the edit. `DESIGN.md` untouched. 58 suites / 651 tests (four new on the ladder: the two reversals, `stampFor`'s clearing, `canRevive`'s day gate, `waitingMinutes`, and that no decline reason is a cancel reason).

---

### S680 — 2026-09-05 — the Reservations audit: a size class that did nothing, and a tooltip that cost every action two Tab presses

**Service worker `crest-v192` → `crest-v193`. No migration, no Edge Function deploy.**

`/impeccable audit` on the Reservations page (`PosReservations.jsx`, `ReservationModal.jsx`,
`SeatTableModal.jsx`) scored **17/20** — zero detector findings on all three files, every colour a
token, every read guarded, the shell classes worn correctly — and every finding that survived was
shared infrastructure the page had inherited rather than anything it did itself. Each was fixed at
the layer it lives on, so the page-local edits are the smallest part of this entry. Code-level only:
no dev server was running, so nothing here was measured in a live browser.

**`.btn-sm` had no base rule.** The only selector in the product mentioning it was the
coarse-pointer `min-height: 32px`, so on every mouse device `btn btn-sm` rendered identically to
`btn` — 13px on 8/16 padding — across 24 sites in 8 files that all believed they were rendering
something smaller. DESIGN.md had described the class as "real" since S678's `.btn`-without-a-variant
rule, which is the same failure one step over: CSS raises no error for a class that does not exist,
and the pair `btn btn-sm` reads complete. It now has one (12px, `5px 10px`, `gap: 4px`, the
`.tab-btn` footprint), and the coarse floor is unchanged. Every Reservations row action, the
day-nav arrows, Legal's nav links, Loyalty's Delete and Table Management's booking controls all
shrink with it, which is what they were written to do.

**`Tip` wrapped around a button was two tab stops, and the wrong one carried the description.** The
component renders a `tabIndex=0` span with `aria-describedby` on the span; around a `<button>` that
is the span, then the button — so a Reservations row with four tipped actions took eight Tab presses
to cross, and a screen reader focused on the actual button never heard the tip. About 60 sites
product-wide. `Tip` now detects a single native interactive child (`button`/`a`/`input`/`select`/
`textarea`/`summary`), adds no tab stop of its own, clones `aria-describedby` and composed
`onFocus`/`onBlur` onto the child, and drops the dashed underline and `help` cursor for that case —
which `Roster.jsx` had already been doing by hand with `style={{ borderBottom: 'none' }}`, the tell
that the default was wrong for buttons. Hover still works on a disabled child, which cannot take
focus at all. A custom component child (react-router's `Link`) is not detectable and keeps the old
behaviour.

**A pressed button that disables itself drops keyboard focus to `<body>`.** Every row action set
`disabled={busyId != null}`, so the button a keyboard user had just pressed vanished from the tab
order and, when the write landed, they restarted from the top of the page. The busy row's buttons
now stay enabled and wear `aria-busy` (`.btn[aria-busy="true"]`: disabled's opacity plus
`cursor: progress`), `transition()` ignores a second press while one is in flight, and other rows'
buttons are disabled as before so only one write can be in flight.

**Page-local, all small.** `Loading…` carries `role="status"` (the shape `LoyaltyTab` already had);
the three filter tabs carry `aria-pressed` and `type="button"`; both tables' empty action header
gets a visually-hidden "Actions"; the table chips in the booking form show their seat count on the
chip instead of only in a hover-only `title` (the host picking tables for a party of eight on a
tablet has no hover — `SeatTableModal` already did this); gaps of 6/10/14/18 move onto the 4/8/16
scale; the seat modal's section eyebrow takes the `data-table th` eyebrow's values instead of its
own weight and a unitless `letterSpacing: 0.4` (= 0.4px); and the capacity strip's 9px denominator
comes up to the 10px floor.

**Left as found, deliberately.** The 7-column table scrolls horizontally on a phone inside
`.table-wrap` — the Guest column absorbs the squeeze and that is the product's table convention.
The `.impeccable/design.json` sidecar was not regenerated: no token value moved, and `.btn-sm` is a
class, not a token. Re-run `/impeccable audit` on the page to see the score move.
58 suites / 647 tests — one of them the `appVersion` parity test, which failed exactly as designed until `APP_VERSION` was bumped with the cache name.

---

### S679 — 2026-09-05 — the Terms and Privacy Policy start on the first phone screen, and open with six plain sentences

**Service worker `crest-v191` → `crest-v192` (app code changed: 6 source files + 1 new). No
migration, no Edge Function redeploy; the two `.md` documents and their hashes are untouched.** An
`/impeccable critique` of `/legal/terms` and `/legal/privacy` — dual-agent, dev server, both
presets, 1440 and 390 — scored **28/40**: 3 P1, 2 P2, nine minors. Every finding then fixed in
seven staged pushes, and the one product question the critique raised was answered with a feature.

**The contract started below the first phone screen.** Measured at 390×844: the header stacked to
three rows (145px, "Sign in" alone on the third), the meta row wrapped to three lines, and the
contents rail — a card of eighteen links — stood 559px, so the first sentence of the Terms sat at
**y = 976**, off the bottom of an 844px screen. A reader who tapped "Terms of Service" met a map of
the terms and, being told "18 sections", closed the tab believing the document enormous (it is
2,517 words; ~10 minutes). Below 900px the rail is now a **closed disclosure** — a real button,
`aria-expanded`, 44px, "Contents · 18 sections" — and choosing a section closes it so the reader
lands on text; the header's three actions share one row (91px). First paragraph at **y = 408**
before the summary box below was added.

**The Privacy Policy's tables stack into cards below 640px.** The purpose table had been 1,175px
tall with 133px rows because three paragraphs shared 338px, and the sub-processor table overflowed
its wrapper by 24px. Each cell carries its column name via `data-label` (the renderer strips the
inline markup from the header for it), the first column is the card's title, and the table
elements carry **explicit ARIA table roles**, because Chrome and Safari both drop a table's
*implicit* roles once `display: block` restyles it — without them a screen reader would read the
sub-processor table as a run of unrelated text. `screen and` on the query, so a phone printing
this still gets a table. jsx-a11y calls the roles redundant and CI treats the warning as an error;
silenced for that block with the reason beside it.

**An "In short" box above §1 of each document.** The critique's first question — should the first
thing a Nepali restaurant owner reads be a contract at all? — answered: six lines for the Terms,
five for the Privacy Policy, each paraphrasing ONE clause and linking to it by section number.
**Page chrome, not document text**: the markdown is hashed byte for byte and every acceptance in
the ledger records that hash, so a sentence added to the `.md` would be a new version, a re-hash
and a re-acceptance for every Owner. The box says so in its own note, and opens with the bridge the
first clause never offered — "Crest Suite is made by Bloom Hospitality Pvt. Ltd." — because the
header names the product and the contract names the company. `src/legal/legalSummaries.js` is
keyed by **doc type AND version**: v1.1 renders no summary until one is written for it, since a
paraphrase of a clause that has since changed is worse than none; the "Adding version 1.1"
checklist gained step 7. Section links resolve from the same contents list the rail is built from,
so a renumbered heading degrades to plain text rather than a link that scrolls nowhere. Not
printed: a filed copy carries the agreement and nothing that could be mistaken for part of it.

**Two colour defects.** `.legal-link` and `.legal-p` were styled only under `.legal-body`, and the
not-found page's two links and the Loading line sit outside it — so on the branch a stale external
link lands on, both links were the UA's `#0000EE` at 16px (about **2.0:1** on Dark; blue beside
visited-purple on Light). The S678 second-brand-colour-by-omission failure, one branch over from
where it was fixed. Bare classes now: **8.26:1** Dark, **5.98:1** Light, 14px like the document. And
the amber banner tint drops 12% → 6%: on the Light preset its text measured **4.19:1** at 13px —
the identical tint-plus-amber-text pairing `PeriodScope`'s docs record failing at exactly that
figure — and is **4.54:1** now, the border carrying the band. Written up in
`.claude/rules/design-system.md` as the general shape: *a class styled only under one ancestor is
half a job everywhere else.*

**The heading outline was H1 → H2 "Contents" → H3 ×18.** The renderer demoted every markdown
level by one for a document H1 that S678 had begun stripping for render, so the only level-2
heading on the page was the rail's label: a screen-reader user walking level-2 headings found a
table of contents and no document. `##` is h2 now; a renderer test asserts the outline. Also: a
**skip link** (23 Tab stops had preceded the first word), `role="list"` on the rail (WebKit drops
list semantics under `list-style: none`), the Copy button named "Copy SHA-256" with a
visually-hidden status line announcing the copy, and the header pill no longer lit on a version
the bundle cannot serve.

**The measure held ~95 characters, not 76.** `ch` is the width of the font's zero, and Poppins'
zero is wide against its average letter, so the 76ch body was 668px and held about 95 characters
per line — past the 75 ceiling, on the one surface in the product that is pure reading. **60ch**
now, on the body's *children* rather than the container (measured **66 per line average, 72 on the
fullest paragraph**), which is also what lets the three Privacy tables take the full 844px column:
the `max-width: none` on the table wrapper had been inert under a capped parent since it was
written. DESIGN.md's two "no reading measure anywhere" sentences now name this exception.

**The rail says which section you are in.** Its 2px transparent left border had been reserved for
this. A scroll listener on `.legal-page` (the page is its own scrollport per S604, so a window
listener would never fire) marks the last heading past the sticky header with
`aria-current="location"` — accent edge, accent ink — at most once a frame; scrolled to the bottom
the last section is current, since the final two or three share one screen.

**Minors.** Two `Legal.css` comments had sized the non-sticky-header decision against "a 16,000-word
contract" (the Terms are 2,517 words, the Privacy Policy 1,700; the decision holds on the real
figure, now stated). The verify panel used the input-well token, which on Dark IS the page ground,
so it was a border with nothing inside it — card token now, at the body's measure. The verify note
goes 11 → 12px. "Verify this document" reads as an instruction to a second-language reader; it is
"Check this is the real text". "Sign in →" and "Sign in" were two spellings of one link. Print
gains "Page n of m" as an `@page` margin box (Chromium prints it; other engines ignore the block).
One critique observation was withdrawn on inspection: Chrome's "Banner not shown:
beforeinstallprompt.preventDefault() called" on the legal route is the *intended* suppression of
its install mini-infobar (S307's `installPrompt.js`), not a prompt firing.

**Verification.** Every figure above was measured in a real browser after the change, at 1440×900
and 390×844, on both presets for the contrast work; the `src/legal` suite is 4 files / 58 tests
green; webpack compiles with no warnings. `pointer: coarse` could not be emulated, so the 44px
floors in the coarse block were read from the CSSOM rather than exercised. Critique snapshot in
`.impeccable/critique/2026-09-05T05-20-54Z__src-pages-legal-jsx.md`; re-run `/impeccable critique`
on `src/pages/Legal.jsx` to see the score move.

**The design sidecar, sidecar only (`/impeccable document sidecar only`, same session).** The S668
middle path, run as assertions rather than a reading pass: 51/51 frontmatter colours carry a
`colorMeta` entry whose `canonical` equals the frontmatter value, 18 mapped tokens equal
`PRESETS.dark`, 17/17 type roles have a `typographyMeta` entry, every `**The X Rule.**` in
DESIGN.md exists in the sidecar, and the do's and don'ts are verbatim. What had drifted since
S668: the Overview's density paragraph (the legal measure), the Brand Lockup Rule's body ("four of
the five surfaces") and the shell rule's body, two rules DESIGN.md had acquired and the sidecar had
not been told about (*the fifth surface is `/legal/*`*; *the print reset targets the CELL*), and the
`.btn-icon` primitive, added as a component snippet with the `.badge-sentence` release valve noted
on the badge entry. **One defect the assertion caught rather than the diff**: the fifth key
characteristic had been truncated at a line wrap when S662 wrote it — *"…the Crest Staff phone
app, the"* — and had survived S668's refresh. 24 insertions, 6 deletions; every other count
unchanged, which is what proves nothing was silently dropped. DESIGN.md untouched.

### S678 — 2026-09-04 — the Terms page stops being unreadable on a phone, and stops rendering its own name in browser blue

**Service worker `crest-v190` → `crest-v191` (app code changed: 12 source files). No migration, no
Edge Function redeploy.** An `/impeccable audit` of the whole legal surface, then every finding
fixed: 1 P0, 3 P1, 8 P2, 5 P3.

The audit scored **11/20**, which reads harsher than the code deserves — the detector found
**zero** findings across all eight files and the reasoning quality throughout is high. The damage
was concentrated: everything wrong sat in the chrome the documents are presented in, and nothing
at all in the prose, the registry, the acceptance ledger or the print stylesheet.

**The public Terms page was broken on every phone, and had been since S672.** Measured at 390px:
`.legal-brand` computed to **0px wide**, and because `.legal-brand-name` carries `white-space:
nowrap` the wordmark painted anyway — from x=64 to x=214, **124px of it directly underneath the
"Terms of Service" button**, with `elementFromPoint` at the wordmark's own centre returning the
button. `.legal-nav-actions` is `flex-shrink: 0`, so it ran to x=424 and lost **34px of "Sign in
→"** to `index.css`'s `overflow-x: hidden`. The one-row header needs **638px** for the default
wordmark (64 padding + 16 gap + 182 brand + 376 actions) and more for a longer one. Below 640 it
now stacks and **stops being sticky**: 84px of fixed chrome is a heavy toll on an 844px screen
when the surface under it is a 16,000-word contract, the contents rail was already static there,
and heading `scroll-margin-top` drops to 12px to match. This is the page the trial consent
checkbox links to, in a market that reads it on a phone.

**`.btn` alone declares no background and no colour, and nobody had noticed.** It is the box —
padding, radius, weight, cursor, focus ring, `:disabled` — and a variant supplies the rest. Three
sites wrote it bare. On the `<a>` for the current document that meant the UA's own link styling:
`#0000EE`, underlined, **1.81:1** against the Dark nav card where AA wants 4.5:1, so the page you
were already on was simultaneously the least legible thing in the header, the only one that looked
clickable, and a second brand colour arrived at by omission. On the `<button>` for "Download the
exact text" — the verify panel's whole point, the control a lawyer actually presses — it meant
`#f0f0f0` on black, sitting on a `#0f1117` panel. Now the accent tint plus accent-ink (the
product's own active-nav treatment, **5.68:1**) and `btn-primary`. **This is the `badge-green`
failure from S576 on the class it was never applied to**, and `btn btn-sm` is the shape that hides
it best, because `btn-sm` is real so the pair reads complete. Written up in
`.claude/rules/design-system.md` and DESIGN.md.

**A versioned URL served a version it does not hold.** `LEGAL_TEXT` is keyed by doc type alone, so
`/legal/terms/0.9` rendered v1.0's full text under a banner disowning 0.9 — and once 1.1 ships,
`/legal/terms/1.0` would render **1.1's text** under "Version 1.0 has been superseded". Both
acceptance tables deep-link there with the version they recorded, so the one link whose purpose is
*show me the text I agreed to* would have shown a different document, correctly labelled, silently.
`loadLegalText(docType, version)` now refuses, and the page renders no body, **no meta row and no
hash** — the hash being the sharp one, since printing v1.0's fingerprint at 0.9's address attaches
this document's identity to another's. That is the safe failure, not the finished one: step 5 of
the "Adding version 1.1" checklist now says to make `SOURCES` a list per doc type and ship prior
text, or the ledger's own links answer with an apology.

**Neither document had a page title.** Both reported `Crest Suite` — WCAG 2.4.2 at Level A, two
indistinguishable tabs, a bookmark of the Terms that says nothing, and no announcement on arrival,
on the most link-shared pages in the product. `GuestMenu` and `GuestBooking` had both done this for
sessions; the legal pages had not. Same save-and-restore shape, composing with `printWithTitle`.

**`Legal.css` had no `@media (pointer: coarse)` block at all.** `Layout.css`'s floors can only
reach selectors they know about, so a stylesheet that names its own controls opts out of the touch
tier silently — measured at 390px, contents-rail links **28.2px tall with a 0px gap** between
eighteen of them, verify toggle 28px, footer links 18px, on the one surface in the product that is
pure reading and pure thumb. All at 44px now.

**And on a phone the reader met eighteen section links before the document's own title.** The
grid hides source order right up until the columns collapse. `.legal-main` is now three explicitly
placed children — head, rail, article — so the rail keeps the left gutter on desktop (spanning both
rows, so `sticky` still has somewhere to travel) and the reading order stays title → contents →
document. Verified `[HEADER, NAV, ARTICLE]`, title at y=165, rail at y=392.

**The legal pages were white-labelled, and in production that meant a client's name on Crest's own
contract.** They read `settings.app_name` like every other signed-out surface — but signed out
there is no client, so `SettingsContext` loads the `client_id IS NULL` row, and the live page
rendered **BHATTI CHOILA** as the brand above a document whose first sentence names Bloom
Hospitality Pvt. Ltd., with `© 2026 BHATTI CHOILA` beneath it and the same name in the running foot
of every printed copy. `PRODUCT_NAME` in `src/legal/index.js` is now the one value for the brand,
the print foot and the agreement's `providerName`; the copyright line is `COMPANY.name`. Same line
DESIGN.md already draws for Pricing's plan names.

**Bare `/legal` rendered a completely blank page** — zero children in `#root`, no text, no error.
The component's own 404 only ever saw an unknown `:docType`, and App.js has no catch-all;
truncating a URL to find "the legal section" is ordinary behaviour. Now a redirect. The 404 branch
also drew no nav and no footer, so the two states that most need the chrome were the two without
it; the shell is drawn once around every state.

**Smaller, all verified.** The badge classes' `text-transform: capitalize` was title-casing prose —
"Do Not Send An Agreement To A Client Until These Are Filled In" on the admin Legal tab, "Draft —
Not Yet In Force" on Help → Legal — so `.badge-sentence` turns it off. `REACT_APP_SITE_ORIGIN` was
in neither `.env.example` nor `README.md` despite being the one variable whose absence prints onto
a contract. The signed-paper date was a native AD `<input type="date">` on the one field where the
date *is* the record, in a product whose calendar is BS — now `BsCalendarPicker`, with BS beside AD
in both history tables via a new `nepalBsLong()` (tested against the registry's own 3 September
2026 = 18 Bhadra 2083). The document's own `# ` title and `**Version …**` line duplicated the page
header one rule below it, so `stripDocFrontMatter()` drops them **for render only** — `text` stays
byte-identical, because that is what the Download button hands over and what the hash was taken
across. `.legal-h1` had no CSS rule at all, rendering the one sans-serif heading among Georgia ones
at `scroll-margin-top: 0`. Sign-in became a `Link`, the rail a `<nav>` + `<ol>` + `<h2>`, Login's
footer legal links `Link`s instead of full-page reloads, and the re-acceptance gate `100dvh`.

**One detector triage.** The two `layout-transition` findings on `Layout.css` (the sidebar collapse,
L361/L863) are a sanctioned exception whose config entries were **inert, not stale**:
`extractFindingIgnoreValue()` returns `''` for any rule outside `directValueRules`, and
`layout-transition` is not in that set, so a per-value entry can never match it — only a
file-scoped wildcard can, which the detector's own comment says. Re-recorded as
`layout-transition=* --file src/components/Layout.css` **with the reason in the config this time**,
which is the S662 lesson. The two 2026-08-18 entries are left in place and are now redundant.

58 suites / **645 tests** (two new, for `nepalBsLong`); `npm run build:verify` clean, no warnings.

**Verified by measurement in a real browser**, both presets, at 390×844 and 1280×900: `/legal`,
`/legal/terms`, `/legal/privacy`, `/legal/terms/0.9` and `/legal/nonsense`, before and after, with
`getBoundingClientRect`, `elementFromPoint`, computed styles and contrast arithmetic rather than by
eye — every figure above came from that. Zero console errors or warnings. **Not browser-verified:**
Help → Legal, Admin → Clients → Legal, the re-acceptance gate and the Subscription Agreement print
page all sit behind a login. Their changes are two class additions, an import, a picker swap and
the shared `nepalBsLong`, which has tests — but they have not been clicked.

**Still open, and not coding problems:** whether Vercel production has `REACT_APP_SITE_ORIGIN` set
could not be checked from here, and until it is, any Subscription Agreement generated prints
`[SET REACT_APP_SITE_ORIGIN]/legal/terms`. Separately, the global settings row carrying a client's
`app_name` is a data condition this change routes around rather than fixes.

**A `/doctor` pass closed the session, and the interesting part is what it refused to move.** The
setup itself was clean — one install (VS Code extension 2.1.260, no CLI and no npm leftovers), every
config file parsing, no plugins, no agent definitions, all five skill frontmatter blocks valid, auto
mode already the default, and hooks well inside their budgets (`PostToolUse:Edit` 87 runs at a
645 ms median against a 2 s guideline, zero timeouts). `taste-skill` was disabled in
`.claude/settings.local.json` — 3 uses ever, none since 2026-07-22, and `impeccable` covers the same
ground 30 times a week.

The real finding was that **`CLAUDE.md` had 391 characters of headroom** against the 53,000 ceiling
`check-claude-size.mjs` enforces inside `build:verify` — one added rule from failing the build. Four
migrations were proposed and **two were dropped on inspection**, because the destination's `paths:`
globs did not reach the rule's readers: `recipe_ingredients has no client_id` is queried from 25
files across variance, the dashboards, `utils/nutrition.js` and TADA, while
`recipes-and-subrecipes.md` covers only `recipes/**` and `stockcount/**`; the purchases qty/rate
convention is read by 20+ files that are overwhelmingly `ims/reports/**`, which
`item-master-rates.md` does not cover. Both would have passed `check-rules-globs.mjs` — the globs
resolve — while silently removing the rule from the files that need it, which is the exact failure
this file warns about. What moved instead was **duplication**: three sections whose full text was
already in `frontend-performance.md`, `component-library.md` and `supabase-sql.md`, each marked
"Migrated from the root CLAUDE.md (S663)" and each of which had kept its body in the root anyway. A
migration that leaves the original in place is the regrowth mode worth watching for. 889 characters
recovered, 99.3% → 97.6%, and the note at the top of `CLAUDE.md` now says five passes rather than
four.

Left flagged rather than fixed: **`.claude/rules/design-system.md` is 74.6 KB — larger than
`CLAUDE.md` — and its globs (`src/**/*.css`, `src/components/**`, `src/pages/**`, `src/modules/**`)
mean it loads in nearly every session that touches app code.** It is lazy in name only. Splitting it
well is its own session's work.

**A documentation sweep closed the session, and the first thing it found was a rule this session had
broken.** DESIGN.md's **Brand Lockup Rule** opened *"All four surfaces that draw the lockup"* and
listed the sidebar plus `/login`, `/pricing` and `/reset-password`. The legal pages are a fifth, and
S678 had just made them the one surface that deliberately does **not** resolve its brand from
`settings` — so the rule, read as written, now described the product incorrectly and would have
taught the next session to white-label a contract. It reads "four of the five" with the exception
stated and argued. **A named rule that gains an exception is not documented until the rule itself
says so**; leaving the exception only in the code and the changelog is how a rule quietly becomes
false. PRODUCT.md's signed-out-surfaces principle gained the same fact from the product side, phrased
to keep its own count honest — the legal pages are a third signed-out surface and explicitly *not*
brand-facing in the sense the other two are.

`docs/CROSS-REPO.md` gained the S678 class-system findings for hss-suite to check: `.btn` declaring
no background or colour, and the badge box's `text-transform: capitalize` title-casing prose. Filed
as *verify*, not as *fix* — the HR module was ported from this repo so its screens carry
Crest-shaped `btn`/`badge-*` usages, but whether that repo's own `Layout.css` has the same gap
cannot be seen from this side, so the entry carries the two greps that would answer it.

**And two numbers in `DOCS-REMEDIATION.md` had moved.** T11 is titled *"Make the `CLAUDE.md` ceiling
mechanical, so there is no fifth `/doctor` pass"* — the fifth pass had just happened, so the title
overstates what a ceiling can do. Recorded honestly: the mechanism is why the pass was survivable,
not why it was avoided, which is worth more than the title claimed. T13 measured the rules corpus on
2026-09-01 and put `design-system.md` at 65,384 chars / 41.1% of the corpus's resident cost. It is
**74,592 today — up 9,208 (+14%) in three days**, ~3,000 of that added by this very session, against
a root file a ceiling holds at 52,004. So the thing T13 recorded as unmeasured is not merely
unmeasured, it is moving — and moving faster than the file the ceiling actually guards, over the
same three-day interval the root file once regrew 7,052 chars in. If any ceiling is worth making
mechanical next, it is that one, keyed on **size × glob reach** rather than size.

---

### S677 — 2026-09-04 — a booking becomes a record, and a customer can scan a QR to ask for a table

**Service worker `crest-v189` → `crest-v190`. Migration `20260904200000_pos_reservations.sql` —
apply in the SQL Editor and run its verification block. Edge Function `admin-user-ops` — redeploy
(two Danger Zone delete lists gained the new tables). Not yet live-tested: see the end.**

Reservations were named a gap in the 2026-07-15 hospitality research and never scoped; the only
trace in the product was `pos_tables.status = 'reserved'`, a label a manager sets by hand with no
guest, time, party or expiry behind it, and neither `POS_TODO.md` nor `POS_DECISIONS.md` nor the
pricing page had ever mentioned a booking. This session researched it (the plan file holds the
market survey: Toast/Resy/OpenTable's status ladder and seat-opens-the-check handoff; Resos'
2.33% no-show index against 5–20% without a commitment and under 5% with deposits; Restronp's
"daily reservation schedules" and RestroX's "Reservation" order type as the Nepali state of the
art; Sparrow/Aakash SMS at ~NPR 1.5 with NTA sender-ID registration; Nepal VAT Act s.16 on
advances) and then built Phase 1 to four decisions taken with Aashish: staff book **and** the
customer-facing booking QR in the first build, request-based; WhatsApp deep link + `tel:` for
confirmations; no deposits; any POS staff rank can book.

**A booking is a promise about a future table, not a table state.** `pos_reservations` (name,
phone with the `pos_customers` generated `phone_canonical`, party, `reserved_for`, expected
duration, status, source, occasion, notes, a transition timestamp per step, `order_id`) plus
`pos_reservation_tables` so a party can span tables. It is DERIVED onto the Orders floor — a chip
on the held table, a quiet strip of today's bookings — and never writes `pos_tables.status`,
which already drifts against open orders. Two CHECKs carry the meaning other readers depend on:
`seated ⇒ order_id` and `cancelled ⇒ reason`; the transition table itself lives in JS with a test
(`reservationStatus.js`), because a wrong status costs a phone call, not money. Same-client RLS
plus the three restrictive staff families; PIN staff deliberately not fenced.

**Seating is the handoff.** Tapping a table whose booking is due (45 minutes before, by default)
offers "Seat Ramesh ×4" instead of the covers numpad; the order opens with covers = party size and
the guest already in the buyer fields, `performSave` writes `order_id` + `seated` onto the booking
right after the `pos_orders` insert (a ref, not state — the same reason `savingRef` exists), and
`closeOrder` completes it alongside the table release, through `warnWrite` so a failed link is a
sentence on the floor rather than a swallowed error. The Reservations page's own Seat button picks
the table and navigates to Orders with the FULL `pos_tables` row, because `table_name` is
snapshotted onto the order and printed on every KOT. Both `PosOrders` additions live in the floor
return — the S578 trap, checked before placing them.

**Durations are measured, not guessed.** The Covers Report's turnover-by-band loop moved to
`coversMath.js` (`coversMath.test.js` keeps a verbatim copy of the old loop and asserts equality),
and Table Management → Reservations shows each band's measured average over the last 90 days with
a one-tap "Use measured" beside the field the booking form prefills from. Booked covers by hour
against the room's seats is a soft warning on the page — never a block; a host will always take
the ninth booking for a forty-seat room, and the strip exists so they take it knowingly.

**The public page copies the guest menu line for line.** `/pos/book/:clientId`, scoped
bone-and-pine CSS, a 14-day chip strip (BS day first, AD beside it), half-hour slots inside
opening hours, name + mobile, and a status card polled by the request's own UUID with the
two-miss stale banner. Three anon `SECURITY DEFINER` RPCs: `get_booking_page` returns nothing
unless `pos_enabled` AND the outlet's own toggle are on; `submit_reservation_request` validates
everything server-side and **returns jsonb rather than raising** for the refusals that should
cost quota — an exception rolls back the attempt row, so a refused request would burn nothing and
the cheapest attack would be to keep getting refused (20260810170000's lesson, which an Edge
Function can apply across two requests and a single SQL transaction cannot); one pending request
per phone (partial unique index), 5 per hour per network and 40 per outlet via
`current_setting('request.headers')`. No phone verification: the staff WhatsApp or call IS the
verification. Requests land in an amber band on the Reservations page, polled every 15 s with a
chime (the fourth inline copy of that chime became `posChime.js`; the three existing ones were
left where they are).

**Two things found on the way.** `pos_customers.phone_canonical` was never in
`restoreClientData.js`'s `GENERATED_COLUMNS`, so every restore of the customer book has been
rejected by Postgres since S545 while the backup looked complete — fixed in the same edit that
registers `pos_reservations`. And `pos_reservations` is the schema's **first table with a real
`updated_at` trigger** (`touch_updated_at()`); `supabase-sql.md`'s "no trigger anywhere" table now
says so, and a new table wanting one attaches that function rather than writing a second.

Also: Covers Report gains a Reservations tab (booked vs walk-in covers, no-show rate, by source,
by hour booked for); Customers gains a No-shows column that renders `?` on a failed read rather
than a clean record; the pricing page's POS list gains the line (the S665 lesson); Help, the POS
guide, `POS_DECISIONS.md` (shipped) and `POS_TODO.md` (SMS, deposits, waitlist, phone
verification as later phases) are updated. `CalendarCheck` is the nav icon, on no other route.
56 suites / 637 tests; `npm run build:verify` clean.

**Applied and live-tested the same day** on BHATTI CHOILA through the local dev server against
the live database, the migration's catalog block returning every expected value first (4 + 4
policies, anon shut out of both tables and the attempts log, the touch trigger present, the RPCs
anon-callable, every reservations index led by `client_id`). The whole loop ran by clicking, not
by reading: a booking for Ramesh ×4 at 6 PM on Table 2 → the WhatsApp button opened `wa.me` with
the template filled in (BS date, AD date, time, outlet) → Confirm → the Orders floor showed the
strip and the Table 2 chip → tapping the table offered "Seat Ramesh Test ×4" → the order opened
with covers 4 and no numpad → Send Order → the booking read **Seated** → the billing modal came
up with Ramesh's name and phone already on the bill → Confirm Payment → **Completed**, table
freed, no floor warning, and Ramesh now in the customer book. Then the public page: the toggle
saved on Tables → Reservations, the link opened with the 14-day BS-first strip and only the
in-hours slots, a request landed as "Request sent", a second submit from the same number was
refused with the guest-facing sentence, an anonymous `GET` on all three tables came back
`401 / 42501`, Accept on the staff page flipped the guest's card to "Confirmed ✓" within one poll,
Decline (reason required) flipped the other to "Not available", No-show recorded, the Covers
Report's Reservations tab read 3 bookings / 50% no-show rate / 4 booked covers, and the booking
form's phone lookup reported "1 no-show" for that number. Two things the live run changed: the
empty-state sentence lower-cased a BS month name, and the settings tab offered a "Use measured"
of 4,681 minutes from test bills left open for days — it now says the average is skewed and
withholds the button past 12 hours (POS_TODO B4 is the real fix).

**Three things the review of the public page changed, same session.** The day strip scrolled
sideways, so day twelve hid behind a scrollbar a phone user never finds — it is a **two-week
calendar** now (weekday headers, cells aligned to their weekday, BS day large with the AD date
under it, all fourteen on screen at 390px). The pills said "Shanibar" after a first pill that said
"Today" in English, so the weekday names are English and the BS date is where Nepali lives. And
"the restaurant confirms by message or call" assumed a business type the product does not know —
every sentence on the page now names the outlet instead, including the server's refusals, which
the page words itself by `code` rather than rendering the RPC's text. The 📞 `tel:` button beside
💬 on every booking row went too: the book is worked from a till or a desk, where a dial link does
nothing, and the number is printed on the row for anyone actually holding a phone.

**And the question the page could not answer: what if the room is full, or closed, or walk-ins
only?** Second migration, `20260904220000_reservation_availability.sql` — **apply it**; the page
falls back gracefully until then (every slot offered, the server still the judge). Three rules on
Tables → Reservations: **closed weekdays**, **closed dates** (picked on the BS calendar — Dashain,
a private function; deliberately not derived from the HR holiday calendar, since most outlets are
open on a public holiday), and **walk-in-only weekdays**, plus a one-line notice under the page
title. A new anon RPC `get_booking_availability` returns booked covers per hour for the next
fourteen days — numbers only — and the page greys a slot as **Full** when, in any hour of the
expected sitting, booked covers plus this party would exceed the room's seats (the staff capacity
strip's arithmetic, in SQL as `reservation_hour_load`). Closed and walk-in days grey out with the
reason on the cell; a day where every slot is full says so and points at the phone.
`submit_reservation_request` refuses all three (`closed_day`, `walk_in`, `full`) so a stale page
cannot slip one through, and a room with no capacity set is never "full" — the host decides at
Accept. `bookingAvailability.test.js` and `reservationSettings.test.js` cover the arithmetic and
the normaliser (closed wins over walk-in; dates de-duplicated and validated).

**Bookings reach the Dashboard.** The POS section of `/dashboard` gains a fifth front-of-house
tile, **Bookings Tonight**: live bookings for tonight's service (today's BS day plus six hours past
midnight, the floor's own window), with the covers not yet seated as the subline — what the floor
should be ready for on top of what Covers Served already counts — and public requests waiting for
an Accept when there are any, in amber, the one state on the tile that is waiting on a person.
It reads the booking, never the order, so nothing is counted twice: a booking becomes covers only
when it is seated, and Covers Served keeps counting the order as it always did. Not on the Owner
Dashboard — that page is IMS+HR by design and POS is its explicit Phase 2.

**Both migrations applied and `admin-user-ops` redeployed the same day.** The second migration
was verified behaviourally from the browser with the anon key against BHATTI CHOILA:
`get_booking_page` returned its twelve columns (24 seats, the four durations, Saturday closed,
Friday walk-in, the notice), `get_booking_availability` returned tonight's 9 PM ×3 booking as hour
21 and hour 22 at 3 covers, the internal `reservation_hour_load` was refused to anon (42501), a
Saturday request came back `closed_day` and a Friday one `walk_in` — both before the attempts row
is written, so the probes left nothing behind. **Still left:** the restore round-trip that now
includes `pos_customers`.

---

### S676 — 2026-09-04 — the acceptance ledger stops writing its dates in an order Nepal reads backwards

**Service worker `crest-v188` → `crest-v189`. No migration.**

Both Legal tabs (Admin → Clients → Manage → Legal, and the client's Help → Legal) rendered the
acceptance date through `nepalDateAd`, which is `en-US` — so yesterday's acceptances showed as
`09/04/2026`. S670 kept that order deliberately for the printed bill, and its comment says so:
Nepal writes DD/MM/YYYY, and flipping a Tax Invoice's date format is "not one to make silently".
That reasoning does not carry to a surface built two days later, where the cell is not a print
habit but the fact on record — a reader who writes DD/MM takes `09/04/2026` as 9 April, and on an
acceptance ledger a misread date is a misread contract.

`nepalDateLong()` is the new formatter — `4 September 2026`, day first, month **named** — and it
is the form the legal registry's own `effectiveAdLabel` already used on the same panel, so the two
dates there finally agree. `month: 'long'` rather than short on purpose: current ICU gives en-GB's
short September as "Sept", which reads as a typo in a legal record. `nepalDateAd` is untouched and
the bills print exactly as before; the test asserts both forms against the same instant so the
distinction is on record. 52 suites / 618 tests.

---

### S675 — 2026-09-04 — every tenant Owner was locked out of the product by a filter that matched nothing

**Service worker `crest-v187` → `crest-v188`. No migration, no Edge Function deploy.**
**This was a live, total lockout of every client Owner, and accepting could not clear it.**

`AuthContext.js` read the acceptance ledger with
`.in('doc_type', docsRequiringReacceptance())` — but that helper returns document **objects**.
PostgREST stringifies whatever it is handed, so the filter was on the literal text
`[object Object]`, matched no rows, and returned `{ data: [], error: null }`. That is a *successful*
read stating the client has accepted nothing. The memo therefore held every Owner at the blocking
gate, and the acceptance they submitted was written correctly and then never seen by the re-read.

**Both of the safeguards built for exactly this screen were bypassed, and for the same reason.** The
read fails OPEN on error — but `legalErr` was null, so that branch was never reached. And S672 had
already been bitten by the objects-versus-strings distinction in the *other* direction, documented
at length in `legal-documents.md` with the same predicted symptom ("held at the blocking screen
permanently, with no way out except Sign Out"). `reacceptGuard.test.js` was written to catch it and
tests the helper, which was correct both times. **The fault was at the call site, which passed the
wrong one of two correct functions — so a test of the helper passes happily while the product is
shut.**

The fix is structural rather than a corrected argument: `reacceptDocTypes()` now exists beside
`docsRequiringReacceptance()`, and the rule is that anything comparing against the `doc_type`
COLUMN calls the former while anything reading a version or hash calls the latter — neither caller
does the `.map` itself. Two new tests read the **source** and fail if any `.in('doc_type', …)` in
`src/` is given anything but a types-shaped value; both were verified against the exact string that
shipped rather than assumed to work.

Found by shipping S674's recovery message first, which is the only reason the state was
diagnosable: the gate said "your acceptance was recorded but this screen did not refresh", which
distinguished a failed write from a failed read. 52 suites / 614 tests.

---
